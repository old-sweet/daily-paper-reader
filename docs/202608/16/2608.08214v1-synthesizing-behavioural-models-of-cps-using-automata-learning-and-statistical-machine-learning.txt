Title: Synthesizing Behavioural Models of CPS Using Automata Learning and Statistical Machine Learning

URL Source: https://arxiv.org/pdf/2608.08214v1

Published Time: Tue, 11 Aug 2026 00:48:22 GMT

Number of Pages: 49

Markdown Content:
# Synthesizing Behavioural Models of Cyber-Physical Systems Using Automata Learning and Statistical Machine Learning 

## Negin Ayoughi 1, Baharin A. Jodat 1, Armina Faghihi 1,Patricio Saavedra 2, Shiva Nejati 1* , Mehrdad Sabetzadeh 1

> 1

School of Electrical Engineering and Computer Science, University of Ottawa, Ottawa, Ontario, Canada. 

> 2

RabbitRun Technologies Inc., Ontario, Canada. *Corresponding author(s). E-mail(s): snejati@uottawa.ca; Contributing authors: negin.ayoughi@uottawa.ca; balia034@uottawa.ca; afagh007@uottawa.ca; pat@rabbit.run; m.sabetzadeh@uottawa.ca; 

Abstract 

Inferring behavioural models from system executions is essential for supporting formal verification and analysis of complex, heterogeneous cyber-physical systems (CPS). Automata learning provides an effective way to infer state machine mod-els from system executions. However, CPS inputs and outputs often consist of numeric time-series data, while automata learning algorithms assume inputs over a finite symbolic alphabet. As a result, raw numeric data must first be abstracted into a finite set of symbols. In this article, we present MELA, a passive automata learning approach enhanced with machine learning to synthesize behavioural models from numeric time-series data generated by CPS. MELA systematically combines statistical machine learning with automata learning to automatically abstract raw numeric signals into interpretable intervals that are strongly corre-lated with system states. Specifically, MELA uses information-theoretic variable selection and decision-tree-based range abstraction to transform numeric traces into symbolic representations suitable for automata learning. We evaluate MELA on two CPS: a commercial network intrusion detection system developed by our industry partner, RabbitRun Technologies, and a publicly available industrial autopilot benchmark from the aerospace domain. Compared with expertise-based numeric data abstraction, MELA reduces the number of states and transitions in the learned state machines by 49 .20% on average, while improving accu-racy by 41 .71% on average. Furthermore, the learned state machines support 

1arXiv:2608.08214v1 [cs.SE] 8 Aug 2026 system-level requirement verification and help practitioners explore behaviours that are not explicit in the system requirements. We make our implementation and experimental data available online (Ayoughi et al. 2026a).  

> Keywords: Automata learning, Cyber-physical systems, Behavioural model synthesis, Decision trees, Model checking, Intrusion detection, Simulink.

# 1 Introduction 

Cyber-physical systems (CPS) are complex and heterogeneous systems in which software controllers interact with physical processes through different sensors, actua-tors, and communication mechanisms. Their behaviour results from the interactions of many components, and their executions often generate long traces. Since this behaviour is difficult to understand directly, inferring state machines from execution data has been proposed as a way to obtain a high-level, interpretable, yet formal rep-resentation of system behaviour (Hranisavljevic et al. 2016; Medhat et al. 2015). Such state-machine models help engineers better understand system behaviour and enable formal verification and validation against requirements. Automata learning has long been used to learn state machines that capture system behaviour (Vaandrager 2017; Aichernig et al. 2022; Muskardin et al. 2021; Garhewal and Damasceno 2023; Neider et al. 2018). Automata learning aims to infer a finite-state behavioural model of a black-box reactive system from observed input/output behaviour. It has been successfully applied to a range of real-world systems, including communication and security protocols such as BLE (Pferscher and Aichernig 2022), MQTT (Tappler et al. 2017), and TCP (Fiterau-Brostean et al. 2016), as well as an elevator controller (Ovsiannikova et al. 2018), a biometric passport (Aarts et al. 2010), and autonomous driving systems (Hajnorouzi et al. 2025). Automata learning can be performed in either active or passive mode. In active learning, the assumption is that a well-defined and controllable interface for the system under learning is available, and the learner interacts with the system by issuing queries and observing the resulting outputs; in passive learning, the learner infers a model from offline execution data such as logs or traces. Active learning requires repeated interaction with the system under learning, as well as a reliable mechanism to query the system and steer it into different states. As a result, active learning can be costly and difficult to deploy in CPS, especially when online interaction and repeated execution are time-consuming and expensive, when a well-defined system interface is not available, and when conditions for steering the system into different states are unknown. For such systems, passive learning is considered a more efficient and effective alternative to active learning, provided that a diverse dataset of system behaviours is available (Aichernig et al. 2022). Regardless of these differences, both active and passive automata learning face a fundamental challenge when applied to CPS: their inputs and outputs are often con-tinuous, numeric signals that evolve over time rather than symbols from a small finite alphabet. Since automata-learning algorithms are inherently symbolic, they cannot 2operate directly on raw numeric time-series data and therefore require these signals to be abstracted into a finite set of symbols before learning can be applied. In many existing automata-learning applications, this abstraction step is either assumed to be available a priori or developed manually for the system under learning, rather than being derived through a systematic and interpretable procedure. Recently, some approaches have sought to automate this abstraction step, for example by using clus-tering or neural-network-based techniques (Medhat et al. 2015; Tappler et al. 2024; Plambeck et al. 2024; Hranisavljevic et al. 2020, 2016). For CPS, however, it is not sufficient to obtain discrete symbols; the abstractions must also reflect distinctions in system behaviour and remain interpretable to engineers. Hence, abstractions must be derived from the observed behaviour of the system, rather than from clustering that groups values by numeric similarity, and must be expressed in a form that engineers can validate and interpret. We propose the MachinE Learning-enhanced passive Automata learning approach (MELA) to derive state machines for CPS with numeric, time-series inputs and outputs. MELA exercises the system under learning (SUL) across diverse operating conditions using coverage-guided input generation (Ammann and Offutt 2008) and collects the resulting system execution traces. It then converts these traces into sym-bolic traces suitable for automata learning by abstracting raw numeric values into a finite set of intervals. To make these intervals behaviourally meaningful, MELA uses decision-tree learning to identify abstractions that are strongly associated with the system states. MELA then applies passive automata learning (G´ omez 2010) to the symbolic traces to infer state machines that capture SUL’s behaviours. Finally, MELA evaluates whether each learned state machine conforms to the observed behaviour of the SUL by measuring its accuracy on unseen executions. If the measured confor-mance falls below a user-defined threshold, MELA generates additional executions and repeats the abstraction and learning process. Once the learned state machine satisfies the required conformance threshold, MELA uses temporal-logic model check-ing (Cimatti et al. 2002) to verify the system requirements over the learned state machine. 

Contributions. This article presents MELA, a systematic approach for synthe-sizing interpretable behavioural models of CPS from numeric time-series executions. The key characteristic of MELA is that it derives interval-based abstractions accord-ing to their ability to distinguish system states. This yields abstractions that are both interpretable to engineers and effective for passive automata learning. This arti-cle extends our previous conference paper (Ayoughi et al. 2024), published at the 27th International Conference on Model Driven Engineering Languages and Systems (MODELS 2024). Compared to our earlier paper, this article offers major expansions in the following areas: 

• We generalize MELA from an approach developed for a specific industrial network-intrusion-detection system into a general approach for synthesizing and analyzing behavioural models of CPS with numeric time-series inputs and outputs. We strengthen the model-learning process to automatically resolve potential incon-sistencies introduced during abstraction and extend our previous work beyond 3behavioural-model synthesis to evaluating model accuracy and supporting the formal verification of system requirements. 

• We extend the empirical evaluation by applying MELA to a second CPS case study from the aerospace domain, in addition to the network system presented in our previous work. 

• We demonstrate how MELA-generated state machines identify unspecified behaviours and characterize the conditions under which requirements hold. 

Findings. To assess the benefits of MELA’s automated abstraction, we compare the state machines learned by MELA with those learned by a variant of MELA in which the automated abstraction step is replaced with expertise-based abstractions. We evaluate both approaches using two criteria: model complexity, measured by the number of states and transitions, and accuracy. Our results show that MELA generates state machines with 49 .20% fewer states and transitions on average, while achieving 41 .71% higher accuracy on average than the expertise-based variant. These results show that combining automata learning with our automated abstraction leads to more concise state machines that more accurately represent the behaviour of the SUL. We further use the learned state machines to verify system requirements and show that the verification results align with domain knowledge or system documentation. Finally, we use the learned state machines to examine behaviours that are not explicitly specified in the requirements. In the network case study, MELA shows how intermediate states respond to different traffic conditions, revealing behaviour that the requirements leave unspecified. In the aerospace case study, MELA identifies the input conditions under which the system satisfies altitude-reaching requirements. 

Replication Package. Our framework, empirical data, and supplementary material are publicly available (Ayoughi et al. 2026a). 

Organization. Section 2 motivates the need for synthesizing behavioural models for CPS. Section 3 presents MELA, our approach for deriving state-machine models for CPS. Section 4 presents the evaluation. Section 5 compares our work with related work. Section 6 concludes the article. 

# 2 Motivation 

We motivate the challenges of learning symbolic and interpretable behavioural models for numeric CPS using a real-world network intrusion detection system (IDS). Figure 1 provides an overview of a router system equipped with an IDS. The router connects local users to the Internet and external networks. It hosts an IDS that monitors exter-nal network traffic to identify denial-of-service (DoS) and distributed denial-of-service (DDoS) attacks originating from external users acting as attackers and targeting local users. DoS attacks flood their target with excessive traffic from a single source, whereas DDoS attacks employ multiple sources to perform a more extensive attack (Zargar et al. 2013). The IDS continuously monitors network traffic and updates its state based on features extracted from traffic flows. The IDS produces an output state indicating its assessment of the monitored traffic. This output state can be one of the following: 

Safe , indicating no signs of an attack; Warning , indicating unusual but not neces-sarily harmful traffic; Tending Warning , indicating elevated unusual traffic that may 4Simulated  Vulnerabilies    

> in Local Users
> Simulated Normal and Attack F lows
> from External Users
> Router
> (IDS-enabled)
> Internet
> Local Users Router
> (IDS-enabled)
> Internet
> ...
> Normal Attacker Vulnerable
> ...
> External Users
> (a) Deployment of an IDS-enabled router in the real-world
> (b) Our testbed simulating the real-world deployment of an IDS-enabled router

Fig. 1 : Real-world deployment of a router system enabled by a network intrusion detection system (IDS) degrade network performance; Tending Alert , indicating highly suspicious traffic; and Alert , indicating harmful or malicious activity. The IDS is expected to satisfy the following two requirements: 

φ1 =“When attacks happen, the system shall change state in a staged manner from safe to warning and from warning to alert” , and 

φ2 =“When attacks are stopped, the system shall restore in a staged manner its state from alert to warning, and from warning to safe.” 

These requirements describe how IDS should move among the Safe , Warning , and 

Alert states when attacks start or stop. They are, however, only a partial specification of the IDS behaviour: while the IDS has five states – Safe , Warning , Alert , Tending Warning , and Tending Alert – the requirements refer only to the first three, leav-ing the behaviour of the two tending states unspecified. Engineers therefore need a behavioural model, learned from system executions, both to verify φ1 and φ2 and to examine how the IDS behaves in the Tending Warning and Tending Alert states under different traffic conditions. Constructing a behavioural model for IDS raises a key challenge: the IDS operates on network traffic with numeric features. In particular, one important IDS input is the number of flows extracted from network traffic. To generate a behavioural model, this numeric input must be abstracted into symbolic values. Abstracting numeric values into symbols manually or in an ad-hoc manner can lead to poor behavioural models: an overly coarse abstraction may merge traffic conditions that correspond to different IDS behaviours, while an overly fine abstraction may introduce too many symbols and produce an unnecessarily complex model. MELA therefore learns these abstractions automatically using decision-tree learning, which groups numeric values according to their association with the IDS output states. This yields interpretable symbols at an appropriate level of granularity: fine-grained enough to distinguish different IDS behaviours, yet coarse-grained enough to avoid unnecessarily complex models. Figure 2 shows a simplified state machine learned by MELA for the IDS. The model contains five states, corresponding to the five IDS operational states, and its transitions are labelled with abstract input symbols that characterize traffic condi-tions. These symbols capture two key aspects of the traffic: whether attack traffic is injected, represented by Attack and Not Attack , and the observed flow volume, rep-resented by Low Flow , Med Flow , and High Flow . Rather than requiring engineers to define these flow-volume categories manually, MELA derives them automatically using decision-tree learning. For the model in Figure 2, MELA partitions the numeric range 5Attack  and  High Flow                

> Attack  and Med Flow
> Attack  and  Med Flow
> Attack and Med Flow
> Attack and High Flow Attack and Med Flow
> Not Attack and Low Flow
> Attack and
> Low Flow
> Not Attack and
> Med Flow
> Attack and
> High Flow
> Not Attack and
> Low Flow
> Attack and Med Flow
> Safe Warning
> Tending
> Warning
> Alert
> Tending
> Alert

Fig. 2 : A simplified example of a state machine learned for a network intrusion detec-tion system (IDS) by our approach (MELA). of network flows, [0 , ∞), into three interpretable intervals: Low Flow =[0,454), Med Flow =[454,3500), and High Flow =[3500 , ∞). Thus, a transition labelled with Attack 

and Med Flow indicates that the IDS changes state when attack traffic is injected and the number of observed flows is between 454 and 3500. The state machine in Figure 2 supports two engineering tasks: verifying requirements φ1 and φ2, and explor-ing behaviour not specified by these requirements, namely the IDS behaviour in the 

Tending Warning and Tending Alert states. 

# 3 ML-Enhanced Automata Learning (MELA) 

Figure 3 provides an overview of MELA. MELA takes as input a system under learning (SUL) and a set of requirements for the SUL, and proceeds in six steps. First, MELA executes the SUL to generate time-series data. Second, it converts the time-series data into traces suitable for automata learning. Third, MELA selects inputs that are most predictive of the system state and therefore most relevant for specifying the learned state machine. It then uses decision-tree learners to abstract the numeric value ranges of the selected inputs into symbolic categories. Fourth, MELA learns state machines from the abstract traces produced in Step 3. Fifth, MELA checks whether each learned state machine sufficiently conforms to the SUL by evaluating its accuracy against unseen system executions. If the accuracy falls below a user-defined threshold, MELA returns to Step 1 to generate additional time-series data and repeats the learning process; otherwise, the state machine is passed to the final step. Sixth, MELA verifies the requirements specified in temporal logic over the learned state machine and returns one of three outcomes: satisfaction , vacuous satisfaction , or violation , depending on whether each requirement is satisfied, vacuously satisfied, or violated. Below, we first provide the background needed for MELA in Section 3.1. In Section 3.2, we describe the IDS discussed in Section 2 in more detail to provide a running example that illustrates the different steps of MELA. Next, in Sections 3.3 to 3.8, we describe each step of MELA in detail. Finally, in Section 3.9, we discuss 6Environment Simulation and  

> Data Generation
> Trace Creation
> Trace Abstraction
> Automata Learning
> System Under Learning (SUL)
> Requirement
> Trace
> Yes
> No Learned Model
> (Moore Machine)

MELA 

# ,

> Accuracy < Threshold ?

1 2 3

4

> Conformance Checking

5

> Verification

6 

> Satisfaction Violation Vacuous Satisfaction

Fig. 3 : Our approach for deriving and analyzing state machines, MELA the determinism of the learned state machines and the soundness of the verification results obtained by MELA. 

## 3.1 Background 

In this section, we provide background on the formal notation for time-series data, the automata-learning algorithm used by MELA, and the learned state-machine representation. 

Time-series data notation. Provided with a SUL S, we denote an input for S

as u = ( u1, u 2 . . . u m) and an output for S as o = ( o1, o 2 . . . o n) where m is the number of system inputs, n is the number of system outputs, and each uj and each oj is a time-series vector for some input and some output of S, respectively. A time series is a function v : [0 , T ] → D where the interval [0 , T ] is a time domain with duration T ,and D is the range of v. The time series v can be continuous-valued when D is an interval of real numbers (e.g., [0 , 1]). The time series v can be discrete-valued when D

is a finite or countable set (e.g., D = {UDP , TCP } or D = {1, . . . , 6000 }). As discussed in Sections 1 and 2, our goal is to abstract numeric input values, whether continuous or discrete, into symbolic categories that can be used to learn automata. 

Automata learning and state machine formalism. MELA learns a state machine using Regular Positive and Negative Inference (RPNI) (De la Higuera 2010; G´ omez 2010; Aichernig et al. 2022; von Berg and Aichernig 2025a), a well-established passive automata-learning algorithm. RPNI was originally developed for learning deterministic finite automata (DFAs), but its variants support richer automata models, including Mealy and Moore machines (Muskardin et al. 2021). While Mealy machines associate outputs with transitions, Moore machines associate outputs with states. In our case studies, MELA learns Moore machines because the observed outputs correspond to operational system states rather than transition-specific outputs. A Moore machine is a tuple M = ( I, O, Q, q 0, δ, λ ), where I is the set of input symbols, 

O is the set of output symbols, Q is the set of states, q0 ∈ Q is the initial state, 7δ : Q × I → Q is the transition function, and λ : Q → O is the state-labelling function, mapping each state to its output symbol (Giantamidis et al. 2021). RPNI first constructs a prefix tree automaton (PTA) from the abstract traces (von Berg and Aichernig 2025a). The PTA is a tree-shaped automaton in which each trace corresponds to a unique path from the initial state. Hence, before any generalization, the PTA captures exactly the behaviours observed in the traces. RPNI then generalizes the PTA by iteratively merging compatible states using the standard red/blue state-merging strategy (von Berg and Aichernig 2025a). Starting from the root state, the algorithm repeatedly considers merges between red and blue states that have the same output symbol. Accepted merges generalize the observed behaviours and gradually produce a compact Moore machine. 

## 3.2 Running Example 

Figure 4 presents a conceptual model of the IDS introduced in Section 2, capturing its context, inputs, and outputs, so that the IDS can be used as a running example to illustrate the different steps of MELA. In Figure 4, inputs are shaded green, the SUL blue, and the system-state output orange. The blue dashed boundary denotes the network environment, including external and local users and network flows, while the purple dotted boundary denotes router-extracted traffic features provided as IDS inputs. As shown in the network environment, external users are either normal users or attackers (DoS/DDoS). External users generate network flows that are forwarded to local users through the router. Each flow is characterized by the communication 

protocol ; the destination port number of the incoming traffic; and packet size . The attribute protocol takes values from a finite set (e.g., {TCP , UDP }); port takes values in [0 , 65535]; and packet size is measured in bytes. The router collects information from all flows traversing it and computes the aggre-gate flow metrics num flows and num unreplied . The metric num flows denotes the total number of flows passing through the router, while num unreplied captures the number of flows that remain unacknowledged by local users. The aggregate met-rics (i.e., num flows and num unreplied ), together with the per-flow attributes (i.e., 

protocol , port , packet size ), are provided to the IDS operating on the router to update its operational state. The IDS selects one of the states Safe , Warning , Tending Warning , Tending Alert , or Alert .

## 3.3 Step 1: Environment Simulation and Data Generation 

In the first step, MELA uses a simulator to exercise the SUL under different input stimuli by modelling its environment and how input values evolve over time. MELA randomly generates inputs using existing parameterized time-series data generation techniques (Tuncali et al. 2019; Arrieta et al. 2019), executes the SUL, and collects the resulting outputs. To ensure that the learned automata effectively capture the behaviours of the SUL, we need to generate inputs that exercise the SUL under varied scenarios and yield out-puts that adequately capture the SUL’s behaviours. To increase the adequacy of the 8Network Intrusion Detection  

> System (IDS)
> System State
> type: System State Type [0, T]
> << enumeration >>
> System State Type
> Safe
> Warning
> Alert
> Tending Warning
> Tending Alert
> Aggregate Network Flow Metric
> value: Numeric [0, T]
> Router
> Network Flow
> protocol: Protocol Name [0, T]
> port: Numeric [0, T]
> packet_size: Numeric [0, T]
> << enumeration >>
> Protocol Name
> UDP
> TCP
> ...
> Local User
> External User
> value: External User Type [0, T]
> << enumeration >>
> External User Type
> Normal
> DoS Attacker
> DDos Attacker
> outputs
> 1
> 1
> receives
> 1
> manages
> 1
> *
> flows to
> *
> 1
> generates
> 11..*
> *
> Num_flows
> Num_unreplied
> reports
> *
> 1
> Data Collected by Router
> Network Environment

Fig. 4 : Conceptual model for the IDS discussed in Section 2. The inputs are shaded green, the SUL is shaded blue, and the observable system-state output is shaded orange. The boundaries distinguish the network environment and the data collected by router. generated data, MELA employs established black-box test coverage criteria for soft-ware testing based on system-state coverage (Ammann and Offutt 2008). Specifically, after each execution, MELA updates the achieved state coverage; when coverage no longer increases, MELA stops generating additional inputs and proceed to the next step. Coverage saturation happens either when the generated outputs cover all sys-tem states or when after several consecutive iterations, the outputs do not cover any new states, indicating that data generation is unlikely to yield further improvements in state coverage. 

## 3.4 Step 2: Trace Creation 

The second step of MELA converts the time-series data generated in Step 1 into traces by sampling each time-series vector at a fixed period δ. This yields finite, discrete-time traces suitable for automata learning. Specifically, each time-series vector v : [0 , T ] →

D is converted into a sequence of samples v0, v 1, . . . , v k, where v0 = v(0), v1 = v(δ), 

v2 = v(2 · δ), . . . , and vk = v(k · δ), with k · δ = T . Let u = ( u1, u 2 . . . u m) be an input of the SUL, and o be the output vector related to the system state. A trace τ

9(a) Before trace abstraction 

> ([Normal, 80, TCP, 173, 82, 60], Safe), ([DoS, 80, TCP, 122, 767, 640], Warning), ([DoS, 80, TCP, 178, 4331, 1250], Alert) ...
> (b) After trace abstraction
> ([Normal, Low, Low], Safe), ([DoS, Med, Med], Warning), ([DoS, High, High], Alert) ...

Fig. 5 : Traces for the IDS: (a) an example of an actual trace and (b) the same trace after trace abstraction. corresponding to u and o is represented as follows: 

τ = (( u01, u 02, . . . , u 0

> m

, o 0), (u11, u 12, . . . , u 1

> m

, o 1), . . . , (uk 

> 1

, u k 

> 2

, . . . , u km, o k)) where k is the number of steps with time-step size δ in time domain [0 , T ]. For each l ∈ { 0, . . . , m }, ujl denotes the value of the input vector ul at step j ∈ { 0, . . . , k },and oj denotes the value of the output vector o at step j. We denote by TR the set of traces generated in Step 2. For example, Figure 5(a) shows small excerpts from traces generated for the IDS discussed in Section 2. Each tuple consists of sampled IDS input values followed by the corresponding IDS state. Specifically, the values inside the brackets are, in order, sampled from the IDS inputs External user type , port , protocol , packet -size , num flows , and num unreplied . The value outside the brackets is the IDS state observed for those inputs. 

## 3.5 Step 3: Trace Abstraction 

This step consists of three substeps. First, MELA selects the SUL inputs that should be included in the learned state machine to enable verification of the system requirements. 

Second, MELA abstracts the raw numeric ranges of the selected SUL inputs into symbolic categories and replaces the numeric values in the set of traces obtained in Step 2 with their corresponding abstract symbols. Since converting numeric traces into abstract traces may lead to inconsistencies, i.e., traces with identical abstract inputs may be associated with different output states, in the third substep, MELA resolves any inconsistencies in the abstract traces. Below, we describe these three substeps, which we refer to as input selection , range abstraction , and inconsistency resolution ,respectively. 

3.5.1 Input Selection 

MELA first prompts the user to identify the inputs referenced in the SUL require-ments, since these inputs are relevant to the requirements and must be preserved for verification. MELA then analyzes the remaining inputs by computing their informa-tion gain with respect to the system state, selecting those that are most predictive 10 of the system state. This ensures that the learned state machine includes both the requirement-referenced inputs and the additional inputs needed to distinguish system states and characterize state transitions. To this end, MELA constructs a table from the set TR of traces obtained from Step 2 by treating each sampled input-output tuple ( uj

> 1

, u j

> 2

, . . . , u jm, o j ) from every trace τj ∈ TR as one row. The table contains one column for each input u1, . . . , u m

and a final column for the system-state output o. Using this table, MELA computes the information gain of each input not referenced in the requirements, ranks these inputs by their information-gain values, and extends the set of requirement-referenced inputs with those whose information gain exceeds a threshold. Following prior work, MELA sets this threshold based on the standard deviation of the information-gain values across all inputs (Prasetiyowati et al. 2021). Finally, MELA refines the traces in TR by removing all inputs that were not selected. For example, for the IDS case study discussed in Section 2, requirements φ1 and 

φ2 describe the expected state changes when attack traffic starts and stops. Based on domain expertise, External User type is retained as a requirement-referenced input because it distinguishes normal users from attackers. MELA then computes information gain over the remaining IDS inputs and selects num flows and num -unreplied as the most predictive inputs for the IDS state. The final selected input set therefore consists of External User type , num flows , and num unreplied , and other IDS inputs are removed from the traces. 

3.5.2 Range Abstraction 

After input selection, MELA derives interval-based abstractions for the selected numeric inputs using decision-tree learners. For each selected numeric input u, MELA constructs a two-column table from the trace set TR . The first column contains sam-pled values of u, and the second column contains the corresponding system-state values. Specifically, for every trace τ ∈ TR and every sampling step j, MELA adds one row of the form ( uj , o j ), where uj is the value of u at step j and oj is the system state at that step. MELA then trains a decision tree using the values of u as inputs and the corresponding system-state values as categorical class labels. Each tree leaf records: (1) the number of samples assigned to the leaf ( support ), and (2) the purity of the leaf (confidence ), indicating the degree to which the samples belong to the same system state. A higher purity therefore indicates a stronger association with a single system state. Each leaf is connected to its parent node through a condition of the form uj < c ,where c is a constant within the range of uj . MELA selects the leaves whose support and purity both exceed user-defined thresholds and extracts the corresponding con-ditions linking those leaves to their parent nodes. Let c1, . . . , c r denote the constants appearing in the selected conditions in ascending order. MELA uses these constants to partition the range of u into the intervals [0 , c 1), [ c1, c 2), . . . , [ cr, ∞). MELA then assigns a unique abstract symbol to each interval and replaces every numeric sample 

uj in the traces of TR with the symbol corresponding to the interval containing uj .For example, Figure 6 shows the decision tree used to abstract the numeric input 

num flows . As illustrated in Figure 5(b), the IDS traces contain tuples relating num -flows and other IDS inputs to system states. MELA constructs the decision tree 11 Node 1    

> Count = 1521
> Node 2: Safe
> Count = 507
> Purity = 1
> Node 3
> Count = 1014
> Node 4: Warning
> Count = 510
> Purity = 0.99
> Node 5: Alert
> Count = 504
> Purity = 0.98

#flows  > 454 #flows  ≤ 454 

#flows  ≤ 3500  #flows  > 3500 Fig. 6 : Illustrating how a decision tree is used to abstract the numeric range of the num flows input attribute. The numeric range of num flows is abstracted to the enumerated range [Low, Med, High] such that Low =[0 , 454), Med =[454 , 3500), and 

High =[3500 , ∞). in Figure 6 . In this example, the maximum tree depth is set to three, while the support and purity thresholds are set to 20% of the total data and 70%, respectively. Figure 6 shows the resulting tree leaves together with their support and purity values. MELA selects Node 2, Node 4, and Node 5 because all three satisfy the support and purity thresholds. Based on the conditions linking these nodes to their parent nodes, MELA partitions the range of num flows into the intervals [0 , 454), [454 , 3500), and [3500 , ∞). The number of intervals is determined automatically by the decision tree and is therefore not fixed a priori. For readability, these intervals are labelled as Low , Med , and High , respectively; specifically, Low =[0,454), Med =[454,3500), and 

High =[3500 , ∞). After input selection and range abstraction, it converts the numeric trace in Figure 5(a) into the abstract trace shown in Figure 5(b). 

3.5.3 Inconsistency Resolution 

Converting numeric traces into abstract traces may introduce inconsistencies when the abstraction is too coarse. An inconsistency occurs when two traces share the same abstract input history but reach different output states. For example, consider two traces τ1 and τ2 with length three for a system with output states state0 , state1 ,

state2 , and state3 , and one numeric input x:            

> τ1= ([35] ,state0 ),([47] ,state1 ),([66] ,state2 )
> τ2= ([35] ,state0 ),([52] ,state3 ),([77] ,state2 ).

In τ1 and τ2, the values in brackets denote values of x. Suppose range abstraction maps the range of x to three symbols: Low = [0 , 40), Med = [40 , 60), and High =[60 , ∞). This yields the following abstract traces: 12 ˆτ1 = ([ Low ], state0 ), ([ Med ], state1 ), ([ High ], state2 )ˆτ2 = ([ Low ], state0 ), ([ Med ], state3 ), ([ High ], state2 ).

The abstract traces ˆ τ1 and ˆ τ2 are inconsistent in the second step: The first steps of the two traces are identical. But in the second step after receiving identical input [ Med ], the traces reach different states, namely state1 and state3 . Such traces cannot be used directly to learn a deterministic state machine (Linz and Rodger 2022). We define inconsistent traces as follows. Let ¯ τ1 and ¯ τ2 be two abstract traces of length k obtained after range abstraction. The traces are inconsistent at step j ≤ k

iff they are identical in the first j − 1 steps, but at jth step they have the same abstract inputs and different outputs. A trace set obtained after range abstraction is inconsistent if it contains at least one pair of inconsistent traces. To resolve inconsistencies, MELA proceeds by refining the numeric-range abstrac-tion by iteratively increasing the decision-tree depth, up to a user-defined maximum, and reconstructing the induced intervals. This addresses inconsistencies caused by overly coarse abstractions. In the example above, the inconsistency between ˆ τ1 and ˆ τ2

can be resolved by partitioning Med =[40 , 60) into finer intervals such as [40 , 48) and [48 , 60). The finer partition separates values that were previously mapped to the same abstract symbol but led to different system states. Not all inconsistencies can be resolved by refining the abstraction. In CPS, the SUL may exhibit unstable behaviour, producing different outputs for the same inputs due to environmental uncertainty or noise. Moreover, increasing the decision-tree depth beyond a user-defined limit may overfit the data and increase the complexity of the learned state machine. Therefore, if inconsistencies persist after refinement, MELA removes traces iteratively. Trace removal is used only as a last resort after abstraction refinement reaches the maximum user-defined depth and is intended to address residual inconsistencies arising from unstable or noisy CPS behaviour. For each trace, MELA counts its pairwise inconsistencies with other traces and removes the trace with the largest count. Ties are broken randomly. This process continues until the abstract trace set is consistent. As reported in Section 4, in our case studies, range-abstraction refinement resolved most inconsistencies, and only 0 .1% of traces had to be removed. 

## 3.6 Step 4: Automata Learning 

In the fourth step, MELA uses the RPNI algorithm (Section 3.1) to learn a Moore machine from the abstract traces generated in Step 3. The input and output alphabets of the Moore machine are defined as follows. Let m′ be the number of inputs retained after input selection, and let A1, A 2, . . . , A m′ denote the finite sets of abstract symbols associated with these retained inputs after range abstraction in Step 3. These sets are finite because Step 3 replaces each numeric range with a finite set of abstract symbols. The input alphabet of the Moore machine is then defined as I = A1 × A2 × · · · × Am′ .Hence, each element of I represents one possible combination of abstract values for the retained inputs and is treated as a single input symbol of the Moore machine. The output alphabet O is the finite set of observable system states. For example, Figure 2 shows a Moore machine learned for the IDS case study. In this machine, the abstract symbols for the number of flows are Low Flow , Med Flow ,13 and High Flow , whereas the abstract symbols for external user type are Attack and 

Not Attack . The set of input symbols I for this machine is I = { (Not Attack, Low Flow ), ( Not Attack, Med Flow ), ( Not Attack, High Flow ), ( Attack, Low Flow ), (Attack, Med Flow ), ( Attack, High Flow ) }. The set of output symbols is O =

{Safe , Warning , Tending Warning , Tending Alert , Alert }, which corresponds to the IDS states. RPNI can learn state machines from both positive traces, which capture behaviours to include, and negative traces, which capture behaviours to exclude. In our setting, MELA uses only positive traces, namely the abstract traces generated in Step 3 from observed SUL executions. We do not provide negative traces because the available requirements and documentation do not precisely characterize invalid behaviours. Nev-ertheless, RPNI remains suitable because it can learn effective state machines from positive traces alone (Bartocci et al. 2020, 2021; Agostinelli et al. 2021). As discussed in Section 3.1, state merging in RPNI may introduce behaviours that do not appear in the original traces. Such generalization can affect verification results. To validate these results against observed executions, as we discuss in Section 3.8, MELA uses the PTAs generated by RPNI to distinguish results supported by the original traces from those caused only by state merging. In the rest of this article, depending on the context, we interchangeably refer to Moore machines as either state machines or automata. 

## 3.7 Step 5: Conformance Checking 

The fifth step of MELA assesses whether the state machine learned in Step 4 conforms to the SUL. Following established practice in automata learning (Aichernig et al. 2022), MELA evaluates conformance using a test set, i.e., a set of traces generated from the SUL but not used during learning. This check is important because, in passive learning, the quality of the learned automaton depends on the completeness and diversity of the traces used for inference; if the learning traces cover only part of the SUL behaviour, the learned automaton may deviate from the actual system. To measure conformance, MELA first constructs an independent set of test traces .Specifically, MELA randomly generates data using Step 1, converts this data into traces using Step 2, and abstracts these traces using the interval abstractions learned in Step 3. MELA then computes conformance as the percentage of test traces accepted by the learned automaton. A test trace is accepted if the automaton contains a path from the initial state that follows the trace’s abstract input symbols and produces the same system states as those in the trace. If the resulting conformance exceeds a user-defined threshold the automaton is considered sufficiently conformant and is used for requirements analysis. Otherwise, MELA generates additional data and repeats the trace abstraction, automata learning, and conformance-checking steps. 

## 3.8 Step 6: Verification 

The final step of MELA uses temporal-logic model checking to verify SUL require-ments on the Moore machines learned in Step 4. MELA first translates the learned Moore machine into the input language of NuSMV (Cimatti et al. 2002). MELA then 14 1 MODULE main  

> 23

VAR  

> 4

input : { i1, i2, ..., im}; -- input alphabet I 

> 5

state : { q0, q1, ..., qn}; -- state set Q 

> 67

DEFINE  

> 8

label := case  

> 9

state = q0 : λ(q0); 

> 10

state = q1 : λ(q1); 

> 11

...  

> 12

state = qn : λ(qn); 

> 13

esac; -- output label of the current state  

> 14 15

ASSIGN  

> 16

init(state) := q0; -- initial state  

> 17 18

TRANS  

> 19

(state = q & input = i & next(state) = δ(q, i )) |  

> 20

... ; -- one disjunct for each defined transition δ(q, i ) of the Moore machine  

> 21 22

-- User-provided temporal-logic formulas of the SUL requirements.  

> 23

SPEC φ; -- if φ is a CTL formula  

> 24

LTLSPEC φ; -- if φ is an LTL formula 

Fig. 7 : Template for encoding a Moore machine M = ( I, O, Q, q 0, δ, λ ) in the input language of NuSMV. prompts users to encode the SUL requirements as temporal-logic properties, which are checked by NuSMV on the NuSMV code of the Moore machine. Because pas-sive automata learning generalizes from a finite set of observed traces, the learned Moore machine may include behaviours that were not observed in the SUL executions. Therefore, when satisfaction or violation of a requirement may be affected by this gen-eralization, MELA uses the PTA produced by RPNI, as discussed in Section 3.1, to interpret the verification result with respect to the behaviours observed in the SUL execution traces. Based on this analysis, MELA classifies each requirement as satisfied ,

vacuously satisfied , or violated . Below, we explain the NuSMV encoding of learned Moore machines and present MELA’s procedure for interpreting NuSMV results as verdicts over the SUL execution traces. 

3.8.1 Moore Machine to the Input Language of NuSMV 

Figure 7 presents the template used by MELA to translate a Moore machine M =(I, O, Q, q 0, δ, λ ) into the input language of NuSMV . The input alphabet I is encoded as the NuSMV variable input , while the state set Q is encoded as the variable 

state . The initial Moore-machine state q0 is specified by assigning it to init(state) .The state-labelling function λ is encoded in the DEFINE block, where label maps each state q ∈ Q to its corresponding output label λ(q). The transition function δ is encoded in the TRANS block as a Boolean constraint over the current state, the current 15 input, and the next state. Specifically, for each defined transition δ(q, i ), the gener-ated NuSMV model contains a disjunct stating that, when state is q and input is i,the next value of state must be δ(q, i ). This encoding therefore preserves the state-based outputs and transition structure of the learned Moore machine. User-provided temporal-logic requirements are then added to the generated model. These require-ments are expressed in Computation Tree Logic (CTL) (Clarke et al. 1986) or Linear Temporal Logic (LTL) (Pnueli 1977), two standard temporal logics for specifying sys-tem requirements (Clarke et al. 2018). CTL expresses branching-time properties over computation trees and allows quantification over possible execution paths (Clarke et al. 1986), whereas LTL expresses linear-time properties over individual executions (Pnueli 1977). In NuSMV , CTL properties are specified using SPEC , while LTL properties are specified using LTLSPEC .Figure 8 shows the NuSMV code capturing the Moore machine in Figure 2. In this Figure, lines 39 and 40, show two user-provided CTL formulas that partially capture requirements φ1 and φ2 described in Section 2. In CTL, the operator “AG” means that the formula must hold globally on all computation paths, and the operator “AX” means that the formula must hold in all immediate successor states. Specifically, line 39 states that whenever the IDS is in Safe and observes attack high flow , all next states must be either Warning or TendingWarning . This partially captures φ1 by requiring a staged response to attacks. Line 40 states that whenever the IDS is in Warning and observes notattack low flow , all next states must be Safe . This partially captures 

φ2 by requiring recovery toward the safe state when attacks stop. The full encoding of 

φ1 and φ2 in CTL is provided in our supplementary material (Ayoughi et al. 2026d). 

3.8.2 Interpreting Verification Results 

Figure 9 shows how MELA interprets the satisfaction and violation results returned by 

NuSMV . The interpretation differs for universal, existential, and hybrid properties. In this section, we focus on universal properties, e.g., the properties on lines 39 and 40 in Figure 8, since all properties in our case studies are captured as universal properties. The interpretation process for existential and hybrid properties, which is similar to the one discussed here, is provided in the supplementary material (Ayoughi et al. 2026a). Based on Figure 9, when NuSMV reports that a requirement is satisfied, MELA checks whether the satisfaction is vacuous. Vacuous satisfaction can occur for impli-cation requirements when the antecedent is never reached along any path (Beer et al. 1997). For a CTL requirement of the form AG (α → β), MELA checks the auxiliary formula EF (α), which holds when α is reachable along some path. If EF (α) is vio-lated, then α is unreachable and the requirement is satisfied vacuously; otherwise, the antecedent occurs in at least one reachable state, and the satisfaction is non-vacuous. Lines 43 and 44 in Figure 8 show examples of the additional CTL specifications gener-ated by MELA for this purpose. For an LTL requirement of the form G(α → β), MELA checks the auxiliary formula G(¬α). If G(¬α) is satisfied, then α never occurs on any path, and the requirement is satisfied vacuously. Otherwise, if G(¬α) is violated, then the requirement is satisfied non-vacuously. When NuSMV reports that a requirement is violated, MELA checks whether the violation is also exhibited by the PTA. To do so, MELA encodes the PTA in 16 1 MODULE main  

> 23

VAR  

> 4

input : {notattack_low_flow, attack_low_flow, notattack_med_flow,  

> 5

attack_med_flow, notattack_high_flow, attack_high_flow};  

> 6

state : { q0, q1, q2, q3, q4};  

> 78

DEFINE  

> 9

label := case  

> 10

state = q0 : Safe;  

> 11

state = q1 : Warning;  

> 12

state = q2 : TendingWarning;  

> 13

state = q3 : TendingAlert;  

> 14

state = q4 : Alert;  

> 15

esac;  

> 16 17

ASSIGN  

> 18

init(state) := q0; 

> 19 20

TRANS  

> 21

(state = q0 & input = notattack_low_flow & next(state) = q0) |  

> 22

(state = q0 & input = attack_low_flow & next(state) = q0) |  

> 23

(state = q0 & input = attack_med_flow & next(state) = q1) |  

> 24 25

(state = q1 & input = notattack_low_flow & next(state) = q0) |  

> 26

(state = q1 & input = notattack_med_flow & next(state) = q1) |  

> 27

(state = q1 & input = attack_med_flow & next(state) = q2) |  

> 28

(state = q1 & input = attack_high_flow & next(state) = q4) |  

> 29 30

(state = q2 & input = attack_high_flow & next(state) = q3) |  

> 31 32

(state = q3 & input = attack_med_flow & next(state) = q1) |  

> 33

(state = q3 & input = attack_high_flow & next(state) = q4) |  

> 34 35

(state = q4 & input = attack_med_flow & next(state) = q1) |  

> 36

(state = q4 & input = attack_high_flow & next(state) = q4);  

> 37 38

-- User-provided temporal-logic specifications for the SUL requirements φ1

and φ2 

> 39

SPEC AG ((input = attack_high_flow & label = Safe) -> AX (label = Warning | label = TendingWarning))  

> 40

SPEC AG ((input = notattack_low_flow & label = Warning) -> AX (label = Safe))  

> 41 42

-- Specifications added by MELA for vacuity checking  

> 43

SPEC EF (input = attack_high_flow & label = Safe)  

> 44

SPEC EF (input = notattack_low_flow & label = Warning) 

Fig. 8 : NuSMV code for the Moore machine in Figure 2 using the template in Figure 7. 

NuSMV using the template in Figure 7, since the PTA is a tree-structured Moore machine whose states, transitions, and output labels can be directly represented in 

NuSMV . Because NuSMV evaluates CTL requirements over infinite paths, MELA adds a self-loop to the last state of each PTA trace to encode finite traces as infinite stuttering executions. Since the PTA captures behaviours before state merging, this check distinguishes violations supported by observed traces from those introduced by state merging. If the PTA also violates the requirement, the violating behaviour is 17 Check reachability      

> of antecedent , if present
> Model-check PTA against requirement
> Yes
> No
> Satisfaction
> Vacuous Satisfaction
> Yes
> Violation
> No
> Yes
> Model-check state
> machine against requirement 𝝋
> 𝜶
> 𝝋
> No
> 𝝋
> Does the state
> machine satisfy ?
> 𝜶 Is reachable?
> 𝝋
> Does the PTA
> satisfy ?

Fig. 9 : MELA’s process in Step 6 for interpreting the verification results. supported by the observed traces, and MELA reports the requirement as violated. If the PTA satisfies the requirement, the violation in the learned Moore machine is attributed to state merging. In this case, MELA reports vacuous satisfaction if the requirement is satisfied vacuously on the PTA; otherwise, it reports satisfaction. 

## 3.9 Determinism, Soundness, and Vacuity 

In this section, we discuss two formal properties of the state machines produced by MELA: determinism and soundness. We also explain how vacuous satisfaction verdicts should be interpreted when reasoning about the SUL. 

Determinism. A state machine is deterministic if each state-input pair has at most one successor state. The state machines learned by MELA are deterministic by construction. Specifically, Step 3 resolves inconsistencies introduced by abstraction, ensuring that the abstract trace set used for learning is consistent. RPNI then learns a deterministic Moore machine (Oncina and Garcia 1992). 

Soundness. In our setting, soundness means that a satisfaction or violation verdict reported by MELA is (1) guaranteed with respect to the observed SUL executions used for learning, and (2) is empirically supported with respect to broader SUL behaviour through conformance checking: First, Step 6 provides an analytical guarantee with respect to the learning traces: MELA reports a requirement as satisfied or violated only when the verdict holds on the learned state machine and, when state merging may affect the outcome, the verdict is checked against the PTA constructed directly from SUL executions. Thus, the reported verdict is guaranteed to hold for the behaviours represented in the learning traces. Second, Step 5 provides empirical support that these traces are representative of the SUL behaviour by evaluating the learned state machine against test traces and admitting only models whose conformance exceeds a user-defined threshold. 

Vacuity. As discussed in Step 6 of MELA (Section 3.8), some requirements may be reported as vacuously satisfied. This occurs when a requirement is satisfied because the stimulus that triggers the requirement never occurs. For example, the CTL requirement on line 39 in Figure 8 may hold vacuously if the antecedent input 

18 = attack high flow & label = Safe is unreachable; that is, the IDS is never in the 

Safe state while high-flow attack traffic occurs. A vacuity result has two possible interpretations: insufficient environmental cover-age or an implicit behavioural property of the SUL. One interpretation is that vacuity indicates a limitation of the environment simulator used for data generation. In this case, the simulator may be too restrictive and may fail to generate scenarios in which the IDS is in the Safe state while high-flow attack traffic is present. The vacuity result therefore identifies a gap in the generated data and suggests that the simulator should be revised to exercise the missing scenario. If the simulator is sufficiently expressive, the vacuity result instead reflects an observed characteristic of the SUL behaviour: the IDS never remains in the Safe state under such traffic, because it has already transitioned to another state before the antecedent can hold. 

# 4 Evaluation 

We evaluate MELA by answering the following research questions (RQs): 

RQ1 (Complexity and Conformance). How effective is the trace abstraction step of MELA in reducing the complexity of the learned state machines while main-taining a high level of accuracy? In RQ1, we evaluate the impact of MELA’s trace abstraction step (Step 3) on the complexity and conformance of the learned state machines. To this end, we compare MELA with an ablated version in which Step 3 is replaced by an expertise-based numeric abstraction. We then compare MELA and its ablation in terms of the complexity of the learned models and their conformance to the SUL. 

RQ2 (Verification). Is MELA effective in verifying requirements for CPS with numeric inputs? In RQ2, we use MELA to verify requirements for our case study sys-tems, which are CPS with numeric inputs, and, for each system, present the verification results together with a domain-based validation of the observed outcomes. 

RQ3 (Exploring Unknown Behaviours). Is MELA effective in identifying system behaviours that are not explicitly covered by the requirements? In RQ3, we use the state machines learned by MELA to explore system behaviours not explicitly specified in the requirements. Specifically, we check temporal-logic properties that capture domain-relevant scenarios, such as how the system behaves under different input conditions or in states not covered by the requirements. We report the observed behaviours together with domain-expert feedback, showing how these findings can help engineers better understand the system. 

## 4.1 Case Studies 

We use two case-study systems discussed below. 

IDS. Our first case study is the IDS introduced in Section 2 and detailed in Figure 4. To simulate the IDS environment, we use the testbed developed in our previous work in collaboration with RabbitRun Tech. Inc. (Ayoughi et al. 2024). Figure 10 provides an overview of this testbed. The testbed consists of three vir-tual machines deployed on separate laptop computers, representing external users, an IDS-enabled router, and local users. It generates normal network traffic together 19 Simulated  Vulnerabilies     

> in Local Users
> Simulated Normal and Attack F lows
> from External Users
> Router
> (IDS-enabled)
> Internet
> Local Users Router
> (IDS-enabled)
> Internet External Users

(a) Deployment of an IDS-enabled router in the real-world 

(b) Our testbed simulating the real-world deployment of an IDS-enabled router Fig. 10 : Our testbed simulating the real-world deployment of an IDS-enabled router Autopilot    

> Controller
> Input Commands
> Pitch Wheel Turn Knob Throttle
> Target
> Altitude
> Heading
> Reference
> Heading
> Mode
> Altitude
> Mode
> Actuator Plant
> (Environment)
> is_Engaged

Fig. 11 : Interactions of the autopilot with its environment and the list of input com-mands provided to the autopilot. with DoS and DDoS attack traffic, simulating the behaviours of both normal users and attackers among the external users, and uses the Metasploit penetration-testing framework (Rapid7 2024) to emulate vulnerabilities in local users. 

Autopilot system. Our second system is an autopilot model of a De Havilland Beaver aircraft from a public-domain benchmark of Simulink specifications provided by Lockheed Martin (Lockheed Martin Corporation 2026). Figure 11 shows the inter-actions between the autopilot controller and its environment, including the plant, as well as the list of inputs to the autopilot. When engaged, the autopilot receives commands for throttle, pitch angle, turn rate, heading, and target altitude. It uses these commands to compute control signals, which the actuator converts into physi-cal actions on the aircraft (plant). The resulting aircraft response is fed back to the autopilot, allowing it to continuously adjust its actions toward the target altitude. The Simulink autopilot model includes a simulation environment that, in addition to the plant, models environmental factors such as wind and turbulence. The autopilot system is expected to satisfy the following requirement: 

φ3 =“When autopilot is engaged, the aircraft should reach a specified altitude within 

500 seconds.” 

Simulations of the autopilot yield three possible outcomes with respect to require-ment φ3. The requirement is unsatisfied when the aircraft fails to reach the target altitude within the specified time bound. The requirement is satisfied when the air-craft reaches the target altitude within the required time bound but maintains it only marginally, with small oscillations around the target altitude under turbulence or other disturbances. The requirement is robustly satisfied when the aircraft reaches the target altitude within the required time bound and maintains it consistently, with no significant oscillations around the target altitude despite turbulence or other disturbances. 20 Autopilot System     

> System State
> type: System State Type [0, T]
> << enumeration >>
> System State Type
> Unsatisfied
> Satisfied
> Robustly Satisfied
> Input Signal
> value: Numeric [0, T]
> interpolation: Interpolation Type
> << enumeration >>
> Interpolation Type
> Piecewise Constant
> *1
> receives 1
> 1
> outputs
> Pitch Wheel Throttle Turn Knob

... Fig. 12 : A conceptual model for the autopilot system. Figure 12 shows a conceptual model capturing the inputs and outputs of the autopi-lot. The inputs are shaded in green, the SUL in blue, and the system-state output in orange. We generate the autopilot inputs using control-point encoding (Tuncali et al. 2019; Arrieta et al. 2019), where each input is defined by values at equally spaced control points. The full time series is then obtained by interpolation. As is common for CPS benchmarks, we use piecewise-constant interpolation (Khandait et al. 2024; Lockheed Martin Corporation 2026; The MathWorks, Inc. 2026b; Sam Elshamy 2026; The MathWorks, Inc. 2026a,c). 

## 4.2 RQ1: Complexity and Conformance 

Below, we first discuss the experiment design and then present the results obtained for RQ1. 

4.2.1 Ablation 

To answer RQ1, we compare MELA with an ablation of MELA, denoted by ABLA-TION, that differs from MELA only in Step 3, trace abstraction. In ABLATION, input selection and range abstraction are guided by domain knowledge rather than learned from the traces. Specifically, the inputs are selected based on domain expertise for the IDS and from system documentation for the autopilot. The numeric range of each selected input is then partitioned into equal-width intervals, while keeping the number of intervals consistent between MELA and ABLATION for each corresponding system and experiment configuration to ensure a fair comparison. For inconsistency resolu-tion, since ABLATION does not learn range abstractions from traces using decision trees, it resolves inconsistencies only by removing inconsistent abstract traces. 

4.2.2 Experiment Design 

We configure MELA and ABLATION using the parameters in Table 1. These parameters specify the data generation, trace abstraction, and automata learning settings. 21 Table 1 : Parameters for our experiments: (a) parameters of the learning sets used by MELA and ABLATION; (b) parameters of the trace abstraction step of MELA; (c) information about trace abstraction in ABLATION; and (d) automata learning algorithm used by MELA and ABLATION. 

a. Data Generation for MELA and ABLATION Case-study System Learning Set Avg Trace Length Execution Time (m) IDS DoS3 : 3-state coverage 1530 870 

DoS5 : 5-state coverage 1402 474 

DDoS : 3-state coverage 1523 1420 

Autopilot Ascent : 3-state coverage 500 125 

Descend : 3-state coverage 500 125 

b. Trace Abstraction for MELA Case-study System Selected Inputs Range Abstraction by Decision Tree (all case-study systems) IDS 

num flows Max Depth : 5 

Purity Th : 0.7 

Sup Th : 0 .2 × n, where n is the total number of data points 

num unreplied flow unreplied 

Autopilot PitchWheel Throttle PWheel Throttle 

c. Trace Abstraction for ABLATION Case-study System Selected Inputs Range Abstraction based on Domain Knowledge (all case-study systems) IDS 

num flows Let d be the max range of the numeric variable, and let k be the number of intervals produced by MELA. The intervals are [0 , ∆), [∆ , 2 × ∆), . . . ,[( k − 1) × ∆, d ], where ∆ = dk .

num unreplied flow unreplied 

Autopilot 

PitchWheel Throttle PWheel Throttle 

d. Automata Learning for MELA and ABLATION Case-study System Passive Learning Algorithm IDS AALpy’s generalized state-merging algorithm (GSM) Muskardin et al. (2021); von Berg and Aichernig (2025b) Autopilot 

4.2.2.1 Data Generation Setting 

We generate time-series data for each case-study system using Step 1 in Figure 3, and create traces from this data using Step 2. We refer to the resulting trace sets used for automata learning as learning sets . In our experiments, both MELA and ABLATION use the same learning sets. For the IDS, we generate time-series vectors over the time domain [0 , 256 min ] and use a sampling period of δ = 10 sec when converting the time-series vectors into traces. The sampling period matches the minimum refresh rate supported by our IDS testbed. The long time domain was recommended by the domain expert to increase state coverage in the generated traces. We construct separate 22 learning sets for DoS and DDoS attacks so that the learned state machines capture the IDS behaviour under each attack type separately. To account for randomness in data generation, we repeat Steps 1 and 2 five times for DoS and five times for DDoS, obtaining five learning sets for each attack type. Among the DoS learning sets, three covered only the states Safe , Warning , and Alert , while the other two covered all five IDS states. For DDoS, all five learning sets covered only Safe , Warning , and Alert .As we will discuss in Section 4.4, the domain expert confirmed that Tending Warning 

and Tending Alert are unreachable under DDoS attacks. For the autopilot system, we generate time-series vectors over the time domain [0 , 500 sec ] and sample them with δ = 1 sec during trace creation. The 500-second dura-tion matches requirement φ3 in Section 4.1, which specifies a 500-second time bound, and the sampling period matches the time resolution used for data generation in the Simulink model. We construct separate learning sets for the two cases of requirement 

φ3: ascent to a target altitude and descent to a target altitude. To account for random-ness in data generation, we repeat Steps 1 and 2 fifteen times for each case, obtaining fifteen learning sets for ascent and fifteen learning sets for descent. In summary, we use the following learning sets for our experiments. For the IDS, we construct three learning sets by taking the union of learning sets with the same attack type and state coverage: DoS3 is the union of the three learning sets for DoS with three-state coverage, DoS5 is the union of the two learning sets for DoS with five-state coverage, and DDoS is the union of the five learning sets for DDoS. For the autopilot, Ascent combines the fifteen learning sets generated for ascent to a target altitude, and Descent combines the fifteen learning sets generated for descent to a target altitude. Table 1(a) shows the average length of traces for each learning set as well as the total execution time (in minutes) required to generate the traces in these learning sets. 

4.2.2.2 Trace Abstraction Setting 

We configure the trace-abstraction step, i.e., Step 3 in Figure 3, separately for MELA and ABLATION. In both approaches, trace abstraction consists of input selection, range abstraction, and inconsistency resolution. 

Settings for MELA. Table 1(b) summarizes the trace abstraction setting for MELA. For input selection , the requirement-referenced input for the IDS is External User type , which distinguishes normal traffic from attack traffic. In addition to this input, MELA considers num flows and num unreplied , the two inputs whose information-gain values exceed the standard deviation computed over the information-gain values of all inputs, as discussed in Section 3.5.1. For each IDS learning set, we define three configurations, each including External User type and one of the fol-lowing choices of additional inputs: num flows , num unreplied , or both. We refer to these configurations as num flows , num unreplied , and flow unreplied , respectively. For the autopilot, the requirement-referenced input is Target Altitude . Among the remaining inputs, MELA identifies PitchWheel and Throttle as the inputs with the highest information-gain values. We define three autopilot configurations, each including Target Altitude : PitchWheel , Throttle , and PWheel Throttle . These configurations use PitchWheel , Throttle , and both inputs, respectively. 23 For range abstraction , we configure the decision trees used to abstract the numeric inputs of each case-study system according to the parameters in Table 1(b). We set the maximum tree depth to five, denoted by Max Depth , to limit overfitting and avoid generating many leaves that would split numeric domains into overly fine-grained intervals. We set the support threshold to 20% of the data used to build the tree, denoted by Sup Th , and the purity threshold to 70%, denoted by Purity Th . These thresholds ensure that the leaves selected for interval construction are both sufficiently populated and strongly associated with a specific system state. For inconsistency resolution , MELA first refines the abstraction and removes traces only when refinement cannot eliminate the remaining inconsistencies. In our case stud-ies, range-abstraction refinement resolved most inconsistencies, and only 0 .1% of traces had to be removed. 

Settings for ABLATION. Table 1(c) summarizes the trace-abstraction settings for ABLATION. For input selection , ABLATION selects inputs based on domain knowledge. For the IDS, the domain expert identified num flows and num unreplied 

as the inputs that most strongly affect the IDS state. For the autopilot, we used the sys-tem documentation (Federal Aviation Administration 2009b,a) and empirical results from the literature (Jodat et al. 2024, 2026) to identify PitchWheel and Throttle as the inputs that affect altitude and, consequently, the system state. These inputs match those selected by MELA. Therefore, for ABLATION, we use the same input-selection configurations defined for MELA. For range abstraction , each selected numeric input is partitioned into the same number of equal-width intervals as in MELA for the corresponding system and learning-set configuration. This keeps the abstraction granularity identical between MELA and ABLATION. Furthermore, using the same number of equal-width inter-vals in ABLATION controls for abstraction granularity, ensuring that any differences from MELA are due to how interval boundaries are derived rather than to the number of abstract input symbols. For inconsistency resolution , as mentioned in Section 4.2.1, ABLATION resolves inconsistencies only by removing inconsistent traces. In our experiments, only 0 .1% of ABLATION traces had to be removed. 

4.2.2.3 Automata Learning 

As shown in Table 1(d), both MELA and ABLATION use AALpy’s implemen-tation of generalized state merging (GSM), an RPNI-based passive learning algo-rithm (Muskardin et al. 2021; von Berg and Aichernig 2025b) to produce state machines. 

4.2.3 Metrics 

To assess the complexity of the generated state machines, we report the number of states, the number of transitions, and the size of the input alphabet. These three size-based metrics are commonly used in the literature to evaluate the complexity of state machines (Hall 2011). We measure the accuracy (or conformance) of the generated state machines using the conformance-checking process in Step 5 of MELA, which evaluates whether each learned automaton accepts unseen test traces from the SUL. For the IDS, we generate 24 Table 2 : Comparing the number of states, number of transitions, and the alphabet size for the state machines learned by MELA versus by ABLATION.                                                                                              

> Case study Learning set Configuration MELA ABLATION # States # Transitions
> |Alphabet |
> # States # Transitions
> |Alphabet |
> IDS
> DoS3 num flows 395290 325 5
> num unreplied 19 51 529 68 5
> flow unreplied 311 723 65 8
> DDoS num flows 395284 319 5
> num unreplied 17 46 524 62 5
> flow unreplied 311 720 52 8
> DoS5 num flows 47 78 5410 499 5
> num unreplied 121 187 5178 267 5
> flow unreplied 46 83 8167 253 9
> Autopilot
> Descend PitchWheel 40 65 359 86 2
> Throttle 126 169 2178 178 1
> PWheel Throttle 40 75 659 86 2
> Ascent PitchWheel 53 106 376 135 3
> Throttle 133 163 2180 180 1
> PWheel Throttle 53 125 476 135 3

five test sets to cover a range of trace lengths: very small , small , medium , large , and 

very large . Each set contains 100 traces, resulting in 500 test traces in total. The average trace lengths are 70, 138, 207, 276, and 345, corresponding to approximately 10%, 20%, 30%, 40%, and 50% of the learning-trace length, respectively. Because generating long IDS traces is costly, we cap the test-trace length at 50% of the learning-trace length. For the autopilot, each test trace has length 500, similar to the traces in the learning set, since requirement φ3 in Section 4.1 requires simulating the system for 500 seconds to observe the altitude change. We therefore generate two test sets, one for ascent to the target altitude and one for descent to the target altitude, with six traces in each set. 

4.2.4 Results 

To answer RQ1, we apply MELA and ABLATION to the IDS learning sets ( DoS3 ,

DoS5 , and DDoS ) and the autopilot learning sets ( Ascent and Descent ). Across these learning sets, we generate 30 state machines by applying the two approaches under three abstraction configurations for each case study: num flows , num unreplied , and 

flow unreplied for the IDS, and PitchWheel , Throttle , and PWheel Throttle for the autopilot. 25 To assess complexity, Table 2 reports the number of states, transitions, and input-alphabet sizes for the state machines generated by MELA and ABLATION for each case study. Overall, the state machines generated by MELA are consistently more compact than those generated by ABLATION, with fewer states and transitions across both the IDS and autopilot case studies. Across all configurations, the ML-based trace abstraction used in MELA reduces the number of states by an average of 53 .79% and the number of transitions by an average of 44 .62% compared to the expertise-based abstraction used in ABLATION. Regarding input-alphabet size, the state machines generated by MELA for the IDS use alphabets that are either the same size as or smaller than those of the corre-sponding ABLATION state machines. For the autopilot, one of the six state machines generated by MELA has the same alphabet size as its ABLATION counterpart, while the remaining five use larger alphabets due to the finer-grained abstractions derived by MELA. Figure 13 shows the accuracy results for the learned automata by MELA and ABLATION. The accuracy values are computed by applying each of the 30 learned automata to the test traces described in Section 4.2.3. The plots in the figure show the accuracy distribution of each learned automaton with respect to these test traces. Figure 13(a) shows the accuracy results for the IDS. Of the nine state machines gen-erated by MELA, eight have higher average accuracy than the corresponding state machines generated by ABLATION, and one has the same average accuracy. Seven of the nine IDS state machines learned by MELA reach 100% accuracy. Figure 13(b) shows the accuracy results for the autopilot. All six state machines learned by MELA are more accurate than their corresponding ABLATION state machines. The three state machines learned by MELA from the Ascent learning set reach 100% accuracy, while the three learned from the Descent learning set reach 86 .96% accuracy. Over-all, across both case studies, the state machines learned by MELA are, on average, 41 .71% more accurate than those learned by ABLATION. To assess whether these differences are statistically significant, we use the Mann-Whitney U test with a significance level of 0 .05 (Conover 1999) and Vargha-Delaney’s effect size ˆA12 (Vargha and Delaney 2000). We compute ˆA12 from the accuracy values, where ˆA12 > 0.5 indicates that MELA achieves higher accuracy than ABLA-TION, while ˆA12 < 0.5 indicates the opposite. We classify effect size values as small, medium, and large when their values are greater than or equal to 0 .56, 0 .64, and 0 .71, respectively (Vargha and Delaney 2000); values between 0 .44 and 0 .56 are considered negligible. Statistical tests comparing the accuracy results in Figures 13(a) and (b) are provided in Table 3. Each test compares the accuracy of an automaton gener-ated by MELA with the corresponding automaton generated by ABLATION for the same learning set and variable-selection configuration. As shown in the table, MELA achieves significantly higher accuracy than ABLATION in nine of the 15 compar-isons. The effect sizes are large for the autopilot system and medium or large for the IDS. For the remaining six comparisons, the accuracy differences between MELA and ABLATION are not statistically significant. 26 MELA ABLATION 

020 40 60 80 100 

> DoS3 Accuracy(%)

100.00 45.40 

num_flows 

MELA ABLATION 

020 40 60 80 100 100.00 62.20 

num_unreplied 

MELA ABLATION 

020 40 60 80 100 

50.60 45.40 

flow_unreplied 

MELA ABLATION 

020 40 60 80 100 

> DDoS Accuracy(%)

100.00 78.80 MELA ABLATION 

020 40 60 80 100 100.00 100.00 MELA ABLATION 

020 40 60 80 100 100.00 58.80 MELA ABLATION 

020 40 60 80 100 

> DoS5 Accuracy(%)

100.00 87.20 MELA ABLATION 

020 40 60 80 100 

57.80 56.20 MELA ABLATION 

020 40 60 80 100 100.00 15.60 (a) IDS case study. MELA ABLATION 

020 40 60 80 100 

> Descend Accuracy(%)

86.96 43.48 

PitchWheel 

MELA ABLATION 

020 40 60 80 100 86.96 47.83 

Throttle 

MELA ABLATION 

020 40 60 80 100 86.96 43.48 

PWheel_Throttle 

MELA ABLATION 

020 40 60 80 100 

> Ascent Accuracy(%)

100.00 22.22 MELA ABLATION 

020 40 60 80 100 100.00 18.52 MELA ABLATION 

020 40 60 80 100 100.00 18.52 

(b) Autopilot case study. 

Fig. 13 : Comparing the accuracy of the state machines learned by MELA versus by ABLATION. (a) shows the results for the IDS learning sets ( DoS3 , DoS5 , and DDoS ). (b) shows the results for the Autopilot learning sets ( Ascend and Descend ). Both case studies are evaluated under different configurations for selected inputs defined in Table 1. 27 Table 3 : Statistical tests comparing the accuracy results of MELA against those of ABLATION. The p-values highlighted in blue rep-resent cases where MELA significantly outperforms ABLATION. The significance level is 0 .05. N, S, M, and L indicate a negligible, small, medium and large effect sizes, respectively.                                   

> Case study Learning set Configuration p-value ˆA12
> IDS
> DoS3 num flows 0.000 0.95 (L)
> num unreplied 0.001 0.90 (L)
> flow unreplied 0.762 0.55 (N)
> DoS5 num flows 0.035 0.70 (M)
> num unreplied 0.676 0.44 (N)
> flow unreplied 0.001 1.00 (L)
> DDoS num flows 0.015 0.75 (L)
> num unreplied 1.000 0.50 (N)
> flow unreplied 0.002 0.85 (L)
> Autopilot
> Descend PitchWheel 0.050 0.82 (L)
> Throttle 0.181 0.72 (L)
> PWheel Throttle 0.050 0.82 (L)
> Ascent PitchWheel 0.025 0.83 (L)
> Throttle 0.007 0.92 (L)
> PWheel Throttle 0.007 0.92 (L)

Finding. Our approach (MELA) leads to an average reduction of 49 .20% in the number of states and transitions of the learned automata, while improving accuracy by an average of 41 .71% compared to using expertise-based abstrac-tions for automata learning. Statistical-test results show that this accuracy improvement is significant in nine of the 15 comparisons, with medium or large effect sizes. 

## 4.3 RQ2: Verification 

We discuss the experiment design and the results obtained for RQ2. 

4.3.1 Experiment Design 

For this research question, we reuse the state machines generated in RQ1. For the IDS, we analyze the models learned from the DDoS learning set and from DoS5 . We choose DoS5 instead of DoS3 because DoS5 exercises all five IDS states under DoS attacks, while DoS3 does not. For DoS5 , we keep the models built with num flows 

and flow unreplied , as both show high conformance accuracy, and discard the num -unreplied model because its accuracy is relatively lower (Figure 13). To simplify the presentation of RQ2 and RQ3, we refer to the three IDS abstraction configurations based on num flows , flow unreplied , and num unreplied as config1 , config2 , and 

config3 , respectively. For the autopilot, we retain all RQ1 state machines because all configurations achieve high accuracy, above 85% (Figure 13). 28 We decompose each IDS requirement into three properties because φ1 and φ2

specify staged behaviour. For φ1, we separately check the progression from Safe to 

Warning , from Warning to Alert , and persistence in Alert under continued attacks; 

φ2 is decomposed analogously for staged recovery after attacks stop. Similarly, we decompose the autopilot requirement, i.e., φ3 from Section 4.1, into two properties to separately capture the two transitions in satisfaction status: from unsatisfied to satisfied, and from satisfied to robustly satisfied. We encode the detailed requirements in CTL. For readability, the leftmost columns of Tables 4(a) and (b) and Table 5 provide their natural-language requirements for the IDS and autopilot case studies, respectively. For example, the first row of Table 4(a) corresponds to the CTL property on line 39 of Figure 8. The full set of CTL properties, including the autopilot descent case, is available in our supplementary material (Ayoughi et al. 2026d,e). In Table 4, we abbreviate the IDS states Safe , Warning , Alert , Tending Warning , and Tending Alert as “S”, “W”, “A”, “TW”, and “TA”, respectively. In Tables 4 and 5, the symbols ✓, ×, and v denote that MELA reports a property as satisfied, violated, or vacuously satisfied, respectively. 

4.3.2 Results 

IDS. Tables 4(a) and (b) show the verification results for the IDS using the DoS5 

and DDoS learning sets, respectively. Based on Tables 4(a) and (b), the IDS reacts to DoS/DDoS attacks only when the number of flows is medium or high, while low-flow attacks do not trigger a reaction. Medium-flow attacks prompt the IDS to transition from S to W or TW, but do not move it further to the critical states, TA or A. Only high-flow attacks move the IDS from W to A or TA, and once the IDS reaches A, it stays in A or TA. Further, the results show that the IDS returns to state S only with low-flow normal traffic. In addition, the combination of normal traffic with a high number of flows does not appear in state machines learned by MELA. The IDS remains in the W state for medium-flow normal traffic, indicating that it considers this traffic suspicious. In addition, the verification results in Tables 4(a) and (b) are largely consistent across the three configurations, config1 , config2 , and config3 . We define a combi-nation as a specific property, learning set, and input range. A combination is consistent when all three configurations produce the same verification outcome. Across the 36 combinations in Tables 4(a) and (b), 34 are consistent. The two remaining combina-tions, marked by superscripts 1 and 2 in Table 4(b), are non-agreement combinations. In both, config1 and config3 produce ✓, whereas config2 produces ×. Thus, even in the two non-agreement combinations, the verification outcome is supported by two of the three learned state machines. 

Domain-based validation for IDS. The results in Tables 4(a) and (b) are consistent with the domain expert’s assessment of the IDS behaviour. The expert con-firmed that low-flow DoS and DDoS attacks do not degrade the quality of service for clients; therefore, the IDS is not expected to escalate in response to them. The absence of high-flow normal traffic from the learned state machines matches the expert’s intu-ition that high numbers of flows are generated by attackers rather than normal users. 29 Table 4 : Verification results for properties derived from the IDS requirements φ1 and 

φ2. The properties related to φ1 are evaluated under different attack-traffic intensities, while the properties related to φ2 are evaluated under different normal-traffic intensi-ties. The table shows whether each property is satisfied ( ✓), violated ( ×), or vacuously satisfied (v). 

(a) Results for state machines learned from the DoS5 learning set, capturing IDS behaviour under DoS attacks. Property config1 config3 

Low Med High Low Med High Properties related to φ1 Attack traffic intensity (Low / Med / High) 

When attacks happen and the system is in state S,the system shall transition to W or TW .

× ✓ v × ✓ vWhen attacks happen and the system is in state W,the system shall transition to A or TA .v × ✓ v × ✓

When attacks happen and the system is in state A,the system shall remain in A or transition to TA .v × ✓ v × ✓

Properties related to φ2 Normal traffic intensity (Low / Med / High) 

When attacks are stopped and the system is in state 

S, the system shall remain in S.

✓ v v ✓ v vWhen attacks are stopped and the system is in state 

W, the system shall transition to S.

✓ × v ✓ × vWhen attacks are stopped and the system is in state 

A, the system shall transition to W or TW .v v v v v v

(b) Results for state machines learned from the DDoS learning set, capturing IDS behaviour under DDoS attacks. Property config1 config2 config3 

Low Med High Low Med High Low Med High Properties related to φ1 Attack traffic intensity (Low / Med / High) 

When attacks happen and the system is in state 

S, the system shall transition to W or TW .

× ✓ v × ✓ v × ✓ vWhen attacks happen and the system is in state 

W, the system shall transition to A or TA .v × ✓1 v × ×1 v × ✓1

When attacks happen and the system is in state 

A, the system shall remain in A or transition to TA .v × ✓ v × ✓ v × ✓

Properties related to φ2 Normal traffic intensity (Low / Med / High) 

When attacks are stopped and the system is in state S, the system shall remain in S.

✓ v v ✓ v v ✓ v vWhen attacks are stopped and the system is in state W, the system shall transition to S.

✓2 × v ×2 × v ✓2 × vWhen attacks are stopped and the system is in state A, the system shall transition to W or TW .v v v v v v v v v 

> 1,2

Non-agreement combinations: for the same property, learning set, and input range, the three con-figurations do not produce the same verification outcome. In both marked combinations, config1 and 

config3 produce ✓, while config2 produces ×.

Finally, the expert confirmed that DoS and DDoS attacks typically begin with a low number of flows and gradually escalate, which explains why high-flow attacks are not observed while the IDS is in state S.

Autopilot. Table 5 summarizes the verification results for the autopilot in the 

Ascent scenario. The results indicate that requirement φ3 is not satisfied when 

PitchWheel remains low over time. A low or high Throttle can lead to the satisfac-tion of φ3, but robust satisfaction occurs only when Throttle is high. When the state machine includes both PitchWheel and Throttle , PitchWheel plays the dominant 30 role: φ3 is satisfied or robustly satisfied when PitchWheel is medium and low Throttle 

is applied. When both inputs are high, robust satisfaction is ensured. Applying a high 

PitchWheel with any Throttle value ensures satisfaction. For the Descent learning set, whose verification results are provided in our online repository (Ayoughi et al. 2026f), the results show, as expected, the reverse pat-tern for PitchWheel : since a low PitchWheel command drives the aircraft toward a lower altitude, requirement φ3 is satisfied or robustly satisfied only when PitchWheel 

remains low over time. This pattern also holds when the state machine includes both 

PitchWheel and Throttle : satisfaction of φ3 in the descent scenario depends on 

PitchWheel remaining low, while the Throttle level does not affect the outcome. 

Domain-based validation for autopilot. The verification results for both 

Ascent and Descent learning sets are consistent with the system documentation: To satisfy requirement φ3, the aircraft must maintain the proper nose attitude and receive sufficient throttle input (Federal Aviation Administration 2009b,a). For ascent, satisfying φ3 requires the aircraft to maintain an upward nose orientation while receiv-ing sufficient engine power to sustain the climb. Through trace abstraction, MELA identifies upward-nose behaviour with medium or high PitchWheel values, whereas 

Throttle mainly determines whether satisfaction is robust. The vacuous verification results for the ascent scenario indicate that high Throttle can override the effect of low or medium PitchWheel values. Specifically, high throttle can drive the aircraft nose into a strongly upward orientation, i.e., high PitchWheel , even when the ini-tially supplied PitchWheel value is only slightly or moderately upward. For descent, the documentation indicates that the aircraft nose must remain oriented downward to reduce altitude, which corresponds to low PitchWheel in the learned abstraction. 

Finding. Overall, our results show that the state machines learned by MELA are effective for verifying the requirements of the IDS and autopilot systems. In both case studies, the verification outcomes are consistent with domain knowledge or system documentation. The vacuous outcomes further reveal unreachable combinations of states and input ranges, such as high-flow attacks from the safe state in the IDS. 31 Table 5 : Verification results for the autopilot properties derived from requirement φ3

(Section 4.1). The table reports whether each property is satisfied ( ✓), violated ( ×), or vacuously satisfied (v) for state machines learned from the Ascent learning set. Colours indicate the input abstractions and requirement states: Low , Med , and 

High denote low, medium, and high input ranges, respectively, while red, blue, and green denote states in which requirement φ3 is unsatisfied, satisfied, and robustly satisfied, respectively. 

Property Result With the PitchWheel input 

From any state in which requirement φ3 is unsatisfied , continuously 

Low PitchWheel input shall eventually cause the system to satisfy 

requirement φ3.

×

From any state in which requirement φ3 is unsatisfied , continuously 

Med PitchWheel input shall eventually cause the system to satisfy 

requirement φ3.

✓

From any state in which requirement φ3 is unsatisfied , continuously 

High PitchWheel input shall eventually cause the system to satisfy 

requirement φ3.

✓

From any state in which requirement φ3 is satisfied , continuously Low 

PitchWheel input shall eventually cause the system to robustly satisfy 

requirement φ3.

×

From any state in which requirement φ3 is satisfied , continuously Med 

PitchWheel input shall eventually cause the system to robustly satisfy 

requirement φ3.

✓

From any state in which requirement φ3 is satisfied , continuously High 

PitchWheel input shall eventually cause the system to robustly satisfy 

requirement φ3.

✓

With the Throttle input 

From any state in which requirement φ3 is unsatisfied , continuously Low 

Throttle input shall eventually cause the system to satisfy requirement φ3.

✓

From any state in which requirement φ3 is unsatisfied , continuously 

High Throttle input shall eventually cause the system to satisfy 

requirement φ3.

✓

From any state in which requirement φ3 is satisfied , continuously Low 

Throttle input shall eventually cause the system to robustly satisfy 

requirement φ3.

×

From any state in which requirement φ3 is satisfied , continuously High 

Throttle input shall eventually cause the system to robustly satisfy 

requirement φ3.

✓

With both PitchWheel and Throttle inputs 

From any state in which requirement φ3 is unsatisfied , continuously 

Low PitchWheel and Low Throttle inputs shall eventually cause the system to satisfy requirement φ3.

×

From any state in which requirement φ3 is unsatisfied , continuously 

Low PitchWheel and High Throttle inputs shall eventually cause the system to satisfy requirement φ3.v

32 Property Result 

From any state in which requirement φ3 is unsatisfied , continuously 

Med PitchWheel and Low Throttle inputs shall eventually cause the system to satisfy requirement φ3.

✓

From any state in which requirement φ3 is unsatisfied , continuously 

Med PitchWheel and High Throttle inputs shall eventually cause the system to satisfy requirement φ3.vFrom any state in which requirement φ3 is unsatisfied , continuously 

High PitchWheel and Low Throttle inputs shall eventually cause the system to satisfy requirement φ3.

✓

From any state in which requirement φ3 is unsatisfied , continuously 

High PitchWheel and High Throttle inputs shall eventually cause the system to satisfy requirement φ3.

✓

From any state in which requirement φ3 is satisfied , continuously Low 

PitchWheel and Low Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.

×

From any state in which requirement φ3 is satisfied , continuously Low 

PitchWheel and High Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.vFrom any state in which requirement φ3 is satisfied , continuously Med 

PitchWheel and Low Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.

✓

From any state in which requirement φ3 is satisfied , continuously Med 

PitchWheel and High Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.vFrom any state in which requirement φ3 is satisfied , continuously High 

PitchWheel and Low Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.

×

From any state in which requirement φ3 is satisfied , continuously High 

PitchWheel and High Throttle inputs shall eventually cause the system to robustly satisfy requirement φ3.

✓

## 4.4 RQ3: Exploring Unknown Behaviours 

We discuss the experiment design and the results obtained for RQ3. 

4.4.1 Experiment Design 

Requirements φ1 and φ2 specify the expected IDS state changes among the Safe ,

Warning , and Alert states, which are verified in RQ2. Because these requirements do not define the expected behaviour of the Tending Warning (TW) and Tending Alert (TA) states, RQ3 focuses on exploring the behaviour of the IDS in the TW and TA states. To this end, we formulate CTL properties that examine how the IDS evolves from TW and TA under low-, medium-, and high-flow traffic: attack traffic for φ1 and normal traffic for φ2. Following the verification procedure described in 33 Step 6 of MELA, we use NuSMV to verify these properties on the five IDS state machines analyzed in RQ2. For the autopilot case study, we examine whether the RQ2 verification results can be used to augment requirement φ3 and identify preconditions for this requirement. 

4.4.2 Results 

IDS. Tables 6(a) and (b) present the verification results for the IDS properties from the TW and TA states for DoS5 and DDoS learning sets, respectively. A property is marked as pass ( ✓) if it holds, fail ( ×) if it is violated, and vacuous (v) if it holds vacuously. Based on Tables 6(a) and (b), we observe the following behaviours for the TW and TA states when the IDS is under DoS or DDoS attacks: 

• The IDS never experiences low-flow DoS attacks while in TW or TA, since all low-flow attack properties for these states are vacuously satisfied. 

• Medium-flow DoS attacks do not drive the IDS from TW or TA to the highest-criticality state A. 

• High-flow DoS attacks drive the IDS toward higher criticality states. In particular, from TW, the IDS moves to TA or A; from TA, the IDS either remains in TA or moves to A. 

• The IDS does not reach the TW or TA states under DDoS attacks. These observations suggest a refinement of requirement φ1 to capture the observed behaviour of the IDS in the TW and TA states: When DoS attacks happen, the sys-tem shall change state in a staged manner from Safe to Warning, from Warning to Alert, and from Tending Warning and Tending Alert toward higher-criticality states. Medium-flow attacks shall not move the IDS from Tending Warning or Tending Alert to Alert, while high-flow attacks shall move the IDS from Tending Warning or Tending Alert to Tending Alert or Alert. 

Further, based on Table 6(a), all properties related to φ2 for TW and TA are vacuously satisfied. This suggests that TW and TA are attack-driven intermediate states that the IDS leaves as the flow level decreases. As verified in RQ2, normal traffic is then processed in lower-criticality states, where the IDS remains low-criticality or continues recovering toward lower criticality. Finally, all verification results in Tables 6(a) and (b) are consistent across the state machines learned under config1 , config2 , and config3 .

Domain-expert feedback for IDS. The explored behaviours matched the domain expert’s intuition; nevertheless, the expert found them useful because the learned state machines provide systematic evidence for behaviours that may not be exposed through ad hoc observations or expert experience alone. Further, the expert confirmed that DDoS attacks involve larger numbers of flows than DoS attacks, causing the IDS to bypass the TW and TA states. 

Autopilot. For the autopilot case study, the verification results in RQ2 reveal the preconditions under which requirement φ3 is expected to hold. In particular, reaching the specified altitude within 500 seconds depends on the aircraft’s nose orientation and throttle. For ascent, the nose should point upward and the throttle should be 34 Table 6 : Verification results for properties derived from the IDS requirements φ1 and 

φ2, used to explore IDS behaviours in states TW and TA . The properties related to φ1

are evaluated under different attack-traffic intensities, while the properties related to 

φ2 are evaluated under different normal-traffic intensities. The table shows whether each property is satisfied ( ✓), violated ( ×), or vacuously satisfied (v). 

(a) Results for state machines learned from the DoS5 learning set, capturing IDS behaviour under DoS attacks. Property config1 config3 

Low Med High Low Med High Properties related to φ1 Attack traffic intensity (Low / Med / High) 

When attacks happen and the system is in state TW ,the system shall transition to W or remain in TW .v ✓ × v ✓ ×

When attacks happen and the system is in state TW ,the system shall transition to A or TA .v × ✓ v × ✓

When attacks happen and the system is in state TA ,the system shall transition to W or TW .v ✓ × v ✓ ×

When attacks happen and the system is in state TA ,the system shall transition to A or remain in TA .v × ✓ v × ✓

Properties related to φ2 Normal traffic intensity (Low / Med / High) 

When attacks are stopped and the system is in state 

TW , the system shall transition to S.v v v v v vWhen attacks are stopped and the system is in state 

TW , the system shall transition to W or remain in TW .v v v v v vWhen attacks are stopped and the system is in state 

TA , the system shall transition to W or TW .v v v v v vWhen attacks are stopped and the system is in state 

TA , the system shall transition to A or remain in TA .v v v v v v

(b) Results for state machines learned from the DDoS learning set, capturing IDS behaviour under DDoS attacks. Property config1 config2 config3 

Low Med High Low Med High Low Med High Properties related to φ1 Attack traffic intensity (Low / Med / High) 

When attacks happen and the system is in state 

TW , the system shall transition to W or remain in TW .v v v v v v v v vWhen attacks happen and the system is in state 

TW , the system shall transition to A or TA .v v v v v v v v vWhen attacks happen and the system is in state 

TA , the system shall transition to W or TW .v v v v v v v v vWhen attacks happen and the system is in state 

TA , the system shall transition to A or remain in TA .v v v v v v v v v

Properties related to φ2 Normal traffic intensity (Low / Med / High) 

When attacks are stopped and the system is in state TW , the system shall transition to S.v v v v v v v v vWhen attacks are stopped and the system is in state TW , the system shall transition to W or remain in TW .v v v v v v v v vWhen attacks are stopped and the system is in state TA , the system shall transition to W or TW .v v v v v v v v vWhen attacks are stopped and the system is in state TA , the system shall transition to A or remain in TA .v v v v v v v v v

sufficient. For descent, the nose should point downward. Based on these observations, requirement φ3 can be refined as follows: when the autopilot is engaged, if the aircraft 

35 nose points upward and the throttle is sufficient for ascent, or if the aircraft nose points downward for descent, then the aircraft should reach the specified altitude within 500 

seconds. 

Finding. Overall, the state machines learned by MELA support requirement refinement by capturing system behaviours that are not explicitly covered by the original requirements. For the IDS, the DoS results show that the IDS never experiences low-flow attacks or normal traffic while in TW or TA. Medium-flow attacks keep the IDS in warning-related states, while high-flow attacks move the IDS toward alert-related states. Under DDoS attacks, the IDS does not reach TW or TA. For the autopilot, the results reveal the preconditions under which the aircraft is expected to reach the specified altitude. 

## 4.5 Validity Considerations 

Internal Validity. To ensure a fair comparison between MELA and ABLATION, we use identical learning sets and identical test sets across all experiments. The construc-tion of the test sets is not biased toward either technique. We implemented measures to minimize the impact of extraneous factors. Specifically, for the IDS, (1) we controlled the trace generation process to prevent external traffic not initiated by our simula-tions from reaching the local users or the router and (2) we monitored the network during the experiments to ensure the absence of anomalies that might have arisen due to events beyond our control. For the autopilot, we kept the simulation setup fixed across runs, including the autopilot Simulink model, controller parameters, and initial conditions, and varied only the input commands used for trace generation. To con-struct diverse learning sets, we used adaptive random testing (Luke 2013) to generate input traces for the IDS and autopilot that explore different regions of the input space while ensuring coverage of all system states. Further, removing inconsistent traces may affect the learned state machines by excluding rare behaviours that are not well captured by the abstraction. To mitigate this threat, MELA first attempts to resolve inconsistencies by refining the decision-tree-induced intervals and removes traces only when inconsistencies remain after refinement. In our experiments, this step removed only a negligible fraction of the traces ( ∼ 0.1%). In addition, requirement verification requires translating natural-language requirements into CTL formulas. An inaccurate formalization may not fully capture the intended requirement. To mitigate this threat, we derived the CTL formulas using domain knowledge, either in consultation with the domain expert or based on system documentation. This helps ensure that the formal properties reflect the relevant input conditions, system states, and expected successor states. All CTL formulas for the IDS and autopilot are available in our supplementary material (Ayoughi et al. 2026d). State-machine generalization during passive learning may produce verification violations that are not supported by the observed executions. We mitigate this threat by checking each violation against the PTA, which captures exactly the observed abstract traces, and by reporting failures in the RQ2 and RQ3 tables only when the violation is present in the PTA. 36 Conclusion Validity. We apply the Benjamini-Hochberg (BH) procedure (Ben-jamini and Hochberg 2018) to control the false discovery rate. In RQ1, all statistical significance tests were reported using the BH-adjusted p-values. 

External Validity. Our experiments are based on two case-study systems: an IDS and an aircraft autopilot system. We selected these systems because they are numeric CPS from two distinct domains, namely network security and avionics. Our IDS case study represents an industrial system for which we could interact with a domain expert (Ayoughi et al. 2024). The aircraft autopilot system is from the Lockheed Martin benchmark and has been previously used in the literature on testing CPS models (Giannakopoulou et al. 2021; Jodat et al. 2024; Nejati et al. 2019; Jodat et al. 2026). Together, these systems strengthen our evaluation by showing that MELA can effectively be applied to numeric CPS with different behaviours, inputs, and system characteristics. Further experiments with a broader range of CPS would strengthen generalizability. 

# 5 Related Work 

This section compares our work with the relevant strands in two areas: (a) behavioural model learning, and (b) supervised rule mining for numeric systems. 

## 5.1 Behavioural Model Learning 

To better position our work within the literature on learning behavioural models from execution data, we present Table 7 as a structured comparison with the research strands most closely related to MELA. We use the following criteria for the comparison: (1) Application domain denotes the specific types of SULs an approach targets. As shown in Table 7, some approaches are designed for systems with continuous and time-varying behaviours (Aichernig et al. 2019; Plambeck et al. 2024), some target software systems whose behaviour is captured through system interactions (Lorenzoli et al. 2008), some focus on reactive and protocol-based systems driven by discrete interactions (Aichernig et al. 2024), and some are developed for sequential decision-making environments with stochastic dynamics (Tappler et al. 2024). (2) Learning paradigm captures how an approach obtains the executions used to construct the behavioural model. Passive approaches rely on a fixed dataset of execu-tion traces collected beforehand (Plambeck et al. 2024; Aichernig et al. 2024; Lorenzoli et al. 2008; Majumdar et al. 2025). Active approaches interact with the SUL during learning, for example by issuing membership or output queries and searching for coun-terexamples to the current model (Aichernig et al. 2019). Hybrid approaches start from an initial trace dataset and collect additional executions during learning to refine the model (Tappler et al. 2024). (3) Access level refers to the level of access required by a behavioural model infer-ence approach. Access level can be categorized as white-box, gray-box, or black-box. A white-box approach (Lorenzoli et al. 2008) requires access to the SUL’s source code for instrumentation and trace collection. A gray-box approach assumes partial access to the SUL’s internal information, such as selected variables, logs, or interfaces, but not 37 Table 7 : Comparison of MELA with related work strands on synthesizing behavioural models. 

> Study  Application Domain  Learning Paradigm Access level  Learned Model Type  SUL Input Type  SUL Output Type Transition-label Abstraction State-space Abstraction
> CASTLE Tappler et al. (2024) Stochastic control environments Passive & Active Black box Markov decision process Categorical  Numeric  N/A 1 Automatic: dimensionality reduction and k-means clustering Aichernig et al. (2019)  CPS  Active  Black box  Mealy machine Numeric  Numeric & Categorical  Manual  Manual FaMoS Plambeck et al. (2024) CPS  Passive  Black box Hybrid decision tree Numeric  Numeric  Automatic: decision tree Automatic: dynamic time warping-based clustering Aichernig et al. (2024) Reactive systems and protocols Passive  Black box Mealy machine Categorical  Categorical  N/A 1 Automatic: neural networks and hidden-state clustering GK-tail Lorenzoli et al. (2008) Software interaction  Passive  Gray box  Extended finite state machine Numeric & Categorical  Numeric & Categorical  Automatic: invariant detection Automatic: trace-prefix state merging LEAP Majumdar et al. (2025) Timed reactive systems Passive  Black box  Event-recording automata Numeric & Categorical  Categorical  Manual  Automatic: SMT-based state merging
> MELA  CPS  Passive  Black box Moore Machine Numeric & Categorical Numeric & Categorical Automatic: information gain and decision tree N/A 2
> 1 N/A indicates that transition-label abstraction is not required, because discrete events or actions already define transition labels.
> 2 N/A indicates that state-space abstraction is not required, because the observed system-state outputs define the state space.

38 full access to the implementation. A black-box approach operates without access to the SUL’s internal implementation and relies only on observable inputs and outputs. (4) Learned model type denotes the state-machine formalism that an approach learns to represent the behaviour of the SUL. Different formalisms capture differ-ent behavioural aspects. For example, Moore and Mealy machines differ in whether outputs are associated with states or with input-state interactions (Aichernig et al. 2024); extended finite state machines capture behaviour governed by conditions over data values (Lorenzoli et al. 2008); and Markov decision processes and event-recording automata, can capture probabilistic, continuous, or timing-related behaviour (Tappler et al. 2024; Plambeck et al. 2024; Majumdar et al. 2025). (5) SUL input/output type refers to the types of inputs provided to the SUL and the types of outputs observed from it. For CPS, inputs consist of numeric, time-varying signals, and outputs are typically numeric measurements and may include categorical system-state labels (Aichernig et al. 2019; Plambeck et al. 2024). In reactive and protocol-based systems, inputs are often categorical, such as commands, events, or messages, and outputs are observed as categorical events such as responses or acknowledgements (Aichernig et al. 2024). (6) Transition-label abstraction refers to the process of converting raw trace data into abstract labels for transitions in the learned model. This abstraction is needed when traces contain numeric or continuous that cannot be used directly as tran-sition labels. Some approaches use discrete symbols as transition labels (Tappler et al. 2024; Aichernig et al. 2024), while others derive more expressive labels in the form of transition conditions, such as numeric ranges or logical predicates (Lorenzoli et al. 2008; Plambeck et al. 2024). Abstractions can be defined manually, based on domain knowledge (Aichernig et al. 2019; Majumdar et al. 2025), or derived auto-matically during learning, for example by dynamic invariant detection or decision-tree learning (Lorenzoli et al. 2008; Plambeck et al. 2024). (7) State-space abstraction refers to whether an approach derives a finite set of abstract states from the raw system traces, and how these states are obtained. Such abstraction is often needed when system behaviour is observed through continu-ous, high-dimensional representations that do not directly provide discrete states. In these cases, the approach constructs a state-based model by mapping observations or behaviours to abstract states. This mapping may be defined manually using domain knowledge (Aichernig et al. 2019) or derived automatically during learning, for exam-ple by clustering similar observations or behaviours (Tappler et al. 2024; Plambeck et al. 2024; Aichernig et al. 2024) or by merging compatible states in a prefix-tree structure (Lorenzoli et al. 2008; Majumdar et al. 2025). Having set the stage with Table 7, we now discuss the specific work strands listed in the table. We begin with approaches for learning behavioural models, followed by techniques for abstracting transition labels and state spaces. 

Learned behavioural formalisms. Recent research has explored how to infer behavioural models of SULs from execution traces. These approaches differ in the type of model they learn and in the aspects of behaviour they are able to represent. Aich-ernig et al. (Aichernig et al. 2019, 2024) learn Mealy machines, which associate outputs with transitions and thus capture behaviour as observable responses to inputs. This 39 formalism is particularly suitable for reactive systems and communication protocols, whose behaviour is naturally viewed as a sequence of observable responses to incoming commands or messages. CASTLE (Tappler et al. 2024) learns a discrete-state Markov decision process (MDP), which represents action-dependent state changes together with probabilistic transition outcomes. This model is appropriate for stochastic con-trol environments, where the SUL’s behaviour depends both on the selected actions and on uncertainty in environment’s response. GK-tail (Lorenzoli et al. 2008) learns an extended finite state machine (EFSM), where states and transitions are enriched with conditions over data values. This representation is suitable for software systems whose executions involve method calls, parameters, and return values, and where dif-ferent behaviours arise under different data conditions. FaMoS (Plambeck et al. 2024) learns a hybrid automaton, in which the system evolves continuously within discrete control modes and switches between modes when numeric conditions are satisfied. This formalism is particularly suitable for CPS, where discrete control logic interacts with continuously changing physical variables. LEAP (Majumdar et al. 2025) learns an event-recording automaton (ERA), which represents behaviour in terms of events and timing constraints that record when events occur. This model is suitable for timed reactive systems, where behaviour depends not only on which events occur but also on the temporal relationships among them. Our approach, MELA, learns a Moore machine, in which outputs are associated with states. This formalism fits our CPS case studies because each trace records a categorical system-state label at every time step. As a result, the learned states are interpretable through these labels, while transitions capture how the system moves between labelled states under abstracted numeric input conditions. 

Abstraction for transition labels. Recent research has studied how raw execution data can be transformed into abstract representations for labelling or char-acterizing transitions in learned behavioural models. Such abstraction is needed when trace data contain numeric information that cannot be used directly as finite tran-sition labels in the target model. Existing approaches differ in how this abstraction is obtained. GK-tail (Lorenzoli et al. 2008) derives transition abstractions in the form of data constraints. It collects parameter values from method calls and returns, applies Daikon (Ernst et al. 2007) to infer invariants over these values, and asso-ciates the resulting invariants with EFSM transitions. FaMoS (Plambeck et al. 2024) derives transition abstractions as predicates over numeric features. After segmenting executions and clustering them into hybrid modes, it trains decision trees to iden-tify conditions associated with switches between modes; the learned predicates then characterize transitions between the modes. Aichernig et al. (Aichernig et al. 2019) abstract continuous inputs and outputs into symbolic categories, which are defined by domain experts and used directly as transition labels in the learned Mealy machine. LEAP (Majumdar et al. 2025), in contrast, does not infer transition abstractions from data; it assumes symbolic constraints on timed events are provided in advance. MELA follows the same goal of transforming raw numeric traces into finite tran-sition labels. It uses a decision tree to identify input ranges, maps these ranges to 40 symbolic categories, and uses the resulting categories as transition labels in Moore-machine learning. This produces data-driven and interpretable abstractions that are strongly associated with the system states. 

Abstraction for state space. Several studies derive an abstract state space from numeric, continuous, or latent behavioural representations. CASTLE (Tappler et al. 2024) maps high-dimensional continuous features to a lower-dimensional rep-resentation and then applies k-means clustering to obtain discrete abstract states. FaMoS (Plambeck et al. 2024) constructs abstract states by categorizing numeric traces and clustering signal segments with similar dynamical behaviour using a dynamic-time-warping-based distance measure. Aichernig et al. (Aichernig et al. 2024) derive abstract states by training a recurrent neural network (RNN) on execution traces and then clustering the resulting hidden-state vectors, which serve as continu-ous behavioural representations. GK-tail (Lorenzoli et al. 2008) constructs an abstract state space through state merging, grouping trace prefixes that exhibit similar con-tinuation behaviour. Aichernig et al. (Aichernig et al. 2019) use Mapper, together with manually defined thresholds on continuous variables, to map continuous observa-tions to a finite representation used as the state space for learning. LEAP (Majumdar et al. 2025) starts from a prefix-tree automaton over symbolic timed words and applies SMT-guided state merging to obtain a compact abstract state space. MELA uses system-state outputs to define the state space when such outputs are available. These outputs also guide the decision-tree-based abstraction of numeric inputs and become the outputs of Moore-machine states. MELA is not limited to this source of state information: it can use abstract states derived through machine-learning-based abstraction or state merging. In our case studies, the observed system-state outputs already provide a discrete and behaviourally meaningful state space. 

## 5.2 Supervised Rule Mining 

Supervised rule-mining techniques, such as decision trees and decision rules, are effec-tive for identifying predicates that relate system inputs to system states. Prior work has used these techniques mainly to characterize or explain specific SUL behaviours, for example, by identifying input conditions associated with particular program out-comes, such as passing, failing, or non-robust behaviours (Kampmann et al. 2020; Jodat et al. 2023, 2024). However, these techniques capture static relationships in the data and are not designed to model temporal relationships between states or input-driven state changes over time. In contrast, MELA uses rule mining to derive interpretable input partitions for numeric traces and then applies passive automata learning to infer the temporal structure of the SUL’s behaviour. This combination pre-serves the interpretability of rule mining while addressing its limitation in modelling state-based temporal behaviour. 

# 6 Conclusion 

In this article, we presented MELA, a passive automata learning approach enhanced with statistical machine learning for synthesizing behavioural models of CPS from numeric time-series data. MELA learns interpretable state machines that can be 41 used to check whether system requirements hold and to examine behaviours that the requirements do not fully specify. Across the network intrusion detection and autopi-lot case studies, the verification and exploration results were consistent with domain expertise, showing that the learned models capture meaningful system behaviour. Future work will explore how learned state machines can be adapted into runtime monitors for CPS, so that deviations from learned behaviour can be detected online. 

Acknowledgements We gratefully acknowledge the financial support received from RabbitRun Technologies Inc, Mitacs, and NSERC of Canada through the Discovery and I2I programs. 

Author Contributions Negin Ayoughi, Baharin A. Jodat, Shiva Nejati, and Mehrdad Sabetzadeh contributed to the conceptualization and methodology of the study. Negin Ayoughi, Baharin A. Jodat, and Armina Faghihi conducted the formal analysis and investigation. Negin Ayoughi and Baharin A. Jodat prepared the origi-nal draft. All authors contributed to reviewing and editing the manuscript. Patricio Saavedra, Shiva Nejati, and Mehrdad Sabetzadeh provided resources. Shiva Nejati and Mehrdad Sabetzadeh acquired funding and supervised the study. 

Data Availability We publicly share the implementation of MELA in our online repository (Ayoughi et al. 2026a), together with the experimental data and learned models used in our evaluation. The repository is organized by case study into five categories. Code provides the scripts for trace creation, input-variable selection, range abstraction with MELA and ABLATION, and automata learning (Ayoughi et al. 2026b). Data includes the generated time-series data, the derived learning sets, and the abstracted traces used as input to automata learning (Ayoughi et al. 2026c). 

Evaluation provides the scripts and test sets for RQ1, RQ2, and RQ3, including conformance checking and NuSMV model checking (Ayoughi et al. 2026d). Result 

reports the learned Moore machines, PTAs, the RQ1 comparison between MELA and ABLATION, and the RQ2 and RQ3 results on requirement verification and behaviour exploration (Ayoughi et al. 2026e). Testbed provides the material needed to reproduce the case-study workflows, including the IDS traffic-generation scripts and the Simulink autopilot benchmark (Ayoughi et al. 2026g). 

## Declarations 

Competing Interests The authors declare no competing interests. 

# References 

Ayoughi, N., et al.: Artifacts and Supplementary Material. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/ neayoughi/CPS-Behavioural-Model-Synthesis.git (2026) Ayoughi, N., et al.: Code for CPS Behavioural Model Synthesis with MELA. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ autopilot-final-mela-artifacts/Code (2026) 42 Ayoughi, N., et al.: Data for CPS Behavioural Model Synthesis with MELA. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ update-combined-readme/Data (2026) Ayoughi, N., et al.: Evaluation Material for CPS Behavioural Model Synthesis with MELA. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ autopilot-final-mela-artifacts/Evalution (2026) Ayoughi, N., et al.: Results for CPS Behavioural Model Synthesis with MELA. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ autopilot-final-mela-artifacts/Results (2026) Ayoughi, N., et al.: RQ2 results for the autopilot case study using the Descent 

learning set. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ update-combined-readme/Results/Autopilot/RQ2/summary/Descend RQ2.pdf (2026) Ayoughi, N., et al.: Testbeds for CPS Behavioural Model Synthesis with MELA. GitHub repository. Artifacts and Supplementary Material [Online]. Available: https://github.com/neayoughi/CPS-Behavioural-Model-Synthesis/tree/ autopilot-final-mela-artifacts/Testbed (2026) Aichernig, B.K., Bloem, R., Ebrahimi, M., Horn, M., Pernkopf, F., Roth, W., Rupp, A., Tappler, M., Tranninger, M.: Learning a behavior model of hybrid systems through combining model-based testing and machine learning. In: Proceedings of 31st IFIP WG 6.1 International Conference on Testing Software and Systems (ICTSS 2019), vol. 11812, pp. 3–21. Springer, Paris, France (2019). https://doi.org/ 10.1007/978-3-030-31280-0 1 Agostinelli, S., Bergami, G., Fiorenza, A., Maggi, F.M., Marrella, A., Patrizi, F.: Discovering declarative process model behavior from event logs via model learn-ing. In: 2021 3rd International Conference on Process Mining (ICPM), pp. 48–55. IEEE, Eindhoven, The Netherlands (2021). https://doi.org/10.1109/ICPM53251. 2021.9576870 Aichernig, B.K., K¨ onig, S., Mateis, C., Pferscher, A., Tappler, M.: Learning minimal automata with recurrent neural networks. Softw. Syst. Model. 23 (3), 625–655 (2024) https://doi.org/10.1007/S10270-024-01160-6 Aichernig, B.K., Muskardin, E., Pferscher, A.: Active vs. passive: A comparison of automata learning paradigms for network protocols. In: Proceedings of Fourth International Workshop on Formal Methods for Autonomous Systems and Fourth 43 International Workshop on Automated and Verifiable Software sYstem DEvel-opment (FMAS/ASYDE 2022), vol. 371, pp. 1–19. Electronic Proceedings in Theoretical Computer Science (EPTCS), Open Access Platform (2022). https: //doi.org/10.4204/EPTCS.371.1 Ayoughi, N., Nejati, S., Sabetzadeh, M., Saavedra, P.: Enhancing automata learning with statistical machine learning: A network security case study. In: Proceedings of the ACM/IEEE 27th International Conference on Model Driven Engineering Languages and Systems, pp. 172–182. Association for Computing Machinery, Linz, Austria (2024). https://doi.org/10.1145/3640310.3674087 Ammann, P., Offutt, J.: Introduction to Software Testing. Cambridge University Press, Cambridge, UK (2008) Aarts, F., Schmaltz, J., Vaandrager, F.: Inference and abstraction of the biometric passport. In: International Symposium On Leveraging Applications of Formal Meth-ods, Verification and Validation, pp. 673–686. Springer, Heraklion, Crete, Greece (2010). https://doi.org/10.1007/978-3-642-16558-0 54 Arrieta, A., Wang, S., Markiegi, U., Arruabarrena, A., Etxeberria, L., Sagardui, G.: Pareto efficient multi-objective black-box test case selection for simulation-based testing. Information and Software Technology 114 , 137–154 (2019) https://doi.org/ 10.1016/j.infsof.2019.06.009 Beer, I., Ben-David, S., Eisner, C., Rodeh, Y.: Efficient detection of vacuity in ACTL formulaas. In: Proceedings of 9th International Conference on Computer Aided Ver-ification (CAV 1997), vol. 1254, pp. 279–290. Springer, Berlin, Germany (1997). https://doi.org/10.1007/3-540-63166-6 28 Bartocci, E., Deshmukh, J., Gigler, F., Mateis, C., Niˇ ckovi´ c, D., Qin, X.: Mining shape expressions from positive examples. IEEE Transactions on Computer-Aided Design of Integrated Circuits and Systems 39 (11), 3809–3820 (2020) https://doi.org/10. 1109/TCAD.2020.3012240 Bartocci, E., Deshmukh, J., Mateis, C., Nesterini, E., Niˇ ckovi´ c, D., Qin, X.: Mining shape expressions with shapeit. In: International Conference on Software Engi-neering and Formal Methods, pp. 110–117. Springer, Virtual Event (2021). https: //doi.org/10.1007/978-3-030-92124-8 7 Benjamini, Y., Hochberg, Y.: Controlling the false discovery rate: A practical and pow-erful approach to multiple testing. Journal of the Royal Statistical Society: Series B (Methodological) 57 (1), 289–300 (2018) https://doi.org/10.1111/j.2517-6161.1995. tb02031.x Cimatti, A., Clarke, E.M., Giunchiglia, E., Giunchiglia, F., Pistore, M., Roveri, M., Sebastiani, R., Tacchella, A.: Nusmv 2: An opensource tool for symbolic model checking. In: Proceedings of 14th International Conference on Computer Aided 44 Verification (CAV 2002), vol. 2404, pp. 359–364. Springer, Copenhagen, Denmark (2002). https://doi.org/10.1007/3-540-45657-0 29 Clarke, E.M., Emerson, E.A., Sistla, A.P.: Automatic verification of finite-state concurrent systems using temporal logic specifications. ACM Transactions on Pro-gramming Languages and Systems (TOPLAS) 8(2), 244–263 (1986) https://doi. org/10.1145/5397.5399 Clarke, E.M., Grumberg, O., Kroening, D., Peled, D.A., Veith, H.: Model Checking, 2nd Edition. MIT Press, Cambridge, MA, USA (2018). ISBN: 9780262038836 Conover, W.: Practical Nonparametric Statistics, 3rd edn. Wiley & Sons, New York (1999) Higuera, C.: Grammatical Inference: Learning Automata and Grammars. Cambridge University Press, Cambridge, UK (2010) Ernst, M.D., Perkins, J.H., Guo, P.J., McCamant, S., Pacheco, C., Tschantz, M.S., Xiao, C.: The daikon system for dynamic detection of likely invariants. Science of computer programming 69 (1-3), 35–45 (2007) https://doi.org/10.1016/j.scico.2007. 01.015 Federal Aviation Administration: Advanced Avionics Handbook. FAA Hand-books Series. Aviation Supplies & Academics, Newcastle, WA, USA (2009). 

https://books.google.lu/books?id=2xGuPwAACAAJ 

Federal Aviation Administration: Pilot’s Handbook of Aeronautical Knowledge. Skyhorse Publishing Inc., New York, NY, USA (2009) Fiterau-Brostean, P., Janssen, R., Vaandrager, F.W.: Combining model learning and model checking to analyze TCP implementations. In: Proceedings of 28th Interna-tional Conference on Computer Aided Verification (CAV 2016), vol. 9780, pp. 454– 471. Springer, Berlin, Germany (2016). https://doi.org/10.1007/978-3-319-41540-6 25 Garhewal, B., Damasceno, C.D.N.: An experimental evaluation of conformance testing techniques in active automata learning. In: Proceedings of 26th ACM/IEEE Interna-tional Conference on Model Driven Engineering Languages and Systems (MODELS 2023), pp. 217–227. IEEE, Piscataway, NJ, USA (2023). https://doi.org/10.1109/ MODELS58315.2023.00012 G´ omez, A.C.: Inferring regular trace languages from positive and negative samples. In: Proceedings of 10th International Colloquium on Grammatical Inference (ICGI 2010), vol. 6339, pp. 11–23. Springer, Berlin, Germany (2010). https://doi.org/10. 1007/978-3-642-15488-1 3 45 Giannakopoulou, D., Pressburger, T., Mavridou, A., Schumann, J.: Automated for-malization of structured natural language requirements. Information and Software Technology 137 , 106590 (2021) https://doi.org/10.1016/j.infsof.2021.106590 Giantamidis, G., Tripakis, S., Basagiannis, S.: Learning moore machines from input-output traces. Int. J. Softw. Tools Technol. Transf. 23 (1), 1–29 (2021) https://doi. org/10.1007/S10009-019-00544-0 Hall, M.: Complexity metrics for hierarchical state machines. In: Proceedings of Third International Symposium on Search Based Software Engineering (SSBSE 2011), vol. 6956, pp. 76–81. Springer, Berlin, Germany (2011). https://doi.org/10.1007/ 978-3-642-23716-4 10 Hranisavljevic, N., Maier, A., Niggemann, O.: Discretization of hybrid cpps data into timed automaton using restricted boltzmann machines. Engineering Applications of Artificial Intelligence 95 , 103826 (2020) https://doi.org/10.1016/j.engappai.2020. 103826 Hranisavljevic, N., Niggemann, O., Maier, A.: A novel anomaly detection algorithm for hybrid production systems based on deep learning and timed automata. In: Proceedings of the International Workshop on Principles of Diagnosis, pp. 1–8. International Workshop on the Principles of Diagnosis, Denver, CO, USA (2016). https://doi.org/10.48550/arXiv.2010.15415 Hajnorouzi, M., Rakow, A., Fr¨ anzle, M.: Model learning for adjusting the level of automation in HCPS. In: Proceedings of 7th International Workshop on Formal Methods for Autonomous Systems (FMAS@iFM 2025). EPTCS, pp. 96–113. Open Publishing Association, Paris, France (2025). https://doi.org/10.4204/EPTCS.436. 10 Jodat, B.A., Chandar, A., Nejati, S., Sabetzadeh, M.: Test generation strategies for building failure models and explaining spurious failures. ACM Trans. Softw. Eng. Methodol. 33 (4) (2024) https://doi.org/10.1145/3638246 Jodat, B.A., Gaaloul, K., Sabetzadeh, M., Nejati, S.: Automated test validators for flaky cyber-physical system simulators: Approach and evaluation. IEEE Transac-tions on Software Engineering abs/2508.20902 (2026) https://doi.org/10.1109/ TSE.2026.3685556 Jodat, B.A., Nejati, S., Sabetzadeh, M., Saavedra, P.: Learning non-robustness using simulation-based testing: a network traffic-shaping case study. In: Proceedings of IEEE Conference on Software Testing, Verification and Validation (ICST 2023), pp. 386–397. IEEE, Piscataway, NJ, USA (2023). https://doi.org/10.1109/ICST57152. 2023.00043 Khandait, T., Formica, F., Arcaini, P., Chotaliya, S., Fainekos, G., Hekal, A., Kundu, A., Lew, E., Loreti, M., Menghi, C., Nenzi, L., Pedrielli, G., Peltom¨ aki, J., Porres, I., 46 Ray, R., Soloviev, V., Visconti, E., Waga, M., Zhang, Z.: Arch-comp 2024 category report: Falsification. In: Proceedings of the 11th Int. Workshop on Applied Verifica-tion for Continuous and Hybrid Systems. EPiC Series in Computing, vol. 103, pp. 122–144. EasyChair, Boulder, CO, USA (2024). https://doi.org/10.29007/hgfv Kampmann, A., Havrikov, N., Soremekun, E.O., Zeller, A.: When does my program do this? learning circumstances of software behavior. In: Proceedings of the 28th ACM Joint Meeting on European Software Engineering Conference and Symposium on the Foundations of Software Engineering, pp. 1228–1239. Association for Computing Machinery, Virtual Event, USA (2020). https://doi.org/10.1145/3368089.3409687 Lorenzoli, D., Mariani, L., Pezz` e, M.: Automatic generation of software behavioral models. In: Proceedings of 30th International Conference on Software Engineering (ICSE 2008), pp. 501–510. Association for Computing Machinery (ACM), Leipzig, Germany (2008). https://doi.org/10.1145/1368088.1368157 Lockheed Martin Corporation: Lockheed Martin. https://www.lockheedmartin.com. Last accessed: June 2026 (2026) Linz, P., Rodger, S.H.: An Introduction to Formal Languages and Automata. Jones & Bartlett Learning, Burlington, MA, USA (2022) Luke, S.: Essentials of Metaheuristics, 2nd edn. Lulu, Morrisville, NC, USA (2013). Available for free at http://people.cs.gmu.edu/ ∼sean/book/metaheuristics Muskardin, E., Aichernig, B.K., Pill, I., Pferscher, A., Tappler, M.: Aalpy: An active automata learning library. In: Proceedings of 19th International Sympo-sium on Automated Technology for Verification and Analysis (ATVA 2021), vol. 12971, pp. 67–73. Springer, Berlin, Germany (2021). https://doi.org/10.1007/ S11334-022-00449-3 Majumdar, A., Mukherjee, S., Raskin, J.: Learning event-recording automata pas-sively. In: Proceedings of 23rd International Symposium on Automated Technology for Verification and Analysis (ATVA 2025), vol. 16145, pp. 27–48. Springer, Bengaluru, India (2025). https://doi.org/10.1007/978-3-032-08707-2 2 Medhat, R., Ramesh, S., Bonakdarpour, B., Fischmeister, S.: A framework for min-ing hybrid automata from input/output traces. In: 2015 International Conference on Embedded Software (EMSOFT), pp. 177–186. IEEE, Amsterdam, Netherlands (2015). https://doi.org/10.1109/EMSOFT.2015.7318273 Nejati, S., Gaaloul, K., Menghi, C., Briand, L.C., Foster, S., Wolfe, D.: Evaluating model testing and model checking for finding requirements violations in simulink models. In: Proceedings of 27th ACM Joint Meeting on European Software Engi-neering Conference and Symposium on the Foundations of Software Engineering (ESEC/FSE 2019), pp. 1015–1025. Association for Computing Machinery, New York, NY, USA (2019). https://doi.org/10.1145/3338906.3340444 47 Neider, D., Smetsers, R., Vaandrager, F.W., Kuppens, H.: Benchmarks for automata learning and conformance testing. In: Models, Mindsets, Meta: The What, the How, and the Why Not? - Essays Dedicated to Bernhard Steffen on the Occasion of His 60th Birthday, vol. 11200, pp. 390–416. Springer, Berlin, Germany (2018). https: //doi.org/10.1007/978-3-030-22348-9 23 Ovsiannikova, P., Chivilikhin, D., Ulyantsev, V., Stankevich, A., Zakirzyanov, I., Vyatkin, V., Shalyto, A.: Active learning of formal plant models for cyber-physical systems. In: 2018 IEEE 16th International Conference on Industrial Informat-ics (INDIN), pp. 719–724. IEEE, Porto, Portugal (2018). https://doi.org/10.1109/ INDIN.2018.8471924 Oncina, J., Garcia, P.: Identifying regular languages in polynomial time. In: Advances in Structural and Syntactic Pattern Recognition, pp. 99–108. World Scientific, Singapore (1992). https://doi.org/10.1142/9789812797919 0007 Pferscher, A., Aichernig, B.K.: Fingerprinting and analysis of bluetooth devices with automata learning. Formal Methods Syst. Des. 61 (1), 35–62 (2022) https://doi.org/ 10.1007/S10703-023-00425-Y Plambeck, S., Bracht, A., Hranisavljevic, N., Fey, G.: Famos- fast model learning for hybrid cyber-physical systems using decision trees. In: Proceedings of 27th ACM International Conference on Hybrid Systems: Computation and Control (HSCC 2024), pp. 7–1710. Association for Computing Machinery (ACM), Hong Kong SAR, China (2024). https://doi.org/10.1145/3641513.3650131 Prasetiyowati, M.I., Maulidevi, N.U., Surendro, K.: Determining threshold value on information gain feature selection to increase speed and prediction accuracy of random forest. Journal of Big Data 8(1), 84 (2021) https://doi.org/10.1186/ S40537-021-00472-4 Pnueli, A.: The temporal logic of programs. In: Proceedings of 18th Annual Sym-posium on Foundations of Computer Science, pp. 46–57. IEEE Computer Society, Piscataway, NJ, USA (1977). https://doi.org/10.1109/SFCS.1977.32 Rapid7: Metasploit Framework. https://www.metasploit.com/. Last accessed: June 2026 (2024) Sam Elshamy: DC Motor Model Simulink Model. https://www.mathworks.com/ matlabcentral/fileexchange/11587-dc-motor-model-simulink. Last accessed: June 2026 (2026) Tappler, M., Aichernig, B.K., Bloem, R.: Model-based testing iot communication via active automata learning. In: 2017 IEEE International Conference on Software Test-ing, Verification and Validation (ICST), pp. 276–287. IEEE, Piscataway, NJ, USA (2017). https://doi.org/10.1109/ICST.2017.32 . IEEE 48 Tuncali, C.E., Fainekos, G., Prokhorov, D., Ito, H., Kapinski, J.: Requirements-driven test generation for autonomous vehicles with machine learning components. IEEE Transactions on Intelligent Vehicles 5(2), 265–280 (2019) https://doi.org/10.1109/ TIV.2019.2955903 The MathWorks, Inc.: Building a Clutch Lock-Up Model. https://www.mathworks. com/help/simulink/slref/building-a-clutch-lock-up-model.html. Last accessed: June 2026 (2026) The MathWorks, Inc.: Cruise Control Test Generation. https://www.mathworks.com/ help/sldv/ug/cruise-control-test-generation.html. Last accessed: June 2026 (2026) The MathWorks, Inc.: Design a Guidance System in MATLAB and Simulink. https://www.mathworks.com/help/simulink/slref/ designing-a-guidance-system-in-matlab-and-simulink.html. Last accessed: June 2026 (2026) Tappler, M., Muskardin, E., Aichernig, B.K., K¨ onighofer, B.: Learning environment models with continuous stochastic dynamics - with an application to deep RL testing. In: Proceedings of IEEE Conference on Software Testing, Verification and Validation (ICST 2024), pp. 197–208. IEEE, Toronto, ON, Canada (2024). https://doi.org/10.1109/ICST60714.2024.00026 Vaandrager, F.W.: Model learning. Commun. ACM 60 (2), 86–95 (2017) https://doi. org/10.1145/2967606 Berg, B., Aichernig, B.K.: Extending aalpy with passive learning: A generalized state-merging approach. In: Proceedings of 37th International Conference on Computer Aided Verification (CAV 2025), vol. 15934, pp. 127–140. Springer, Zagreb, Croatia (2025). https://doi.org/10.1007/978-3-031-98685-7 6 Berg, B., Aichernig, B.K.: Extending aalpy with passive learning: A generalized state-merging approach. In: Proceedings of 37th International Conference on Computer Aided Verification (CAV 2025), pp. 127–140. Springer, Cham (2025). https://doi. org/10.1007/978-3-031-98685-7 6 Vargha, A., Delaney, H.: A critique and improvement of the cl common language effect size statistics of mcgraw and wong. Journal of Educational and Behavioral Statistics 

25 (2), 101–132 (2000) https://doi.org/10.3102/10769986025002101 Zargar, S.T., Joshi, J., Tipper, D.: A survey of defense mechanisms against distributed denial of service (ddos) flooding attacks. IEEE Commun. Surv. Tutorials 15 (4), 2046–2069 (2013) https://doi.org/10.1109/SURV.2013.031413.00127 49