---
title: Synchronous Observers Revisited for Runtime Verification of Lustre Using STL
title_zh: 同步观察器再探：使用STL对Lustre进行运行时验证
authors: "Logan Kenwright, Partha Roop, Sobhan Chatterjee, Nathan Allen"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12693v1"
tags: ["query:fsh"]
score: 8.0
evidence: 将STL编译为Lustre同步观察器的运行时验证技术，适用于硬件设计中的同步数据流语言。
tldr: 针对STL在同步语言中较少用于运行时验证的问题，提出将同步STL片段（SSTL）编译为Lustre同步观察者的技术。支持任意嵌套有界属性及全局无界外层算子，观察者可同时用于在线监控和Kind2静态验证。另提供交互式可视化器展示三值判定，并在弹簧-质量系统和跟车巡航控制上验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.12693v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 679, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.12693v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 651, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.12693v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1285, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.12693v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1285, \"height\": 480, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.12693v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1281, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.12693v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1256, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.12693v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 587, \"height\": 345, \"label\": \"Table\"}]"
motivation: STL常用于运行时验证，但同步语言中安全属性多用同步观察者表达，其在运行时验证中很少被探索。
method: 将SSTL模块化编译为Lustre观察者，允许任意嵌套有界属性和全局无界外层算子，兼顾运行时与静态验证。
result: 实现交互式可视化器，在可编辑轨迹上展示三值判定，并在弹簧-质量系统和巡航控制两个案例中评估。
conclusion: 该技术使同步观察者同时服务于在线监控和模型检验，为同步语言与STL结合提供新途径。
---

## 摘要
信号时序逻辑（STL）是一种用于网络物理系统时间安全属性的流行形式，最常用于运行时验证。在同步语言族中，安全属性则表示为同步观察器，即与程序组合用于静态验证的模块，它们也是适用于运行时验证的可运行规范，尽管这种用途很少被探索。我们提出了一种将STL的同步片段（SSTL）编译为数据流语言Lustre中的同步观察器的技术。与先前工作不同，我们通过模块化编译允许有界SSTL属性的任意嵌套，并允许用于在线监控的全局无界外部算子；由此产生的观察器既服务于运行时验证，又作为副产品服务于使用Kind2模型检查器的静态验证。我们进一步贡献了一个交互式可视化器，可在可编辑轨迹上呈现属性的三值判定结果，并在文献中的两个案例研究上进行了评估：一个弹簧-质量系统和一个车辆跟随巡航控制器。

## Abstract
Signal Temporal Logic (STL) is a popular formalism for the temporal safety properties of cyber-physical systems, most often used for runtime verification. In the synchronous family of languages, safety properties are instead expressed as synchronous observers, modules composed with a program for static verification, which are also runnable specifications suitable for runtime verification, though this use is rarely explored. We present a technique for compiling the synchronous fragment of STL (SSTL) into synchronous observers in the dataflow language Lustre. Unlike previous work, we allow arbitrary nesting of bounded SSTL properties via modular compilation, and admit a globally unbounded outer operator for online monitoring; the resulting observers serve both runtime verification and, as a by-product, static verification with the Kind2 model checker. We further contribute an interactive visualiser that renders a property's three-valued verdict over an editable trace, and evaluate on two case studies from the literature: a spring-mass system and a car-following cruise controller.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：信号时序逻辑（STL）是网络物理系统（CPS）时间安全属性规范的主流形式，最常用于**运行时验证**。然而，在同步语言族（如 Lustre）中，安全属性通常以**同步观察器**形式表达，主要用于静态验证，其作为可运行规范用于运行时验证的潜力很少被探索。
- **与现有工作的差距**：
  - 现有 STL 工具大多监控**预记录或模拟信号**，或运行于抽象自动机模型上，缺少与系统同步执行、具备运行时等价语义的可执行监控器。
  - 先前的相关工作（如 Bellanger et al.）仅支持**非嵌套**的有界 STL 算子，且偏向静态验证（Kind2 contracts），而非语言原生的可执行规范。
- **整体含义**：本文提出将同步 STL（SSTL）编译为与系统同一语言（Lustre）的同步观察器，使**同一个产物**既能在线监控运行时行为，又能作为静态验证义务由模型检查器（Kind2）直接检验，从而弥合在线监控与静态验证之间的鸿沟。

## 2. 论文提出的方法论（核心思想、关键技术细节、流程）

- **核心思想**：
  - 基于同步假设，将连续时间信号离散为逻辑 tick；将 SSTL 公式展开为有限个固定窗口的**叶子义务（leaf obligation）**，每个叶子由统一的 `until_leaf` 原语实现。
  - 采用**三值 Kleene 流式语义**（`def_true`/`def_false`/`unknown`）：监控器可在公式时域（horizon）之前尽早提交确定答案。
- **关键构造**：
  - **`until_leaf` 原语**（Listing 1.1）：一个 Lustre 节点实现有界直到 `ψ U[a,b] φ`，通过计数器 `n`、窗口标志 `in_win`、激活标志 `in_active`、前提存活性 `alive` 和见证锁存器 `found` 生成正向（`rp`，def_true）和负向（`rn`，def_false）判定。`♢[a,b]` 是 `ψ=true` 的特例；`□[a,b]` 通过德摩根对偶（`¬♢¬`）实现。
  - **模块化嵌套合成**（Algorithm 1）：从公式树自底向上递归合成。外层有界算子 `□` 或 `♢` 为内层子公式的每个起始 tick 实例化一个叶子；`∧`/`∨` 按 Kleene 规则聚合子对的判定；`U` 算子通过维护各分支的 `alive` 位和锁存器来处理前提条件的时序依赖。
  - **无界外层 `□`**（Algorithm 2）：利用 Lemma 2（每个有界副本身在内部时域 H 内收敛），维护一个 **H+1 槽移位寄存器**来复用跨起始 tick 的状态，对落在寄存器末端的副本汇总结论；无界 `□` 只能被**证伪**（latch FALSE），不能有限前提前被证实。
  - **可视化器**：浏览器端工具，在可编辑轨迹上渲染每个外层算子副本的逐 tick 三值判定甘特图，并支持与 lv6 编译器结果交叉核对。
- **正确性证明**：Lemma 1（叶子语义正确）、Lemma 2（收敛性与因果性）、Lemma 3（组合替换的可靠性）、Theorem 1（对任意有界 SSTL 公式，合成观察器判定与显式三值语义完全一致，即充要条件）。

## 3. 实验设计（数据集/场景、Benchmark、对比方法）

- **场景 1：弹簧-质量系统**（Spring-Mass，参照 Bellanger et al.）
  - 阻尼二阶系统，4 个布尔谓词（`reach`、`weps`、`wstrict`、`mono`），监控 4 条嵌套属性：
    - P1：`□[2,4](♢[3,5] reach)`（2 层嵌套，超越 Bellanger 等人的非嵌套片段）
    - P2：`□(♢[5,20](□[1,5] weps))`（无界外 □，仅可证伪）
    - P2'：`□(♢[3,5](□[1,2] wstrict))`（收紧阈值，预期被违反）
    - P3：`mono U[3,15](♢[1,5](□[1,3] weps))`（U 作为最外层算子）
- **场景 2：自适应巡航控制（ACC）紧急制动**
  - 两车跟随场景，4 个布尔谓词（`safe_gap`、`adeq_gap`、`gap_settled`、`vel_moving`），监控 4 条镜像属性：Q1、Q2、Q2'、Q3。
- **验证工具链**：
  - 运行时验证用 **lv6**（Lustre 解释器）在线执行。
  - 静态交叉验证用 **Kind2** 模型检查器，对比运行时与静态判定是否一致。
  - 测速用 lv6 的 C 后端，在 2020 款 MacBook Pro 上测量纯观察器计算的 **每 tick 纳秒数**。
- **对比方法**：主要与 **Bellanger et al. [5]** 对比（非嵌套、Kind2 契约导向），以及提及 Jakšić 等人、Reinbacher 等人、RTAMT、STLmc 等工具，但未见直接数值对比实验。

## 4. 资源与算力

- 论文明确提到了性能测量用的硬件为 **2020 款 MacBook Pro**（无具体型号细节），但**未提及任何 GPU 型号、数量或训练时长**——因为方法属于形式化验证与编译合成领域，不涉及深度学习或大规模并行计算。
- 所有的逻辑验证（lv6 解释、Kind2 模型检查）均可在普通桌面级硬件上完成；每 tick 执行成本在 **6.7–206.5 ns** 区间，远低于毫秒级控制周期。

## 5. 实验数量与充分性

- **实验数量**：共 2 个案例系统、8 条监控属性（含 2 条无界 □ 变体）、每条属性均做了运行时判定与静态模型检查交叉验证，并测算了每 tick 性能。
- **充分性评估**：
  - **优点**：覆盖了 2–3 层嵌套、三种有界算子（□/♢/U）作为最外层、无界外 □、布尔连接词组合、满足与违反两种结局，覆盖面较广；每个观察器都验证了互斥不变量（`pos∧neg=false`）。
  - **不足**：两个系统均为轻量化的最小实例，非公开标准 benchmark；没有与其他在线 STL 监控工具（如 RTAMT）做**直接的性能或内存横向对比**；未做消融实验（如不同窗口宽度对叶数量/性能的敏感性）；对比主要停留在定性层面。

## 6. 论文的主要结论与发现

- 合成观察器可在系统同一同步语言中运行，**三值语义能在公式时域之前给出确定判定**：
  - 所有有界属性均在 horizon 前收敛，如最深嵌套的 P3 在 tick 8 即判定 T（horizon 23），提前 15 tick。
- **无界 □ 作为证伪算子**行为符合预期：安全属性（P2、Q2）全程无违例；被收紧的变体（P2'、Q2'）在违反窗口关闭的瞬间即 latched FALSE。
- 运行时（lv6）与静态（Kind2）判定在全部属性上一致，验证了“同一产物既可在线监控又可静态验证”的核心主张。
- 性能开销极低（最慢 206.5 ns/tick），足以支撑真实的在线监控场景。

## 7. 优点

- **任意嵌套支持**：突破了 Bellanger 等人的非嵌套限制，通过模块化递归合成支持任意深度的 `□`/`♢`/`U` 嵌套。
- **双用途产物**：观察器既是语言原生可执行规范，可直接随系统编译在线运行；又是 Kind2 可消费的静态验证义务，省去另建模型的开销。
- **无界外层 □**：通过移位寄存器回收有界观察器状态，实现了对自由运行系统的无界全局属性监控（可证伪）。
- **三值因果语义**：`unknown` 表示时域未过、日志尚未决定，且判定在时域前即可锁定，配合每 tick 常数时间开销，实用性强。
- **交互式可视化器**：将嵌套算子的横跨多窗口的动态判定过程可视化，显著降低了嵌套公式的调试和教学难度。
- **证明完备**：给出 Leaf/Composition/Induction 三级引理与定理，对任意有界 SSTL 公式保证观察器语义与显式枚举语义完全一致（sound & complete）。

## 8. 不足与局限

- **叶节点爆炸**：叶子数量是嵌套窗口宽度的乘积，如 `♢[0,100]` 这类大窗口会导致观察器规模爆炸；作者指出可共享重叠叶子或使用符号枚举，但留待未来工作。
- **闭式系统假设**：两个案例均在 Lustre 内部建模整个闭环系统，观察器读取的是内部生成信号，而非真实传感器/环境输入；对开放系统的在线部署尚未验证。
- **仅限离散采样域**：未支持连续 STL 鲁棒性语义（robustness degree），对采样引起的误判问题未做处理。
- **对比实验不足**：缺少与其他在线监控工具（如 RTAMT）的直接定量对比；性能测量未与内存占用、代码体积等指标系统评估。
- **无界 □ 的信息量有限**：在有限前缀上只能证伪、不能证实，实用性受限；作者也承认单窗口判定（per-window verdict）往往更有信息量。

（完）
