Title: Formally Verified Synthesizable Floating-Point Data Types in ARCH HDL

URL Source: https://arxiv.org/pdf/2607.23715v1

Published Time: Tue, 28 Jul 2026 01:11:09 GMT

Number of Pages: 8

Markdown Content:
# Formally Verified Synthesizable Floating-Point Data Types in ARCH HDL 

## Shuqing Zhao 

## shuqing.zhao@gmail.com 

## July 28, 2026 

# Abstract 

We report the design and end-to-end verification of first-class IEEE-754 binary32 (FP32) and bfloat16 (BF16) arithmetic for ARCH, a hardware description language in-tended to be generated by language models from natural-language specifications. Every floating-point operator — comparisons, conversions, add, sub, multiply, and fused multiply–add, in both formats — is described once in a host-language embedded DSL against a single bit-vector intermediate representation, which is rendered three ways from one source: to synthesizable SystemVerilog (the hardware), to SMT-LIB (a formal model), and to Lean 4 (a structured proof model). Because the three back-ends consume one description and one linearization, the hard-ware, the formal model, and the proof model cannot drift apart structurally: they are the same dataflow graph printed in three syntaxes. What remains between them is the per-node syntax table of roughly twenty printers; on the hardware side even that is machine-checked — Yosys independently reads the emitted SystemVerilog and a solver proves it equivalent to the SMT model, for all operators. We verify the operators with a hybrid strategy matched to the solver-tractability frontier: the multiplier-free operators (all six FP32 comparisons and bounded add/sub over the full 264 input space, the format conver-sions, and all binary BF16 arithmetic over 232 ) are proved equivalent to the SMT-LIB FloatingPoint theory ex-haustively; the multiplier-bearing operators (FP32 mul 

and fma ), which are SAT-hard for any bit-blaster, are machine-proved correctly rounded in Lean by algebraic lifting, with zero sorry . (The one deliberate exception: the BF16 fma is an FP32-accumulating fusion — the Tensor-Core/TPU convention — machine-characterized as exactly that, not claimed correctly rounded.) Physical characterization (open-source Yosys synthesis and Open-STA timing) then exposed the fma as the timing outlier: its exact-wide 470 -bit aligned adder is a single combi-national cone that our synthesis flow cannot pipeline (ABC retiming moves no registers through it). We there-fore reimplemented the fma as a bounded 98 -bit guard / round / sticky datapath that pipelines cleanly to 268 MHz — and proved, in Lean and sorry -free, that the bounded datapath is bit-identical to the exact-wide reference over all 296 inputs, so it inherits the reference’s proven correct rounding without re-engaging the multiplier. The equiv-alence proof is tractable precisely because the shared multiplier appears on both sides and cancels. We give the architecture, the two-tier proof methodology, the synthesis results, the trust base (the standard three ax-ioms plus bv_decide ’s native-checked LRAT-certificate axioms), and an independent adversarial audit. 

# 1 Introduction 

ARCH 1 is a purpose-built hardware description language for micro-architecture work, designed so that the lan-guage surface can be generated correctly by language models from natural-language hardware descriptions, and compiled to deterministic, readable SystemVerilog. A lan-guage meant to describe real datapaths needs first-class floating-point: not a library bolted on, but FP32 [ 1] and BF16 [ 2] as built-in types with operators that are (i) 

correctly rounded to IEEE-754, (ii) synthesizable to effi-cient RTL, and (iii) trustworthy , since machine-generated hardware most needs machine-checked guarantees. These three goals are in tension. Correct rounding is a precise but subtle specification. Exhaustive formal equivalence checking hits a wall at the one operator that matters most for throughput — the fused multiply–add — because its significand multiplier makes the miter SAT-hard [ 9 , 11 ](a miter ties the two circuits under comparison to shared inputs and XORs their outputs, reducing equivalence to unsatisfiability of that XOR). And the most obviously-correct fma datapath (compute the exact wide product-sum, then round) is the worst for timing. This paper reports how we resolved all three for ARCH, and the path is the contribution as much as the endpoint: 1. One source, three back-ends. Each operator is defined once against a shared bit-vector IR — a DAG whose subterms are shared by construction; three renderers emit synthesizable SystemVerilog, SMT-LIB, and Lean 4 from that one description and one linearization (Section 3). The hardware, the SMT model, and the proof model are structurally the same 

> 1https://github.com/arch-hdl-lang/arch-com

1arXiv:2607.23715v1 [cs.CL] 26 Jul 2026 artifact in three syntaxes; the SystemVerilog printer’s semantic correspondence is machine-checked by a Yosys-to-SMT miter, the Lean printer’s by a byte-identical regeneration audit (Section 10). 2. A two-tier proof methodology at the tractabil-ity frontier. Multiplier-free operators are dis-charged exhaustively by SMT against the IEEE-754 

FloatingPoint theory; multiplier-bearing operators are structurally proved in Lean by lifting the bit pattern to an algebraic value, so the 24 × 24 array never gets bit-blasted (Section 4). 3. Timing-driven design, verified after the fact. 

Synthesis and static timing (Section 5) show the exact-wide fma is the outlier and resisted retiming (ABC moved no registers through the cone in any tested configuration); we redesign it as a bounded sticky-fold datapath (Section 6) that pipelines to 

268 MHz, and prove it bit-identical to the exact-wide reference over all 296 inputs (Section 7), so the fast datapath inherits the reference’s proven cor-rect rounding. The equivalence is provable without re-engaging the multiplier, because the shared mul-tiplier cancels. The result is a floating-point feature whose every op-erator is either exhaustively or structurally verified against its stated contract (for the BF16 fma , an FP32-accumulating fusion, that contract is stated rather than correct rounding; Section 4.3), on a model that is prov-ably the emitted RTL, with a clean axiom base and an independent audit (Section 8). 

# 2 Background 

Correct rounding. For a destination format with p-bit significand, a correctly rounded operation returns 

RNE (v), where v is the exact real result and RNE is round-to-nearest, ties-to-even [ 1]. For fma , v = a ·

b + c formed without intermediate rounding. “Correctly rounded” is thus two claims: form the exact value, and round it. 

Guard / round / sticky. Hardware keeps the result significand plus three summary bits: guard (the first bit below the kept significand), round (the next), and sticky (the OR of all lower bits). RNE on a GRS-encoded value is exact — round up iff guard ∧ (round ∨ sticky ), ties to even — so the engineering problem is producing the GRS bits without materializing the full exact value. 

The multiplier wall. After special values are dis-patched, equivalence of an arithmetic circuit to its speci-fication is a quantifier-free bit-vector (and floating-point) formula. Bit-blasting turns the 24 × 24 significand mul-tiplier into a quadratic gate network whose conjunctive        

> one operator description (Rust eDSL)
> bit-vector IR: pointer-shared DAG
> shared linearization: one SSA order _t0 . . . _tN
> render_sv
> SystemVerilog
> render_smt
> SMT-LIB
> render_lean
> Lean 4
> synthesis, sim; SV →SMT miter: 24/24 unsat
> miters vs. fp.*
> (z3 / cvc5, §4.1)
> equivalence + value
> proofs (§4.2, §7– 8); printer: byte-identical regen
> proved: one struc-ture, one order

Figure 1: One description, three back-ends. Everything above the fork is a single shared artifact (structural iden-tity is by construction); the residual trust is the per-node syntax table below the fork; the SystemVerilog side is dis-charged mechanically by the Yosys-to-SMT miter (all 24 

operators unsat , §10), the Lean side by the byte-identical regeneration audit. normal form is intractable across the binary32 input space. Multiplier miters are canonically hard SAT instances — a standard benchmark family for exactly this reason [ 9], with lower-bound analyses of why solvers struggle on nonlinear integer arithmetic [ 10 ] — hard enough that state-of-the-art multiplier verification abandons plain SAT for computer algebra [ 11 ]. Consistently, z3, cvc5, and the Lean tactic bv_decide [ 8] all stall on mul and 

fma here. Everything without a multiplier — compar-isons, conversions, and bounded add/sub whose datapath stays narrow — is exhaustively decidable; the multiplier-bearing operators are not. Our methodology is organized around exactly this frontier. 

# 3 Single-source architecture 

Each operator is built once, in Rust, against a pointer-shared bit-vector IR (Figure 1): a directed acyclic graph of nodes ( add , sub , mul , ite , extract , concat ,

zero_extend , the bit-vector comparisons, and named calls), where a subterm the builder reuses is one node, not a copy. One shared linearization pass flattens that DAG into a straight-line program: a single topologi-cal sequence of temporary assignments _t0 , _t1 , . . . in single-static-assignment form — each temp defined once, referring only to earlier temps, each shared node emitted exactly once. The three renderers walk that one num-bered sequence (Table 1) and differ only in how a line is spelled, via the per-node syntax table (a Bin::Add 

node prints + in SystemVerilog, bvadd in SMT-LIB, and 

+ on BitVec in Lean); consequently _t44 names the same node in the hardware, the SMT model, and the proof model. Because all three consume the same IR and the same linearization, they cannot disagree on sharing, control flow, or node order; the Lean proof model is a generated snapshot of render_lean , and we confirm (Section 8) it 2renderer output consumer         

> render_sv SystemVerilog arch build render_smt SMT-LIB2 define-fun sz3 / cvc5
> render_lean Lean 4 BitVec def sthis proof

Table 1: One bit-vector IR, three renderers. Sharing the IR and the linearization, the renderers cannot disagree on structure — only on the per-operator syntax table. is byte-identical to a fresh regeneration — so the proof is about the emitted artifact, not a paraphrase. The only way the three artifacts can diverge is a wrong en-try in the syntax table — a mistranslation of a single node kind, which would appear uniformly in every op-erator using that node, not as an operator-specific bug. That failure mode is what Section 10 scopes as the resid-ual renderer trust — and what the Yosys-to-SMT miter now discharges mechanically for the SystemVerilog ren-derer. The operator set spans the six FP32 comparisons; FP32 add , sub , mul , fma ; the conversions FP32 →BF16 (narrowing, RNE), BF16 →FP32 (widening, exact), and FP32 →signed/unsigned integer (toward-zero, saturat-ing); and the BF16 comparisons and arithmetic, which route through the FP32 datapath. NaN canonicalization follows a selectable profile (e.g. the RISC-V vs. CUDA conventions). 

# 4 Verification methodology 

## 4.1 Tier 1 — exhaustive SMT for the multiplier-free operators 

The SMT renderer emits each operator as a define-fun ;a miter asserts the negation of equivalence to the SMT-LIB FloatingPoint theory [ 7] — the standardized for-malization of IEEE-754 RNE, the same value seman-tics Berkeley SoftFloat implements. An unsat verdict means the emitted RTL operator matches the theory’s RNE value semantics — the result bit pattern, with our canonical-NaN choice pinned explicitly in the miter — over its entire input space. We discharge, exhaustively: 

• all six FP32 comparisons and the conversions over their full domains (the 264 compare space; FP32 →BF16 narrowing over 232 ; BF16 →FP32 widening over 216 ; FP32 →integer for in-range inputs, the partial-function boundary documented rather than silently claimed); 

• FP32 add and sub versus fp.add /fp.sub over all 

264 inputs. The decisive factor is datapath width, not input space: the bounded adder keeps the aligned magnitude near 56 bits (no multiplier), so the bit-blasted miter stays small; 

• all binary BF16 arithmetic ( mul , add , sub , and the six compares) over the full 232 input space. BF16’s small input space makes even the multiplier-bearing binary operators tractable, even though they route through the FP32 datapath; cross-checked across solvers. The ternary BF16 fma (a 248 space) is deliberately not on this list: it is FP32-accumulating, and its distinct contract is stated in Section 4.3. 

The exact contract. “IEEE-754” in this paper means 

result-bit value semantics under a fixed rounding regime ,and no more. Rounding is statically RNE for arithmetic and format conversions (toward-zero for the saturating float-to-integer path); there is no dynamic rounding-mode input, and no status flags — invalid , inexact ,

overflow , underflow are not computed. NaN handling is by explicit canonical profile: IEEE-754 and SMT-LIB both abstract NaN payloads, so each miter’s NaN branch pins our canonicalized output rather than appealing to the theory. Comparisons implement the quiet predicates (fp.eq and kin); there are no signaling variants and hence no invalid-operation side channel. Float-to-integer is sat-urating with defined NaN and out-of-range results at the ARCH level; SMT-LIB leaves fp.to_sbv /fp.to_ubv 

unspecified there, so the miter covers exactly the in-range domain where the theory is defined, and the satu-ration/NaN behavior is a documented ARCH contract validated by directed simulation rather than by the proof. Finally, all three models are two-state : the IR and its SMT and Lean renderings have no X/Z, so SystemVerilog four-state behavior ( X propagation, high impedance) is outside the verified contract, which speaks to fully driven binary inputs. 

## 4.2 Tier 2 — structured Lean for the multiplier wall 

FP32 mul and fma carry the 24 × 24 multiplier and time out for any bit-blaster. A structured prover clears the wall by not bit-blasting. Following the Flocq / FloVer methodology [ 3, 4], the proof lifts the bit pattern to an al-gebraic (sign , significand , exponent ) view over a rational value and shows the operator computes RNE (a · b [+ c]) 

structurally: the 24 × 24 array collapses to a single Nat 

multiply, whose algebra is elementary. The develop-ment factors into a reusable rounder characterization — that the shared round-to-nearest-even encoder finds the true leading bit (a count-leading-zeros / most-significant-bit bridge), performs an exact round-trip, and satisfies 

1 · x = x — and per-operator reductions on top of it. For 

fma this yields arch_round470_correct and a sorry -free proof that the finite FP32 fma is correctly rounded against a value-level RNE specification. The development is dependency-free (Lean core BitVec and bv_decide ;no Mathlib). 34.3 BF16 fma : an honest exception 

The BF16 fma is intentionally a fused f32-accumulate :one correctly rounded FP32 fma followed by a narrowing rounding FP32 →BF16. It is therefore not a correctly rounded BF16 fma : the second rounding is not innocu-ous, and it differs from RNE bf16 (a · b + c) on a small fraction of finite inputs, always by one unit in the last place. This is the FP32 accumulation-format convention of NVIDIA tensor cores [ 12 ], Google TPUs [ 13 ], and BF16 training practice [ 2] — a convention about the accumulation format only; vendor implementations are not otherwise bit-identical (TPU subnormal handling differs, for instance). The operator is separately machine-characterized as “the narrowing of the correctly rounded FP32 fma ” rather than claimed as correctly rounded. An earlier “innocuous double rounding” argument for it was a round-to-nearest fallacy; we record the corrected characterization. 

## 4.4 Differential backstop 

The same IR also drives a differential campaign against Berkeley SoftFloat [ 15 ], the de-facto reference IEEE-754 implementation, on directed corner vectors (signed ze-ros and infinities, quiet and signaling NaNs, subnormal and normal extrema, exact ties, overflow and underflow boundaries) and large randomized vectors, providing an empirical cross-check independent of the SMT theory. 

# 5 Physical characterization 

We synthesized the emitted SystemVerilog of every arith-metic and format-conversion operator through an open-source flow (Yosys for logic synthesis, OpenSTA for static timing), reporting maximum frequency as fmax =1000 /(critical-path delay in ns) against a virtual clock. Table 2 gives the per-operator combinational result on Nangate45 standard cells (typical corner); the six com-parisons and the saturating integer conversions carry no rounding chain and are not characterized here. The complete flow — the Yosys scripts, the buffered abc 

repair script of Remark 1, the OpenSTA scripts, tool ver-sions (Yosys 0.67, OpenSTA 3.1.0), and the Liberty file’s provenance by SHA-256 — is archived in the repository (tests/fp_v1/synth/nangate45/ ), with a reproduction check: the archived seven-stage fma source re-measures at 268 .0 MHz, and a representative combinational opera-tor regenerated from the current compiler lands within 

3% of its published figure (compiler-side RTL drift, not flow drift). 

Remark 1 (Fanout buffering matters) . Yosys’s default 

abc -liberty mapping performs no fanout buffering or post-mapping gate sizing, so high-fanout nets accu-mulate hundreds of femtofarads and the reported fmax 

is dominated by a handful of overloaded drivers rather than by logic depth. All numbers in this paper there-fore use an explicit repair pass in the abc script ( buffer -N 8; upsize; dnsize ), which eliminates every max-capacitance violation. The effect is not a refinement but a correction: the exact-wide fma measures 45 MHz un-buffered and 160 MHz buffered — a 3.5× artifact. (AIG optimization is dch -f , with dc2 substituted for the two BF16 add/sub netlists on which abc ’s dch aborts; the mapping and repair stages, which carry the timing result, are identical for all operators.) The exact-wide fma is not merely the slowest: it does not pipeline . Its 470 -bit aligned adder is a single com-binational cone, and ABC’s register retiming moves no flops into such a cone at all (we verified this directly: under every retiming configuration we tried, the regis-ter count and positions are unchanged), while a hand-inserted three-stage split of the 470 -bit datapath yielded no improvement either. This is the design problem the next section addresses. (The exact-wide fma is, however, the simplest specification : it computes the exact value and rounds once, and is the object the equivalence proof targets.) 

# 6 The bounded sticky-fold fma 

We reimplement the fma as a bounded guard/round/sticky datapath. Both datapaths share the front end — unpack to sign / biased exponent / 24 -bit significand (subnormals floored at exponent −149 ), form the 48 -bit product, select the higher operand by unbiased exponent. Let ∆ be the exponent gap; for finite operands 

∆ ≤ 421 .

Reference (arch_fma_f32_ref ). Zero-extend both significands to FMA _W = 470 bits, shift the higher one up by ∆, add or take the absolute difference, round once. Since ∆ ≤ 421 < 470 the field is exact; that the result is RNE (exact a · b + c) is itself a theorem, not a construction argument: the aligned magnitude and se-lected sign are proved equal to the decoded |a · b + c| and its sign ( fma_mag_exact , fma_sign_exact ; Section 8). This is the specification (and the 160 -MHz circuit above that our flow could not retime). 

Sticky-fold ( arch_fma_f32 ). Use a 98 -bit field (Fig-ure 2) with FMA _G = 48 guard bits: the high signifi-cand sits at weight 249 and the shifted low significand is doubled to make room for a guard bit, with one sticky bit absorbing everything the shift drops. The guard width FMA _G = 48 is chosen so that any cancellation in a · b + c that could affect rounding keeps the prod-uct/addend least-bit gap at most 47 — it stays inside 

the guard region, so the fold below it loses no rounding information. This is the structural fact the equivalence proof formalizes. 4operator fmax cells operator fmax cells BF16 →FP32 (widen) 7456 25 FP32 sub 318 2288 FP32 →BF16 (narrow) 3997 94 FP32 mul 276 6124 BF16 mul 468 1664 BF16 fma 185 4872 BF16 sub 420 1315 FP32 fma (exact-wide) 160 19152 

BF16 add 406 1323 FP32 fma (sticky fold, comb) 180 10805 FP32 add 320 2263 Table 2: Per-operator combinational fmax (MHz) and mapped cell count, Nangate45 standard cells (typical corner), Yosys + OpenSTA, buffered flow (Remark 1). The widen/narrow conversions are near-wire; every arithmetic operator clears 276 MHz — except the fma s, and the exact-wide FP32 fma is the clear outlier: slowest at 160 MHz and, at 

19 k cells, 3× the area of the multiplier.                  

> exact-wide reference (470 b): alignment exact, nothing dropped
> hi ·2∆(48 b) lo (48 b)
> gap ∆≤421 — every lo bit retained
> 2469
> 20
> add / |−| ,round once
> sticky-fold (98 b): same hi, lo survives only down to the guard; the rest ORs into one sticky bit
> hi at weight 249 (48 b) surviving top of lo, doubled (guard at 21)st
> bits of lo shifted below the guard (dropped)
> OR-fold ⇒
> sticky bit 20

Figure 2: The two fma datapaths, schematic (not to scale). The exact-wide reference keeps a field wide enough (∆ ≤ 421 < 470 ) that alignment loses nothing. The sticky-fold keeps the same high significand at weight 249 and only the top of the shifted low operand, doubled to expose a guard bit; everything shifted below the guard is OR-folded into a single sticky bit — exactly the information RNE needs (§2). Section 7 proves the two produce identical results on all 296 inputs. Being a fixed-width GRS datapath rather than a 470 -bit adder, the sticky-fold pipelines. Table 3 reports an 

fmax -versus-pipeline-depth sweep on the same Nangate45 cells and flow: 180 MHz combinational, rising to a 260 –

268 MHz plateau at five to seven stages, with the best measured point at seven stages ( 268 MHz). Beyond the plateau more registers only add fixed per-stage overhead (clock-to-Q + setup ≈ 0.4 ns each) without splitting the remaining fine-grained cones — eight and ten stages regress. The depth– fmax relation is also non-monotone within the sweep because register placement strategy matters as much as count. Because both tables use the same Nangate45 flow, the numbers are directly comparable, and they separate the two effects cleanly. Combinationally the sticky-fold is a modest win: 180 MHz vs. the exact-wide’s 160 MHz, at 

1.8× less area ( 10 .8k vs. 19 .2k cells). The decisive dif-ference is pipelinability : the sticky-fold reaches 268 MHz at seven stages — 1.68 × the exact-wide’s ceiling, since the exact-wide gained nothing from retiming in our flow. The fma was the only operator in Table 2 whose ceiling demanded a redesign; at seven stages it sits within 3% of FP32 mul (268 vs. 276 MHz) instead of 40% below the field. Replacing a verified datapath with a faster one creates a new obligation: does the fold change the result? The next section answers it. 

# 7 Bit-exact equivalence of the two 

# fma datapaths 

Our central new theorem is a sorry -free Lean 4 proof 

∀ a, b, c ∈ BitVec 32 ,

arch_fma_f32 (a, b, c ) = arch_fma_f32_ref (a, b, c ),

(1) over the entire 296 input space. Composed with the reference’s proved correct rounding (Section 8), it yields the machine-checked end-to-end theorem 

arch_fma_f32_correct : the synthesizable bounded fma 

is correctly rounded against the exact value of its decoded operands — proved without ever re-engaging the multi-plier. 

Why it is tractable. Both datapaths compute the 

same product significand and apply the same special-value wrapper; in the equivalence miter these cancel under common-subexpression elimination, so neither a bit-blaster nor the Lean proof ever solves a multiplier equivalence. The residual content is (a) bit-precise field construction and operand selection, discharged by 

bv_decide over concrete widths — an identity over the shared multiply, hence cheap — and (b) rounding, dis-charged structurally on integers; the variable alignment 5pipeline stages 0 (comb.) 3 4 5 6 7 8 10 

fmax (MHz) 180 223 223 260 260 268 254 242 cells 10805 12466 12424 13224 13196 13935 14127 16236 Table 3: Bounded sticky-fold FP32 fma : fmax and cell count vs. pipeline depth, Nangate45 standard cells (typical corner), Yosys + OpenSTA, buffered flow (Remark 1; best register-placement strategy per depth). The bounded datapath pipelines cleanly to a 260 –268 MHz plateau — unlike the exact-wide reference, which retiming does not improve at all. shift is never bit-blasted. 

Rounding collapse. Reduce each fma to 

roundNE_f32 (s, m, e ) and compare magnitudes: 

Lemma 1 (GRS collapse) . Let m1, m 2 agree above bit 

g ( m1/2g = m2/2g ) and share their low-g sticky status ( m1 mod 2g = 0 ⇐⇒ m2 mod 2g = 0 ). If g lies at or below the rounding position — g + 24 ≤ log 2 m1 for a normal result, or g < −149 − e for a subnormal one — then roundNE_f32 (s, m 1, e ) = roundNE_f32 (s, m 2, e ).

To apply Lemma 1 across datapaths, the proof charac-terizes the sticky-fold and reference magnitudes as closed-form integer expressions (for both same- and opposite-sign alignment), relates them by an exact power of two when ∆ ≤ 48 , and relates the two alignment exponents through a balanced-modulus bridge at the signed-integer level; an exponent-alignment lemma scales the sticky-fold magnitude into the reference’s frame. 

The case lattice. The top theorem is a decision tree: NaN / infinity / zero-factor special cases (each a two-step rewrite chaining a sticky-fold special-value lemma with its reference mirror, both discharged by bv_decide 

on the 98 - and 470 -bit datapaths); zero addend; exact cancellation ( magnitude = 0 ⇒ ± 0); and the finite non-cancelling region, split by sign, by ∆ ≤ 48 vs. ∆ >

48 , and by the higher significand’s magnitude (with the normal / subnormal result branch dispatched internally via Lemma 1). 

The hard case. One configuration resists the generic collapse: an opposite-sign fma whose higher significand is exactly a power of two, with a non-zero lower operand and a normal result. The subtraction borrow drops the leading bit, so the round shift coincides with the natural collapse position and violates the strict precondition of Lemma 1. We resolve it in two pieces: at ∆ = 49 the fold is the identity, so the two magnitudes are equal and the rounding is trivial; for ∆ ≥ 50 a tighter bound on the folded low part lets the collapse be taken one bit lower, satisfying the precondition with equality. This case is a good illustration of why the development must be bit-precise: the rounding decision turns on a single guard bit at a position that moves with the inputs. 

# 8 Trust base, audit, and assurance 

Axioms. #print axioms on the equivalence and end-to-end theorems reports the standard three ( propext ,

Classical.choice , Quot.sound ) plus one _native. bv_decide.ax_* axiom per bv_decide invocation. To be precise about what those axioms mean: the external SAT solver (CaDiCaL) is untrusted — it must emit an LRAT refutation certificate [ 14 ] (a SAT-unsatisfiability proof format whose steps carry explicit propagation hints, checkable in linear time without search — which is what makes a small verified checker practical), validated by ex-actly such a formally verified checker — but the checker is executed by Lean’s compiled native evaluator , and its verdict enters the proof as an axiom. The trusted base for the bv_decide steps is therefore the Lean ker-nel plus the Lean compiler and native-evaluation bridge (the same trust class as native_decide ), not the kernel alone [ 8]. By contrast, the value-level rounder anchor is kernel-only: #print axioms on roundNE_nearest re-ports exactly the standard three (and on the R1 kernel characterization rneQuot_nearest , only propext and 

Quot.sound ) — no native evaluation anywhere in that chain; the native trust enters solely through the bit-level datapath lemmas. There is no sorry , no hand-declared axiom, and no soundness-weakening option. The devel-opment is about 9 000 lines across 23 modules (Lean v4.30.0). 

Artifact. Every machine-checked claim in this pa-per — the Lean theorems, the Tier-1 SMT proofs, the renderer miters, and the archived synthesis flow — is checked against one release of the ARCH repository: 

v0.71.0 (commit 1a7d9fd ), https://github.com/ arch-hdl-lang/arch-com/releases/tag/v0.71.0 .

Independent audit. An adversarial audit, run sepa-rately, rebuilt the project, re-derived the axiom set, and — the strongest check against a hollow proof — regenerated 

the Lean model from the operator source and confirmed it byte-identical to the committed model. So arch_fma_f32 

and arch_fma_f32_ref are the genuine emitted artifacts, structurally independent ( 98 -bit fold vs. 470 -bit exact), hence not trivially equal; the audit further confirmed the top-level case split exhaustive and non-vacuous, ev-ery bv_decide operating on the real datapaths, and the rounder a genuine RNE encoder. 6Where the IEEE anchor bottoms out. No ver-ification proves a specification correct absolutely; it bottoms out in a trusted definition. For fma that definition is RNE (exact a · b + c) — and the chain from it to the emitted datapath is now proved end to end , with no by-construction step. A value-level Lean theorem ( arch_fma_f32_correct , module 

ArchFpEquiv.FmaValue ) states that for finite nonzero operands whose exact result is nonzero and in range, the 

fma returns the finite FP32 pattern nearest in magni-tude to the exact real value a · b + c among all finite pat-terns, with ties to an even mantissa ( roundNE_tie_even ), the exact sign, and overflow to signed infinity occurring exactly when the correctly rounded magnitude reaches 

2128 , the IEEE criterion (arch_fma_f32_overflow ,

biasedFinal_overflow_iff ). Both halves of the speci-fication are theorems rather than construction arguments: “exact” is the magnitude identity fma_mag_exact — the reference’s aligned 470 -bit magnitude at its alignment ex-ponent equals |a · b + c| of the decoded operands, with the datapath’s sign selection equal to the exact value’s sign (fma_sign_exact ) — and “RNE” is the rounder theorem 

roundNE_nearest . All quantities in play are dyadic ra-tionals, so every comparison is carried out exactly over N

(the value a·b+c lives on the fixed grid 2−298 ); no reals and no rational arithmetic are needed, and the proofs stay in core Lean. The trusted base thus shrinks to three defining lines a reviewer checks by reading: the signed magnitude an FP32 bit pattern denotes (in units of 2−149 ), the exact value fmaExact = sAsB + sC · 2149 (in units of 2−298 ), and the nearest-among-representables predicate. The datapath additionally remains cross-checked by the SMT 

FloatingPoint theory and the SoftFloat differential. 

# 9 Related work 

Mechanized floating-point verification has a long lineage: Harrison verified IEEE division and transcendental func-tions for Intel in HOL Light [5]; Russinoff verified arith-metic units at AMD in ACL2 [ 6]; the Flocq [ 3] Coq library formalizes IEEE-754 and underpins verified com-pilers and the FloVer [ 4] round-off analyzer. These efforts prove hardware or kernels correctly rounded against a real/rational specification — the methodology our Tier-2 multiplier proofs follow, through to the end-to-end value-level fma theorem. On the automated side, the SMT-LIB FloatingPoint theory [ 7] and solvers such as z3, cvc5, and Bitwuzla decide bit-vector floating-point for-mulas, and Lean’s bv_decide [ 8] brings LRAT-certified bit-blasting into Lean, validating solver certificates with a verified checker run under compiled native evaluation; all hit the multiplier wall on fma , which our two-tier split (exhaustive SMT below the wall, structured Lean above) and our multiplier-cancelling equivalence proof are organized to route around. Berkeley SoftFloat [ 15 ] is the standard differential oracle. We are not aware of prior work that delivers FP32/BF16 as a verified, synthesiz-able HDL feature from a single source, nor of the specific device of pairing a provably-wide exact fma reference with a bounded GRS datapath and joining them by an equivalence in which the multiplier cancels. 

# 10 Limitations 

The proofs establish equivalence and correct rounding of the Lean-rendered models; transfer to the emitted Sys-temVerilog rests on the SV and Lean renderers denoting the same IR semantics. The shared linearizer reduces this to a per-operator correspondence rather than a whole-function obligation, and for the SystemVerilog renderer that correspondence is now machine-checked: Yosys — an independent implementation of SystemVerilog seman-tics — reads the emitted SV and exports it to SMT, and a solver proves the export equivalent to the SMT renderer’s model, unsat for all 24 exposed operators — including the saturating integer conversions — (the two 

fma miters via a sound 510 -way case-split on the align-ment gap, under which both barrel shifters collapse; the split covers all inputs by construction). Three mechan-ical transformations sit inside this path and are part of its trust base, disclosed in the artifact: a syntactic declaration-hoisting preprocessor on the SV (Yosys can-not parse declaration-with-initializer inside functions), Yosys’s opt_clean dead-wire removal after flattening, and a specialization of the SMT export that eliminates Yosys’s state sort by instantiating it at the single state constant. Modulo those, a divergence would have to be a bug shared with Yosys’s independent frontend. The Lean renderer remains covered by the byte-identical regenera-tion audit. Relatedly, the IR and both formal models are two-state: SystemVerilog X/Z behavior is out of scope, the verified statements quantify over binary inputs, and driving every input is the environment’s obligation. The IEEE-754 anchor is proved end to end against a value-level dyadic specification of the exact a · b + c (Section 8); the residual trust there is the three-line spec itself — the signed magnitude a bit pattern denotes, the exact-value expression, and the nearest-among-representables predi-cate — checked by reading. The verified contract is value semantics under static rounding (Section 4): dynamic rounding modes, IEEE status flags, and signaling com-parisons are not implemented, and NaN payloads follow an explicit canonical profile. The synthesis numbers are preliminary characterizations from a single flow on a sin-gle open cell library and corner (Nangate45, typical); a multi-corner, multi-library study is future work. Finally, the BF16 fma is, by design, fused f32-accumulate and not correctly rounded, as Section 4 states. 711 Conclusion 

We added first-class FP32 and BF16 to a hardware de-scription language and verified every operator against its stated contract from a single source — end to end, with the SystemVerilog renderer correspondence machine-checked and the Lean renderer audited (Section 10); for the BF16 fma , that contract is the FP32-accumulating fusion of Section 4.3. The methodology splits at the solver-tractability frontier — exhaustive SMT below the multiplier wall, structured Lean above — and the head-line result turns a timing problem into a verification one and dispatches it cleanly: a correctly rounded fma can be made fast and pipelineable by replacing its exact-wide datapath with a bounded sticky fold, and the replacement can be proved bit-identical to the exact reference over all 

296 inputs without ever solving a multiplier equivalence ,because the shared multiplier cancels. We see the two-reference pattern — an inspectable wide specification plus a synthesizable bounded implementation, joined by a multiplier-cancelling equivalence proof — as broadly applicable to multiplier-bearing arithmetic that defeats direct bit-blasting. 

Availability. The compiler, the operator IR and its three renderers, and the Lean development are intended for open release; the proofs are reproducible with Lean 4 (v4.30.0) and a standard bv_decide toolchain. 

# References 

[1] IEEE, IEEE Standard for Floating-Point Arithmetic ,IEEE Std 754-2019 (and 754-2008). [2] D. Kalamkar, D. Mudigere, N. Mellempudi, et al., “A Study of BFLOAT16 for Deep Learning Training,” arXiv:1905.12322, 2019. [3] S. Boldo and G. Melquiond, “Flocq: A Unified Library for Proving Floating-Point Algorithms in Coq,” 20th IEEE Symposium on Computer Arith-metic (ARITH) , 2011. [4] H. Becker, E. Darulova, M. O. Myreen, and Z. Tat-lock, “FloVer: A Certificate Checker for Finite-Precision Round-off Error,” Tools and Algorithms for the Construction and Analysis of Systems (TACAS) ,2018. [5] J. Harrison, “Formal Verification of Floating Point Trigonometric Functions,” Formal Methods in Computer-Aided Design (FMCAD) , 2000. [6] D. M. Russinoff, Formal Verification of Floating-Point Hardware Design: A Mathematical Approach ,Springer, 2018. [7] M. Brain, C. Tinelli, P. Rümmer, and T. Wahl, “An Automatable Formal Semantics for IEEE-754 Floating-Point Arithmetic,” 22nd IEEE Symposium on Computer Arithmetic (ARITH) , 2015. (SMT-LIB 

FloatingPoint theory.) [8] Lean bv_decide : an LRAT-certified bit-vector deci-sion procedure for Lean 4 (Lean FRO); certificates are validated by a formally verified checker executed via Lean’s native evaluator. [9] A. Biere, “Collection of Combinational Arithmetic Miters Submitted to the SAT Competition 2016,” 

Proc. SAT Competition 2016 — Solver and Bench-mark Descriptions , University of Helsinki, Dept. of Computer Science Series B-2016-1, pp. 65–66, 2016. [10] P. Beame and V. Liew, “Towards Verifying Nonlinear Integer Arithmetic,” Computer Aided Verification (CAV) , 2017. [11] D. Kaufmann, A. Biere, and M. Kauers, “Verifying Large Multipliers by Combining SAT and Computer Algebra,” Formal Methods in Computer-Aided De-sign (FMCAD) , pp. 28–36, 2019. [12] NVIDIA Corporation, NVIDIA A100 Tensor Core GPU Architecture (Ampere architecture whitepa-per), v1.0, 2020. https://www.nvidia.com/ content/dam/en-zz/Solutions/Data-Center/ nvidia-ampere-architecture-whitepaper.pdf 

[13] Google Cloud, “Improve Your Model’s Performance with bfloat16,” Cloud TPU documentation. https: //cloud.google.com/tpu/docs/bfloat16 

[14] L. Cruz-Filipe, M. J. H. Heule, W. A. Hunt, M. Kauf-mann, and P. Schneider-Kamp, “Efficient Certified RAT Verification,” Automated Deduction — CADE 26 , LNCS 10395, pp. 220–236, 2017. [15] J. Hauser, Berkeley SoftFloat , release 3e. http:// www.jhauser.us/arithmetic/SoftFloat.html 

8