---
title: Bit-Precise CHC Satisfiability Using Theory-Modular Reasoning
title_zh: 位精确CHC可满足性：基于理论模块化推理
authors: "Omer Rappoport, Orna Grumberg, Yakir Vizel"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05337v1"
tags: ["query:fsh"]
score: 8.0
evidence: 理论模块化CHC求解用于位精确验证
tldr: 位精确程序验证中，判定CHC在位向量理论下的可满足性至关重要，但现有求解器难以有效处理，限制可扩展性。为此提出Mosaic框架，将CHC集划分为位向量和整数算术两个片段，通过跨理论可靠翻译交换信息，实现模块化推理。基于Z3和Spacer的原型评估显示，Mosaic在位操作基准上显著优于Spacer，展现了提升位精确推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05337v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1221, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1394, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1493, \"height\": 1257, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1268, \"height\": 818, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1307, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1358, \"height\": 870, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1529, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1465, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1477, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1295, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05337v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1446, \"height\": 929, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05337v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 722, \"height\": 459, \"label\": \"Table\"}]"
motivation: 现有CHC求解器对位向量理论支持不足，位精确程序验证的可扩展性受限。
method: Mosaic将CHC集划分为位向量和整数算术片段，用理论间可靠翻译实现模块化推理。
result: 在Z3和Spacer上实现原型，位操作基准上显著优于Spacer。
conclusion: 为位精确CHC求解提供可扩展的理论模块化方案。
---

## 摘要
判定约束Horn子句（CHC）在固定大小位向量理论（$\mathcal{T}_B$）下的可满足性，是位精确程序验证的基础。然而，最先进的CHC求解器在处理$\mathcal{T}_B$时常常遇到困难，限制了位精确推理的可扩展性。我们提出了Mosaic，一个理论模块化框架，通过结合$\mathcal{T}_B$和整数算术理论（$\mathcal{T}_I$）的推理，判定CHC在$\mathcal{T}_B$下的可满足性。给定一组关于$\mathcal{T}_B$的CHC，Mosaic将其划分为两个分别基于$\mathcal{T}_B$和$\mathcal{T}_I$解释的片段。此外，它实现了一种以模块化方式推理这些片段的算法，通过跨理论的可靠翻译在片段之间交换信息，并确定相对于原始CHC集合的可满足性。我们使用Z3和Spacer实现了Mosaic的原型，并在位操作基准测试上进行了评估。评估结果表明，Mosaic在这些基准测试上显著优于Spacer。

## Abstract
Deciding satisfiability of Constrained Horn Clauses (CHCs) modulo the theory of fixed-size bit-vectors ($\mathcal{T}_B$) is fundamental to bit-precise program verification. However, state-of-the-art CHC-solvers often struggle with $\mathcal{T}_B$, limiting scalability in bit-precise reasoning. We present Mosaic, a theory-modular framework for deciding satisfiability of CHCs modulo $\mathcal{T}_B$ by combining reasoning in $\mathcal{T}_B$ and the theory of Integer Arithmetic ($\mathcal{T}_I$). Given a CHC set modulo $\mathcal{T}_B$, Mosaic partitions it into two fragments interpreted over $\mathcal{T}_B$ and $\mathcal{T}_I$. Moreover, it implements an algorithm that reasons about the fragments in a modular fashion, exchanges information between them via sound translations across theories, and determines satisfiability w.r.t. the original CHC set. We implemented a prototype of Mosaic using Z3 and Spacer and evaluated it on bit-manipulating benchmarks. Our evaluation shows that Mosaic significantly outperforms Spacer on these benchmarks.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- 位精确程序验证（bit-precise program verification）需要判定约束 Horn 子句（Constrained Horn Clauses, CHC）在固定大小位向量理论（$\mathcal{T}_B$）下的可满足性。这是程序安全验证中的基础问题，因为程序语义需要精确反映底层数据的位级表示。
- 现有 CHC 求解器（如 Spacer）在整数算术理论（$\mathcal{T}_I$）上表现良好，但在 $\mathcal{T}_B$ 上效果不佳，导致位精确推理的可扩展性受限。
- 另一种做法是用 $\mathcal{T}_I$ 对位精确运算进行编码（例如通过乘除和模运算），但这种方式会产生复杂约束，尤其遇到位操作时计算开销极大。因此存在一种根本性权衡：$\mathcal{T}_B$ 形式削弱求解器的泛化能力，而位精确 $\mathcal{T}_I$ 编码生成高计算复杂度约束。
- 论文提出 **Mosaic** 框架，旨在同时利用 $\mathcal{T}_B$ 和 $\mathcal{T}_I$ 的互补优势，通过“理论模块化”推理高效判定 CHC 在 $\mathcal{T}_B$ 下的可满足性，从而解决上述权衡问题。

## 2. 方法论

### 2.1 核心思想

- 将输入 CHC 集合 $\Pi(\mathcal{T}_B)$ 划分为两个片段：
  - **位向量片段（$\Pi_B$）**：保留在 $\mathcal{T}_B$ 下解释，主要处理位级操作（如 XOR、移位等）。
  - **整数片段（$\Pi_I$）**：翻译为 $\mathcal{T}_I$ 下的 CHC，主要处理纯算术约束。
- 两个片段通过**接口谓词副本**和**接口约束** $\Theta$ 保持语义一致性。
- 核心优势：让算术部分在整数领域求解（求解器擅长），位级部分留在位向量领域，避免将位操作翻译成复杂整数算术的代价。

### 2.2 关键技术细节

- **领域转换器（Domain Transformer）**：为每个位向量变量定义无符号或有符号映射到整数常量，保证语义对应关系（Definition 5）。
- **理论转换器（Theory Transformer）**：
  - $\tau_{B⇝I}$：将 $\mathcal{T}_B$ 公式翻译为 $\mathcal{T}_I$ 公式，并满足语义保持（Eq. 2）。
  - $\tau_{I⇝B}$：反向翻译，但需要处理溢出问题（如通过符号扩展避免算术环绕）。
- **理论模块化编码**（Definition 8）：
  - 对每个共享谓词 $p$ 创建两个副本 $p_B$ 和 $p_I$，分别用于两个片段。
  - 生成接口约束 $\Theta = \Theta_{B⇝I} \cup \Theta_{I⇝B}$，用于连接两个片段的谓词副本。
  - 在整数片段中引入领域限制（domain restriction）以排除位向量域之外的整数赋值。
- **推理算法 MosaicSolve**：
  - 定义为一个转换系统（图 2），维护证明义务集合 $Q$、下近似引理集 $\Delta_u$ 和上近似引理集 $\Delta_o$。
  - 通过规则 `Init`、`NegAnswer`、`PosAnswer`、`Query`、`Sat`、`Unsat` 协调两个 CHC 求解器（如 Spacer）之间的信息交换：
    - `NegAnswer` 用上近似证明某个证明义务不可能被满足；
    - `PosAnswer` 用下近似确认某个反例确实可达；
    - `Query` 在近似不足时生成新证明义务传递到另一片段。
- 正确性由定理保证：编码保持可满足性（Theorem 1），解的存在性一致（Theorem 3），算法是可靠（Theorem 4）且可终止的（需额外细化条件）。

## 3. 实验设计

### 3.1 基准与场景

- 使用 11 个小型真实位操作程序，编码为 $\mathcal{T}_B$ 上的 CHC 集。这些程序来自常见位操作技巧（如斯坦福 bit hacks），包括 `abs-ge`、`abs-sum`、`cond-neg`、`max-inv`、`opp-signs`、`swap`、`turn-off-rm` 等。
- 每个基准程序模拟典型程序验证场景：假设输入条件、循环、算术运算和位级断言。

### 3.2 对比方法

- **BV 配置**：所有 CHC 放在位向量片段，等价于直接在 $\mathcal{T}_B$ 上运行 Spacer。
- **IA 配置**：所有 CHC 翻译到整数片段，等价于在 $\mathcal{T}_I$ 上运行 Spacer（使用 Zohar et al. [19] 的翻译）。
- **BV+IA 配置**：Mosaic 的理论模块化方法。

### 3.3 实验设置

- 工作站：AMD EPYC 74F3（CPU），未使用 GPU。
- 每个测试超时：7200 秒（2 小时），内存限制：16 GB。
- 衡量指标：在超时内求解成功的**最大位宽**（W）及该位宽下的**运行时间**（Time）。

## 4. 资源与算力

- 论文**未提及 GPU**（如型号、数量），也未涉及训练时长（本方法无训练阶段）。
- 明确指出使用配备 AMD EPYC 74F3 处理器的**工作站**进行实验，每个基准允许 7200 秒和 16 GB 内存。
- 由于是符号推理而非机器学习，算力需求主要体现为单机 CPU 时间。

## 5. 实验数量与充分性

- 共 11 组基准程序，每个程序测试三种配置（BV、IA、BV+IA），并报告最大位宽和对应运行时间，实验数据记录在 Table 1 中。
- **优点**：
  - 三种配置的对比可视为消融实验：BV 和 IA 分别对应“纯位向量”和“纯整数”的极端方案，BV+IA 体现理论模块化的增益。
  - 基准选择具有代表性，覆盖了位操作（XOR、与/或/移位）与算术的混合情况，能突出方法的优势。
  - 结果差异显著（如 `opp-signs` 从 BV 的 9 位/IA 的 3 位提升到 BV+IA 的 63 位），证明方法有效。
- **不足**：
  - 基准数量较少（11 个），且均是小型程序，缺乏大规模/复杂工业程序验证。
  - 未与其他专门的位精确 CHC 求解器或抽象-精化工具（如 Ultimate Automizer）对比，只与 Spacer 比较。
  - 未系统探索不同划分策略（如按谓词而非按约束划分）对性能的影响。

## 6. 主要结论与发现

- Mosaic（BV+IA 配置）在大多数基准上取得了**最大可求解位宽**，且运行时常比 BV/IA 快数个数量级。
- 纯整数翻译（IA）因位操作转为复杂算术项通常效果最差；纯位向量（BV）受限于泛化能力，无法扩展到较大位宽。
- 理论模块化方法成功结合两种理论的优势：算术部分在整数推理中受益于求解器的成熟技术，位级部分避免了翻译开销。
- 作者总结：Mosaic 可显著提升位精确 CHC 求解的可扩展性，为位精确程序验证提供了新的有效途径。

## 7. 优点

- **方法创新性**：首次在 CHC-SAT 中提出基于多种背景理论联合推理的框架，打破了单一理论的限制。
- **理论完备**：严格定义了领域转换器、理论转换器和理论模块化编码，并给出可满足性、解对应性和算法可靠性的证明，为后续工作奠定基础。
- **实现简洁**：基于现有 Z3/Spacer 构建，易于复现和集成。
- **实验清晰**：三配置对比直接展示了模块化带来的增益，数据完整，可复现（代码和基准公开，DOI 提供）。
- **实用意义**：能大幅增大可验证的位向量宽度，满足实际程序验证的常见需求。

## 8. 不足与局限

- **实验规模有限**：仅 11 个小型程序，未覆盖更复杂的数据结构、递归程序或多循环场景，普遍性有待验证。
- **对比范围较窄**：仅与 Spacer 的两种配置比较，未与最新位精确求解器（如基于抽象-精化的工具）对比，可能低估或高估领先程度。
- **划分策略启发式**：文中划分仅按约束中是否含位操作进行，未提供自适应或学习式的划分机制，对某些程序可能不是最优。
- **位宽上限限制**：Z3 在 64 位以上出现内部错误，导致无法评估更大位宽的表现。
- **对求解器底层性能依赖强**：该方法假设底层 CHC 求解器（尤其整数算术求解器）具有较好鲁棒性，但实际求解器在特定算术形式上仍可能退化。
- **实现中省略了部分终止性细节和优化**（如迭代深化、引理去重），可能影响实际稳定性和性能上限。

（完）
