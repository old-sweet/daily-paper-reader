---
title: Separation Logic for Memory Conflict Detection in High-Level Synthesis
title_zh: 用于高层次综合中内存冲突检测的分隔逻辑
authors: Yeonseok Lee
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07126v1"
tags: ["query:fsh"]
score: 8.0
evidence: 在高层次综合验证中使用分离逻辑检测内存冲突
tldr: 高层次综合中，非仿射索引导致内存冲突检测困难，传统多面体方法过度近似造成保守串行化性能损失。本文提出基于分离逻辑的空间验证框架，在LLVM IR层提取算术表达式，将内存库建模为多态空间谓词，通过分离合取条件强制无冲突展开。冲突检测转化为SMT可满足性检查，对不可判定情况采用顺序后备。可靠性定理关联验证与RTL安全，确保硬件无结构冲突。该方法显著减少保守串行化，提升综合性能，同时保证硬件安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07126v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 1178, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07126v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1357, \"height\": 974, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07126v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 925, \"height\": 116, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07126v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1184, \"height\": 225, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07126v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1748, \"height\": 1064, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07126v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1026, \"height\": 144, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07126v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1022, \"height\": 144, \"label\": \"Table\"}]"
motivation: 传统多面体框架对非仿射索引过度近似，导致保守串行化，严重降低高层次综合性能。
method: 在LLVM IR层提取getelementptr表达式，用分离逻辑的空间谓词建模内存库，通过分离合取条件检查并发访问冲突，并转化为SMT不等式。
result: 冲突检测转化为SMT可满足性检查，对不可判定非线性有顺序后备，确保硬件无结构内存冲突。
conclusion: 本框架有效减少保守串行化，提升综合性能，并通过可靠性定理保证综合后硬件免受结构内存冲突。
---

## 摘要
高层次综合利用循环展开和数组分割，但当索引包含非仿射算术时，调度并发访问具有挑战性。传统的多面体框架系统地过度近似这些非线性变换，迫使保守的串行化，从而降低性能。为最小化这一瓶颈，我们提出一个在LLVM中间表示（IR）级别运行的空间验证框架。通过从“getelementptr”指令中提取扁平算术表达式，它将内存库建模为多态空间谓词以处理非仿射项。通过使用分隔逻辑的分离合取，强制执行冲突无关展开条件来确保结构安全性；针对同一内存库的并发操作会触发自动空间矛盾。这种不相交性要求被简化为一个关于不可变静态单赋值（SSA）变量的成对不等式矩阵，用于可满足性模理论（SMT）求解器。为了确保针对不可判定非线性算术的安全性，我们实现了一个确定性顺序回退。最后，一个可靠性的定理将代数SMT验证与寄存器传输级跟踪安全性连接起来，确保物理硬件免受结构性内存冲突的影响。

## Abstract
High-Level Synthesis leverages loop unrolling and array partitioning, but scheduling concurrent accesses is challenging when indices contain non-affine arithmetic. Conventional polyhedral frameworks systematically over-approximate these non-linear transformations, forcing conservative serialization that degrades performance. To minimize this bottleneck, we present a spatial verification framework operating at the LLVM Intermediate Representation (IR) level. By extracting flat arithmetic expressions from "getelementptr" instructions, it models memory banks as polymorphic spatial predicates to handle non-affine terms. Structural safety is enforced via a Conflict-Free Unrolling condition using Separation Logic's separating conjunction; concurrent operations targeting the same bank trigger an automatic spatial contradiction. This disjointness requirement is reduced to a matrix of pairwise inequalities over immutable Static Single Assignment (SSA) variables for a Satisfiability Modulo Theories (SMT) oracle. To guarantee safety against undecidable non-linear arithmetic, we implement a deterministic sequential fallback. Finally, a theorem of soundness bridges algebraic SMT verification with Register Transfer Level trace safety, ensuring physical hardware immune to structural memory collisions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：高层次综合（HLS）通过循环展开和数组分区利用并行性，但非仿射索引（如乘法、除法、取模等非线性算术）导致传统多面体模型**系统性地过度近似**，被迫采用保守串行化，严重降低硬件性能。
- **意义**：本文提出一个在**LLVM中间表示（IR）级别**运行的**空间验证框架**，利用分离逻辑（Separation Logic）的分离合取运算符原生建模内存冲突，绕过多面体模型的仿射限制，以**静态编译时验证**替代保守串行化，提高并行效率。

### 2. 论文提出的方法论
- **核心思想**：将物理内存库建模为**多态空间谓词** `bank(k, A, N)`，通过分离合取 `*` 强调堆的不相交性。当两个并发操作映射到同一银行时，`bank(k) * bank(k) ⇒ ⊥` 自动触发逻辑矛盾，检测冲突。
- **关键技术细节**：
  - 在LLVM IR层拦截 `getelementptr` 指令提取扁平地址算术表达式。
  - 定义**静态硬件环境 Γ**，记录数组基地址、大小、分区策略（循环、块、完全）。
  - 定义**银行映射函数 B(expr, N)**：循环分区用 `expr mod N`，块分区用 `⌊expr/B⌋`，完全分区用 `expr`。
  - 形式化**无冲突展开条件** `cfu(E, A, N) ≜ ∗^U_{i=1} bank(B(expr_i, N), A, N)`。
  - 通过翻译函数 `T` 将空间谓词化为纯一阶逻辑：`π ⇒ ∧_{i<j} (k_i ≠ k_j)`，提交给SMT求解器（如Z3）。
  - 若SMT返回 `Unknown` 或超时，采用**顺序后备**：`M(S) = {Parallel if Sat, Sequential otherwise}`，保证硬件安全性。
- **公式/算法流程**：LLVM基本块复制 → 提取GEP表达式 → 构建CFU条件 → 翻译为不等式矩阵 → SMT求解 → 若饱和则并行调度，否则串行化。

### 3. 实验设计
- **无实验**：本文为**纯理论论文**，未提供任何实验数据集、benchmark或方法对比。仅在最后“未来工作”中提及计划实现LLVM分析pass并集成Z3进行经验评估。
- **Benchmark/方法对比**：未涉及。

### 4. 资源与算力
- **未提及**：文中没有描述任何GPU型号、数量、训练时长等算力消耗。考虑到论文尚处于理论阶段，未见实际实现。

### 5. 实验数量与充分性
- **无实验**：因此无法评价实验充分性、客观性或公平性。

### 6. 论文的主要结论与发现
- **结论**：基于分离逻辑的空间验证框架能够**静态地、精确地检测非仿射索引下的内存冲突**，将问题简化为SMT可满足性检查，避免多面体模型的过度近似。
- **发现**：分离合取的固有性质（`P*P⇒⊥`）可自动捕获结构冒险；通过可靠性定理（Theorem 1）证明：SMT验证通过 ⇒ 空间堆不相交 ⇒ RTL无结构冲突。
- **意义**：该理论为HLS编译器提供了一种**可替代多面体方法的路径**，特别适用于指针运算、动态索引等场景。

### 7. 优点
- **创新性**：将分离逻辑从软件验证引入HLS硬件冲突检测，并首次在LLVM IR层级工作，而非C抽象语法树。
- **处理非仿射**：通过将索引视为符号表达式并延迟到SMT求解，无需线性限制。
- **自动冲突检测**：利用分离合取的矛盾性质，无需手动编写依赖分析规则。
- **安全性保障**：对不可判定非线性算术有确定性顺序后备，保证绝对安全，不欠近似。
- **形式化证明**：给出了完整语法、语义和可靠性定理，理论坚实。

### 8. 不足与局限
- **缺乏实验验证**：论文尚停留在理论推导阶段，没有实际实现或基准测试，无法评估性能开销、正确率或硬件资源利用率。
- **保守后假定可能造成假阳性**：对 `Unknown` 或超时直接回退到串行化，可能误伤可安全并行的非线性场景。
- **未考虑多端口内存**：当前模型假设单端口SRAM，未来需扩展到多端口场景以支持并发读。
- **未区分读写权限**：独占所有访问（包括读）导致读–读共享被误判为冲突，需借助分数权限优化。
- **依赖SMT求解效率**：非线性算术的SMT求解可能很慢，实际工程中需考虑超时阈值。

（完）
