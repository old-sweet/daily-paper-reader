---
title: Towards Automated Formal Verification of zkEVMs Using LLM-Guided Constraint Synthesis
title_zh: 迈向使用LLM引导的约束综合实现zkEVM的自动化形式化验证
authors: "Shichen Huang, Zhenghe Jiang, Yi Jiang, Ling-I Wu, Jingyang Li, Guoqiang Li"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19795v1"
tags: ["query:llm-hw-fv"]
score: 9.0
evidence: LLM引导的约束综合用于zkEVM形式验证
tldr: "zkEVM实现错误可能让有效零知识证明认证错误状态，形式化验证可预防但手动生成规约成本高。提出VeriSynth框架，让LLM作为形式化前端将Rust代码翻译为符号约束，SMT求解器判定正确性，集成语义分解、检索增强提示和验证引导自动修复。在首个源码级zkEVM基准上，bug检测率超90%，远优于直接LLM、对话LLM和手动变异测试基线。该工作提供自动化形式化验证方案，减少手动工作，提升zkEVM安全性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1864, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 912, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 636, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 741, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 808, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 947, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 621, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 926, \"height\": 227, \"label\": \"Table\"}]"
motivation: 现有zkEVM开发缺乏从Rust代码自动生成验证模型的自动化方法，手动规格难以持续，LLM直接生成缺乏形式保证。
method: VeriSynth混合LLM和SMT求解器，通过语义分解、检索增强提示和验证引导自动修复，从Rust代码合成可执行Python/Z3验证模型。
result: "在首个源码级zkEVM验证基准上，VeriSynth bug检测率超90%，显著优于直接LLM、对话LLM和手动变异测试基线。"
conclusion: VeriSynth提供高效的zkEVM自动化形式化验证，各组件对整体效果均关键。
---

## 摘要
零知识以太坊虚拟机（zkEVM）通过生成零知识证明来保证链下执行正确性，从而保障以太坊卷叠的安全性。然而，细微的实现错误（例如不正确的Gas记账）可能导致有效证明认证了语义错误的状态，从而悄然破坏密码学保证。通过SMT求解器进行形式化验证可以防止此类问题，但受到了规约的瓶颈：当前zkEVM开发实践缺乏将Rust操作码处理程序自动转换为验证模型的方法。现有实践依赖于不可持续的人工规约，而基于LLM的方法则存在幻觉问题且缺乏形式化保证。为解决此问题，我们提出VeriSynth框架，该框架从Rust zkEVM代码合成可执行的Python/Z3验证模型。VeriSynth采用混合范式：LLM严格作为形式化前端，将代码翻译为符号约束，而SMT求解器则作为正确性仲裁者。为处理复杂的多组件状态转换，VeriSynth将语义分解、检索增强提示和验证引导的自动修复集成到闭环流水线中。我们在首个源代码级zkEVM验证基准上评估了VeriSynth，该基准包含正确和错误操作码实现。VeriSynth实现了超过90%的错误检测率，显著优于直接和对话式LLM基线，以及一个生产级的手写变异测试套件。消融实验证实，每个流水线组件对框架的整体有效性都至关重要。

## Abstract
Zero-Knowledge Ethereum Virtual Machines (zkEVMs) secure Ethereum rollups by generating zero-knowledge proofs that guarantee off-chain execution correctness. However, subtle implementation bugs (e.g., incorrect gas accounting) can lead to valid proofs certifying semantically faulty states, thereby silently defeating cryptographic guarantees. Formal verification via SMT solvers can prevent this, but is bottlenecked by specification: current zkEVM development practice lacks automated methods to translate Rust opcode handlers into verification models. Current practices rely on unsustainable manual specifications, while LLM-based approaches suffer from hallucination and lack formal guarantees. To address this, we propose VeriSynth, a framework that synthesizes executable Python/Z3 verification models from Rust zkEVM code. VeriSynth enforces a hybrid paradigm: an LLM acts strictly as a formalization frontend to translate code into symbolic constraints, while an SMT solver serves as the correctness arbiter. To handle complex multi-component state transitions, VeriSynth integrates semantic decomposition, retrieval-grounded prompting, and verification-guided auto-repair into a closed-loop pipeline. We evaluate VeriSynth on the first source-level zkEVM verification benchmark, encompassing both correct and faulty opcode implementations. VeriSynth achieves a bug detection rate of over 90%, substantially outperforming direct and conversational LLM baselines, as well as a production-grade handwritten mutation-testing suite. Ablation studies confirm that each pipeline component is critical to the framework's overall effectiveness.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 核心问题与整体含义

- **研究动机**：零知识以太坊虚拟机（zkEVM）是保障以太坊卷叠安全性的关键组件，它通过生成零知识证明来验证链下交易执行的正确性。然而，zkEVM 实现中的细微 bug（如错误的 Gas 记账、缺失状态更新、越界检查遗漏等）可能导致有效零知识证明认证了语义错误的状态，从而悄然破坏密码学保证。形式化验证（如 SMT 求解器）能预防此类问题，但当前 zkEVM 开发缺乏从 Rust 操作码代码自动生成验证模型的自动化方法，手动编写规约成本高且难以持续，而直接使用 LLM 生成规则面临幻觉、缺乏形式保证等问题。
- **整体含义**：提出一种自动化框架，将 LLM 作为形式化前端、SMT 求解器作为正确性仲裁者，从 Rust zkEVM 源码合成可执行的 Python/Z3 验证模型，填补从源码到形式验证的自动化鸿沟，提升 zkEVM 的安全性。

## 2. 方法论

- **核心思想**：LLM 提出候选约束，SMT 求解器判定正确性。框架集成三个关键技术：
    - **语义分解**：将原始 zkEVM 源码分解为语义上连贯的单元（例如 SSTORE 操作码涉及读取、Gas 计算、存储更新等），每个单元对应一个可独立建模的转换，并明确读写状态组件。
    - **检索增强提示**：从已验证的参考数据库（包含先前通过的单元及其模型）中检索与当前单元语义最相似（基于代码嵌入和操作码家族匹配）的 top-k 示例作为锚点，稳定符号变量、状态模式和转换结构。
    - **验证引导的自动修复**：生成候选模型后，经过语法检查、排序检查、执行检查、语义检查四个阶段；若失败，根据错误类型（解析错误、排序错误、执行错误、语义错误）触发相应修复动作（补充声明、对齐位宽、修正 API、细化守卫约束等），最多 3 次迭代修复。
- **符号状态建模**：EVM 执行状态表示为预状态和后状态，包含栈、内存、存储、Gas、退款、上下文、状态标志；位宽用固定宽度位向量，存储/内存用符号数组；分支用 Z3 If 表达式，赋值用后状态等式，未修改组件需显式帧条件。
- **验证查询**：对每个语义单元 u，构建 `C_impl_u(σ, σ′) ∧ ¬Φ_u(σ, σ′)`，其中 C_impl_u 编码实现行为，Φ_u 是语义义务（如算术等价、栈高度保持、Gas 非负等）。若可满足则存在反例，表示实现违反义务。

## 3. 实验设计

- **数据集/基准**：自建首个源码级 zkEVM 验证基准，包含 95 个负样本（注入单个 bug）和少量正样本，覆盖 5 个操作码语义家族：
    - 算术/位运算（22 个）
    - 内存/存储（26 个）
    - Gas/退款（15 个）
    - 调用/创建路径（21 个）
    - 环境/上下文依赖（11 个）
    Bug 类型包括错误算术规则、边界处理、缺失状态更新、返回值传播、Gas/退款逻辑错误、执行路径错误等。
- **对比方法**：
    - **LLM-only**：直接让 LLM 生成验证脚本或识别 bug，无分解、检索、修复。
    - **对话式 LLM**：允许 LLM 利用编译器/求解器反馈修订输出，但不使用检索锚点或结构修复。
    - **手写 Rust 测试**：Scroll 项目的生产级变异测试流程，每个注入 bug 打补丁后运行对应 cargo test。
- **评估指标**：检测率（DR，负样本中被正确识别为不一致的比例）、可执行率（ER，生成模型可编译执行的比例）、Token 成本。

## 4. 资源与算力

- 论文中未明确说明使用的 GPU 型号、数量、训练时长等硬件资源。
- 仅提及使用 GPT-4o 作为后端 LLM，并报告了 Token 消耗：平均每样本约 100K tokens，总约 9.5M tokens，平均修复轮次 1.8（最大 3 次）。
- 强调该框架适用于离线验证场景，Token 开销可接受，但具体算力细节缺失。

## 5. 实验数量与充分性

- **实验数量**：主要实验包括：
    - 整体检测率对比（3 个基线，95 个样本）
    - 按操作码家族检测率（5 个家族）
    - 消融实验（5 个变体：去分解、去检索、去操作码提示、去修复、去 Z3 验证）
    - Token 成本分析
    - 与手动 Rust 测试的实战对比（2 个 bug 分组：语义错误和执行路径错误）
- **充分性**：
    - 消融实验覆盖所有核心组件，量化贡献，设计合理。
    - 对比基线包括了最简 LLM 和带反馈的对话 LLM，公平比较。
    - 按家族分解验证了泛化能力。
    - **不足**：基准为自建，可能引入偏见；未对正样本系统评估假阳性率（论文明确说明延迟至未来工作）；仅测试了基于 Scroll 项目的手动测试，未在多个 zkEVM 实现上验证；缺少跨模型（如 GPT-4 vs Claude）对比。

## 6. 主要结论与发现

- VeriSynth 在 95 个负样本上检测率达 **91.6%** (87/95)，显著优于 LLM-only (46.3%)、对话式 LLM (61.1%) 和 Scroll 手写测试 (57.9%)。
- 在所有 5 个操作码家族上表现稳健，算术/位运算最高 (95.5%)，调用/创建路径最低 (85.7%)。
- 消融实验中自动修复贡献最大（移除后 DR 降至 66.3%），检索增强和语义分解同样关键。
- 与工业手写测试相比，VeriSynth 多检测出 32 个额外 bug，证实自动约束合成是手动测试的有力补充。

## 7. 优点

- **方法创新**：首次将 LLM 与 SMT 求解器以“前端提议、仲裁者判定”的混合范式用于 zkEVM 级形式验证，而非将 LLM 直接作为正确性裁判，克服了 LLM 幻觉和缺乏形式保证的问题。
- **技术集成**：语义分解、检索增强、并发起验证-修复闭环，三者协同提升模型可执行性和语义准确性，并通过消融实验验证了各自贡献。
- **实用导向**：构建首个源码级 zkEVM 操作码验证基准，填补了该领域标准化评估空白；与生产级手写测试对比，证明了实际工程价值。
- **实验设计**：多维度评估（整体、分家族、消融、实战对比），指标清晰，结果可信。

## 8. 不足与局限

- **基准偏见**：基准为自建，且基于 Scroll 项目（特定 zkEVM 实现），可能无法完全代表其他 zkEVM 实现或通用 Rust 代码。
- **假阳性未评估**：未在正样本上系统测量误报率（false alarms），可能将正确实现误判为有 bug（作者声明留作未来工作）。
- **泛化性有限**：仅使用 GPT-4o，未测试其他 LLM（如 LLaMA、Claude）或更小模型；修复预算固定为 3 次迭代，可能遗漏需更多轮次修复的样例。
- **成本问题**：每样本约 100K tokens，总约 9.5M tokens，对大规模持续集成可能成本较高；未提供替代方案如蒸馏模型或缓存策略。
- **硬件资源不透明**：未报告 GPU 型号、数量、训练时长等算力消耗，难以评估实际部署成本。
- **形式保证范围**：保证限于编码的语义单元和抽象级别，未覆盖完整 zkEVM 实现的所有潜在交互。

（完）
