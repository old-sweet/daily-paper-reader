---
title: A Progressive Approach to Synthesizable RTL Design Generation Using LLMs
title_zh: 一种基于大语言模型的可综合RTL设计生成的渐进式方法
authors: "Xiangfei Kong, Tasnim Tabassum, Marwan Abdelwahab, Hao Zheng"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18519v1"
tags: ["query:llm-hw-fv"]
score: 8.0
evidence: 基于LLM的RTL生成与可验证的规范细化
tldr: "大语言模型生成RTL设计常因理解规范出错。VeriRefine先精炼自然语言规范为显式的ASTF表示，经五层审计修复解释错误后再生成Verilog。在RTLLM v2.0上达94.0%功能正确率，VerilogEval-Human v2上达98.1%。将规范精炼作为可验证阶段，实现理解错误的定向修复，并保证可综合性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18519v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1778, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1735, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1356, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 573, \"height\": 990}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 851, \"height\": 628}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 869, \"height\": 708}, {\"url\": \"assets/figures/arxiv/2607.18519v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 857, \"height\": 443}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18519v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 916}, {\"url\": \"assets/tables/arxiv/2607.18519v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 771}, {\"url\": \"assets/tables/arxiv/2607.18519v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 893, \"height\": 1186}, {\"url\": \"assets/tables/arxiv/2607.18519v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1855, \"height\": 725}]"
motivation: LLM生成RTL的错误多源于对非正式规范的理解偏差，现有方法未能在表示层修复解释错误。
method: 渐进精炼规范为基于信号的ASTF，经五层审计后生成Verilog，并将仿真失败分类为理解或编码错误反馈。
result: "在RTLLM v2.0和VerilogEval-Human v2上分别达到94.0%和98.1%的功能正确率。"
conclusion: 将规范精炼作为可验证阶段能显著提升RTL生成正确性，并确保可综合性。
---

## 摘要
大语言模型可以直接从自然语言规范生成寄存器传输级（RTL）设计。然而，其失败主要源于理解而非编码\cite{zhang2026understanding, qiu2025towards}。规范是非正式且模糊的，模型的解读保持隐式，每次误读都被无声地提交到Verilog中，只有仿真才能暴露它。中间表示使解读部分显式化，但现有工作不验证解读与规范的一致性，并且无论误读源于何处，都在代码层面修复仿真失败。VeriRefine则将规范精炼视为RTL生成的一个可验证阶段。它逐步将散文式规范精炼为显式的、受模式约束的设计意图描述，以每信号抽象信号转移函数（ASTF）表示，在生成任何代码之前承诺每个信号的逻辑风格、时钟域和复位行为，并将每个行为基于逐字规范句子。然后，精炼后的规范通过五层审计，涵盖合理性、完整性、一致性、有限状态机完整性和核心RTL设计规则，因此在生成任何Verilog之前，在表示层面修复解读错误。一旦生成代码，每个仿真失败被分类为理解错误或编码错误，并路由到相应阶段进行针对性修复。由于每个信号的硬件类别在精炼过程中固定，可综合性成为流水线的结构属性，而非事后检查。使用Claude Sonnet 4.6，VeriRefine在RTLLM v2.0上达到94.0%的功能正确性，在VerilogEval-Human v2上达到98.1%。

## Abstract
Large language models can generate register-transfer-level (RTL) designs directly from natural language specifications. Their failures, however, arise mostly from understanding rather than coding \cite{zhang2026understanding, qiu2025towards}. A specification is informal and ambiguous, the model's interpretation stays implicit, and every misreading is committed silently into Verilog, where only simulation can expose it. Intermediate representations make the interpretation partly explicit, yet existing works don't verify the interpretation against the specification, and repair simulation failures at the code level regardless of where the misreading originated. VeriRefine instead treats specification refinement as a verifiable stage of RTL generation. It progressively refines the prose specification into an explicit, schema-constrained account of design intent, expressed as per-signal Abstract Signal Transition Functions (ASTFs) that commit each signal's logic style, clock domain, and reset behavior before any code exists and ground every behavior in a verbatim specification sentence. The refined specification then passes a five-layer audit spanning soundness, completeness, consistency, FSM integrity, and core RTL design rules, so interpretation errors are repaired at the representation level before any Verilog is generated. Once code is generated, each simulation failure is classified as an understanding error or a coding error and routed back to the corresponding stage for targeted repair. Because every signal's hardware class is fixed during refinement, synthesizability becomes a structural property of the pipeline rather than a post-hoc check. With Claude Sonnet 4.6, VeriRefine reaches 94.0\% functional correctness on RTLLM v2.0 and 98.1\% on VerilogEval-Human v2.

---

## 论文详细总结（自动生成）

# 中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLM）直接从自然语言规范生成RTL代码时，错误主要源于对规范的理解偏差（而非编码错误），但现有方法：
  - 未能显式验证中间表示与规范的一致性；
  - 仅在仿真失败后直接修改代码，无法定位误读根源；
  - 生成过程缺乏对可综合性的内建保证。
- **研究动机**：填补规范与RTL之间的语义鸿沟，将规范精炼作为可验证阶段，使理解错误在表示层被修复后再生成代码，从而提升功能正确性和可综合性。
- **整体含义**：提出首个将抽象精炼操作化的LLM-Based RTL生成框架，通过可审计的中间表示和分层反馈回路，显著提高生成质量。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想：VeriRefine框架
- 采用**代理式（agentic）架构**，包含三个模块：精炼模块（Refinement）、实现模块（Implementation）、验证与调试模块（Verification & Debug）。
- 中间表示：**抽象信号转移函数（Abstract Signal Transition Functions, ASTF）**，为每个被驱动信号定义：
  - `H(s)`：硬件类别（组合/时序/锁存）、时钟域、复位行为；
  - `G(s)`：优先级排序的守卫动作列表 `(condition, action, provenance)`，其中provenance是规范中的逐字引用句子。
- 关键流程：
  1. **精炼模块**：从自然语言规范生成候选ASTF（JSON格式），通过**封闭模式验证**（语法层）和**五层审计**（语义层：合理性、完整性、一致性、FSM完整性、核心RTL设计规则）修复表示错误；
  2. **实现模块**：利用已审计的ASTF直接翻译为可综合Verilog，强制遵循结构翻译规则（端口匹配、风格保真、GCA逐字翻译等）；
  3. **验证与调试模块**：生成仅依赖规范的内建测试平台（ITB），仿真失败后通过**失败分析器**将错误分类为ASTF问题或RTL问题，分别路由回精炼模块或实现模块进行针对性修复（最多两次迭代）。
- **关键技术细节**：
  - 分类阶段：通过关键词扫描识别设计类型（FSM、双时钟、时序、组合等），组装定制化生成提示（含基节、规则节、示例、可解析表格）。
  - 五层审计内容：见Table 1（FSM完整性检查）和Table 2（核心RTL设计规则检查）。
  - 内建测试平台：两阶段生成（验证计划→SystemVerilog激励模块），与DUT和黄金参考RTL比较输出。
  - 失败分类依据：规范原文、当前ASTF摘要、仿真日志（含首失配时间、信号状态等）。

## 3. 实验设计：数据集、Benchmark、对比方法
### 数据集与Benchmark
- **RTLLM v2.0**：50个Verilog设计任务（算术电路、FSM、数据结构、控制逻辑）。
- **VerilogEval-Human v2**：156个HDLBits派生问题（组合与时序设计，不同复杂度）。

### 对比方法
1. **直接生成（Direct generation）**：单次从规范生成Verilog（基线）。
2. **VeriRefine-Direct（消融）**：仅使用精炼模块和实现模块一次，无调试循环。
3. **VeriRefine（完整框架）**：含验证与调试模块（最多两次修复迭代）。
4. **VerilogCoder（SOTA基线）**：已发布的代理框架，使用图形化规划和波形追踪调试。

所有对比使用**Claude Sonnet 4.6**（主要模型）和**GPT-4-turbo**（部分对比）。温度设为0。

### 实验指标
- **功能正确性**：编译且通过基准测试仿真。
- **可综合性**：Yosys综合成功（SkyWater sky130A PDK）。
- **PPA质量**：`(P_int + P_sw + P_leak) × Area × T_cp`。
- **Token成本**：总输入+输出token数（百万）。

## 4. 资源与算力
- **论文未明确说明**使用的GPU型号、数量或训练时长。
- 仅提及使用Icarus Verilog (iverilog) v13.0、Yosys 0.30+48、OpenLane v1.0.2 进行仿真和综合。
- 所有LLM调用通过官方API（无本地训练），仅推理，不涉及微调或强化学习训练。

## 5. 实验数量与充分性
### 实验数量
- **两组基准**共206个设计（50+156）。
- **四种评估条件**（直接生成X2模型、VeriRefine-Direct X2模型、VeriRefine Sonnet 4.6、VerilogCoder Sonnet 4.6）。
- **消融研究**：对比VeriRefine-Direct（迭代0）与VeriRefine（迭代1-2），明确分离ASTF贡献与调试贡献。
- **失败案例分析**：详细分析6个未通过设计的根因（3个基准缺陷、1个规范歧义、2个能力极限）。

### 充分性与客观性
- 公平性：所有条件使用相同提示和重试限制；温度0减少随机性；VerilogCoder按发布实现重现并统一模型。
- 充分性：覆盖功能正确性、可综合性、PPA、Token成本四个维度；消融实验验证各组件贡献；失败分析揭示框架边界。
- 局限性：仅两个基准；模型仅Claude Sonnet 4.6和GPT-4-turbo；无跨模型泛化实验；未涉及大规模多模块设计。

## 6. 论文的主要结论与发现
- **功能正确性**：VeriRefine在RTLLM v2.0达94.0%（Sonnet 4.6），VerilogEval-Human v2达98.1%，分别比直接生成提高30.0%和12.8%。
- **可综合性**：功能正确设计中有93.8%~100%可综合，说明ASTF保证了可综合性。
- **PPA质量**：与SOTA基线VerilogCoder相当，未因ASTF约束而牺牲实现质量。
- **Token成本**：VeriRefine每正确设计约54K token，VerilogCoder约197K，成本约1/4。
- **消融结论**：ASTF审计是主要贡献（RTLLM贡献18.0%中的12.0%来自ASTF，6.0%来自调试循环）；调试循环对VerilogEval贡献更大（6.4%中的2.0%来自ASTF，4.4%来自调试）。
- **失败分析**：剩余6个失败中4个源于基准规范与测试矛盾或规范歧义，2个是能力极限（稳定误读）。

## 7. 优点
- **方法创新**：
  - 首次将规范精炼作为可验证阶段，而非仅作为中间步骤；
  - ASTF提供每信号显式硬件类别、守卫动作及逐字溯源，支持独立审计；
  - 失败分类路由机制（ASTF vs RTL）实现精准修复。
- **实验设计**：
  - 严格对比不同条件（直接生成、消融、完整框架、SOTA基线）；
  - 使用多维度指标（功能、可综合、PPA、成本）避免片面评估；
  - 消融研究和失败案例分析增强了结论可信度。
- **实用性**：在几乎相同功能正确率下，Token成本仅为VerilogCoder的1/4，适合预算受限场景。

## 8. 不足与局限
- **实验覆盖**：
  - 仅两个基准（RTLLM v2.0、VerilogEval-Human v2），未在更大规模或实际工业设计上验证；
  - 仅使用两个模型（GPT-4-turbo和Claude Sonnet 4.6），未测试其他LLM或开源模型；
  - 未涉及多模块层次化设计、流水线、异步电路等复杂场景。
- **偏差风险**：
  - ASTF分类和审计依赖手工设计的关键词和规则，可能对未见模式泛化不足；
  - 内建测试平台仅由LLM生成，可能遗漏关键边界情况。
- **应用限制**：
  - 框架假设规范可被LLM理解并转化为结构化ASTF，对极端模糊或自相矛盾的规范效果有限；
  - 最大修复迭代次数固定为2，可能不足以解决稳定误读；
  - 未处理PPA优化（仅保持可接受，非主动优化）。
- **资源信息缺失**：未报告计算资源消耗（GPU型号、运行时等），难以评估实际部署成本。

（完）
