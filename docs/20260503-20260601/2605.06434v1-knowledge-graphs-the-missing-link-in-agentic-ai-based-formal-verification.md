---
title: "Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification"
title_zh: 知识图谱：基于智能体人工智能的形式验证中缺失的一环
authors: "Vaisakh Naduvodi Viswambharan, Keerthan Kopparam Radhakrishna, Deepak Narayan Gadde, Aman Kumar"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06434v1"
tags: ["query:llm-assert"]
score: 9.0
evidence: LLM利用知识图谱从自然语言生成SystemVerilog断言
tldr: "现有LLM生成形式验证断言时，因自然语言规格模糊且RTL细节缺失，导致语义不匹配与语法错误。本文构建以验证为中心的知识图谱，从规格、RTL和工具反馈中提取结构化中间表示，链接需求、设计层次与信号。多Agent工作流通过图谱驱动语法修复、反例纠正和覆盖率增强，生成可编译的SystemVerilog断言。在七个基准设计上实现78.5%-99.4%的覆盖率，显著提升规格到RTL的接地性，但复杂时序和算术推理仍是挑战。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06434v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1881, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2605.06434v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 542}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.06434v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 956, \"height\": 355}, {\"url\": \"assets/tables/arxiv/2605.06434v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2012, \"height\": 693}, {\"url\": \"assets/tables/arxiv/2605.06434v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1906, \"height\": 383}]"
motivation: 现有LLM生成断言方法因规格与RTL脱节，导致语义不匹配和语法错误频繁。
method: 构建验证知识图谱整合规格、RTL和工具反馈的结构化信息，多Agent工作流查询更新图谱进行断言生成与优化。
result: "在七个基准上实现78.5%-99.4%的覆盖率，语法修复开销低，但复杂时序与算术推理仍困难。"
conclusion: 知识图谱有效提升断言生成的规格接地性，但LLM在复杂时序与算术推理上存在局限。
---

## 摘要
大型语言模型（LLM）的最新进展使得能够从自然语言规范生成SystemVerilog断言（SVA）的工作流程成为可能，这有望加速形式验证（FV）。然而，高质量的断言合成仍然具有挑战性，因为规范通常模糊或不完整，并且关键的微架构细节存在于寄存器传输级（RTL）中。许多现有方法将规范和RTL视为松散结构的文本，这削弱了规范到RTL的锚定，导致语义不匹配，并在形式解析和细化过程中频繁出现语法错误。本研究通过一个以验证为中心的知识图谱（KG）解决了这些局限性，该知识图谱是从从规范、RTL和形式工具反馈中提取的结构化中间表示（IR）构建的，包括语法诊断、反例（CEX）和覆盖率报告。KG将需求、设计层次结构、信号、假设和属性联系起来，为生成提供了可追溯的、基于设计的上下文。一个多智能体工作流程查询并更新该KG以生成SVA，并驱动三个精细化循环：由工具诊断引导的语法修复、使用跟踪链接的CEX引导修正以及覆盖导向的属性增强。在七个基准设计上的评估表明，基于KG的上下文检索改进了规范到RTL的锚定，并一致地生成可编译的SVA，语法修复开销较低。该方法实现的正式覆盖率范围从78.5%到99.4%，尽管收敛性表现出设计依赖性，复杂的时序和算术推理对于当前LLM能力仍然具有挑战性。

## Abstract
Recent advances in Large Language Models (LLMs) have enabled workflows that generate SystemVerilog Assertions (SVAs) from natural-language specifications, with the potential to accelerate Formal Verification (FV). However, high-quality assertion synthesis remains challenging because specifications are often ambiguous or incomplete and critical micro-architectural details reside in the Register Transfer Level (RTL). Many existing approaches treat the specification and RTL as loosely structured text, which weakens specification-to-RTL grounding and leads to semantic mismatches and frequent syntax failures during formal parsing and elaboration. This work addresses these limitations with a verification-centric Knowledge Graph (KG) constructed from structured Intermediate Representations (IRs) extracted from the specification, RTL, and formal-tool feedback, including syntax diagnostics, Counterexamples (CEXs), and coverage reports. The KG links requirements, design hierarchy, signals, assumptions, and properties to provide traceable, design-grounded context for generation. A multi-agent workflow queries and updates this KG to generate SVAs and to drive three refinement loops: syntax repair guided by tool diagnostics, CEX-guided correction using trace links, and coverage-directed property augmentation. Evaluation across seven benchmark designs indicates that KG-based context retrieval improves specification-to-RTL grounding and consistently produces compilable SVAs with low syntax-repair overhead. The approach achieves formal coverage ranging from 78.5% to 99.4%, though convergence exhibits design dependence with complex temporal and arithmetic reasoning remaining challenging for current LLM capabilities.

---

## 论文详细总结（自动生成）

# 论文总结：Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：利用大型语言模型（LLM）从自然语言规格自动生成SystemVerilog断言（SVA）时，存在两个主要障碍：①自然语言规格往往模糊或不完整；②关键的微架构细节存在于RTL（寄存器传输级）代码中。现有方法将规格和RTL视为松散结构的文本，导致规格到RTL的“接地性”（grounding）弱，产生语义不匹配和频繁的语法错误。
- **整体含义**：这项工作提出以知识图谱（KG）作为桥梁，将规格意图、RTL结构、生成属性及形式工具反馈（语法诊断、反例CEX、覆盖率报告）统一表示为可查询、可追溯的结构化信息，从而驱动多智能体LLM进行属性生成与闭环精化，以提升形式验证的自动化质量和可靠性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个以验证为中心的知识图谱（KG），该KG由从规格、RTL代码和形式化工具输出中提取的结构化中间表示（IR）拼接而成。多智能体工作流查询并更新该KG，用于生成SVA，并驱动三个精化循环：语法修复、反例修正和覆盖率提升。
- **关键技术细节**：
  - **中间表示（IR）**：将验证工件存储为类型化JSON IR，包括spec_chunks.json、requirements.json、design_model.json（RTL元数据）、properties.json、formal_results.json、cex_cases.json、coverage_metrics.json等，以及节点和边文件nodes.csv、edges.csv。
  - **KG构建**：使用NetworkX构建运行时KG，从IR导出节点和边。KG通过任务锚点节点（如需求或属性）遍历出有限邻域，为各智能体提供局部上下文（包括目标需求、父规格片段、相关RTL信号/模块、关联属性及工具输出）。
  - **多智能体分解**：
    - 属性生成：sva_lead（协调）、spec_analyst（分解需求为可测试条件）、sva_author（翻译为SVA）、sva_reviewer（校验）、sva_patcher（修正）、code_extractor（组装）。
    - 语法修复：syntax_analyzer（解析错误）、syntax_fixer（确定性修复+LLM处理复杂错误）、syntax_validator（隔离测试）、code_extractor。
    - 反例修正：vcd_parser（提取波形）、spec_assertion_analyzer（根因分类）、rtl_analyzer（检查RTL）、cex_fixer（修正属性）、code_extractor。
    - 覆盖率提升：cov_lead_agent（优先级）、cov_analyzer（根因分析）、cov_processor（链接需求）、cov_improver（生成覆盖属性）。
  - **精化循环**：每个循环最多尝试3次，失败则标记为手动审查。

## 3. 实验设计

- **数据集/场景**：7个开源基准设计，来自OpenCores[14]与CVDP[15]：
  - FIFO、Lemming、ALU、CIC Decimator（CICD）、Binary Search Tree（BST）、TTC Counter、CSR APB Interface（CSR）。
- **Benchmark**：无统一baseline对比列表，但论文在相关工作中与AssertLLM[2]、ChIRAAG[3]、Saarthi[4]、AssertionForge[6]等现有方法进行了定性比较。
- **对比方法**：本文主要展示自身方法的端到端效果，并未在同一实验设置下与特定baseline进行定量对比。部分指标（如语法修复次数、覆盖率）可作为与文献中报告值的间接参照。

## 4. 资源与算力

- **论文明确未提及具体算力信息**：未说明使用的GPU型号、数量、训练/推理时长、显存等硬件资源。仅提到使用GPT‑5.2模型（闭源）和Cadence JasperGold形式验证工具。因此无法从本文中获知算力需求。

## 5. 实验数量与充分性

- **实验数量**：在7个不同复杂度的设计上进行了完整流程评估（属性生成→语法修复→反例修正→覆盖率提升）。每组实验均记录了：生成属性总数、通过/失败数、语法修复次数、反例修正成功/失败数、空属性比例、可达到RTL覆盖率、最终形式覆盖率。此外还报告了KG节点/边数。
- **充分性评价**：
  - **优点**：覆盖了多种类型的设计（FIFO、FSM、ALU、数字滤波器、树结构、计数器、总线接口），具有代表性；指标多维度（语法、语义、覆盖率）。
  - **不足**：①未包含消融实验（如“不使用KG” vs “使用KG”）；②未与现有同类工具（如AssertLLM、ChIRAAG、Saarthi等）在同一基准上复现对比；③只使用单一LLM（GPT‑5.2），缺乏跨模型比较；④基准设计规模较小（均为开源小型IP），未验证工业级大型设计。

## 6. 论文的主要结论与发现

- **主要结论**：基于KG的多智能体工作流能够持续产生可编译的SVA，且语法修复开销低（对于7个设计，修复尝试次数在1~3次之间），形式覆盖率范围78.5%~99.4%。KG提供的结构化上下文（尤其是design_model IR中确定的信号和层次链接）显著减少了未声明标识符、宏不匹配等前端错误，并提升了规格到RTL的接地性。
- **发现**：
  - FIFO和CIC Decimator通过反例修正和覆盖率提升获益最大，初始失败属性多数可通过局部修正转为通过。
  - Lemming和ALU因初始属性质量高，剩余失败多涉及复杂时序（复位行为、FSM状态转换意图、多周期算术），自动化精化效果有限。
  - BST最困难：大量属性生成后仍失败，反例修正难以收敛，需要全局数据结构不变量和归纳推理。
  - 空属性在CIC Decimator和BST中较多，表明默认环境下部分属性缺乏强约束力。
  - KG规模随设计复杂度增长，但不能直接预测验证成功与否。

## 7. 优点

- **方法创新性**：将知识图谱引入LLM辅助形式验证，构建了从规格、RTL到工具反馈的统一、可追溯表示，实现了真正的闭环精化（语法→反例→覆盖率）。
- **多智能体分解**将复杂任务拆分为关注点分离的智能体，使每个智能体获得精准的KG上下文，提升LLM输出质量。
- **明确的中间表示（IR）**确保跨迭代的确定性和可审计性，且支持属性粒度的选择性失效与重验证（增量更新）。
- **实验覆盖多类型设计**：评估了不同逻辑结构（时序、组合、有限状态机、树、控制寄存器），提供了全面的成功率、覆盖率和KG指标。

## 8. 不足与局限

- **实验局限性**：
  - 缺乏与现有工作的严格定量对比（缺乏基线复现）。
  - 未进行消融实验（如无KG版本、无多智能体分解版本），无法直接归因KG带来的增益。
  - 只使用一个LLM模型（GPT‑5.2），未评估其他模型或开源模型的鲁棒性。
  - 基准设计均为小型开源IP（数百到数千行RTL），未在工业级百万门级设计上验证。
- **方法局限**：
  - 复杂时序推理（多周期路径、全局不变量、归纳推理）仍是LLM的瓶颈，当前方法仅依赖局部KG邻域和反例追踪，不足以处理深层语义。
  - 反例修正和覆盖提升中的根因分类依赖LLM的准确性，可能引入错误分类导致无效修正。
  - 对空属性的处理仅标记但未主动约束环境，可能造成覆盖率虚高。
  - 无法自动发现RTL bug与属性错误的根本区别（系统将RTL bug记录为手动修复，未自动化RTL修改）。
- **应用限制**：方法依赖商业LLM API（GPT‑5.2）和商业形式工具（JasperGold），可复现性受限；且需要人工为每个设计准备规格片段、需求提取等前处理，自动化程度仍有提升空间。

（完）
