Title: On Parameterized Verification Over Tree Topologies

URL Source: https://arxiv.org/pdf/2606.27172v1

Published Time: Fri, 26 Jun 2026 01:11:30 GMT

Number of Pages: 22

Markdown Content:
# On Parameterized Verification Over Tree Topologies 

## Romain Delpy 

LaBRI, Univ. Bordeaux, CNRS, Bordeaux INP, Talence, France 

## Anca Muscholl 

LaBRI, Univ. Bordeaux, CNRS, Bordeaux INP, Talence, France 

## Grégoire Sutre 

LaBRI, Univ. Bordeaux, CNRS, Bordeaux INP, Talence, France 

Abstract 

Parameterized verification of finite-state processes with rendez-vous synchronization is notoriously undecidable when processes are linearly ordered. In this paper we study two kinds of bounds under which we determine the complexity of safety checking over tree topologies. When bounding the depth we obtain that the complexity is related to the fast growing hierarchy. Our second bound limits the alternations between upwards and downwards synchronizations in the tree (phases), and occurs naturally in many concrete settings. If we fix the number of phases then the complexity of safety checking is Expspace complete, and if the number of phases is part of the input it is 2Expspace complete (both for arbitrary depth). 

2012 ACM Subject Classification Theory of computation → Logic and verification 

Keywords and phrases Concurrent programming, Parameterized verification 

Funding This work was (partially) supported by the grant ANR-23-CE48-0005 of the French National Research Agency ANR (project PaVeDyS). 

## 1 Introduction 

Analyzing the correctness of concurrent or distributed systems is an inherently challenging task, due to inter-process interactions that can result in very large state spaces. Beyond this state explosion problem, a different challenge arises in settings where one is interested in proving correctness independently of the system size. This is the realm of parameterized verification, that has gained intensive momentum over the past decade (cf. surveys [9, 18]). In this paper we investigate the complexity of safety checking of parameterized systems over tree topologies. Our focus on trees is motivated by two key ideas. First, trees naturally model modern execution environments: in recursive process creation, parent and child processes synchronize through shared communication channels. This structure also arises in cloud infrastructures (master-worker hierarchies), file systems, and hierarchical cache coherence protocols. Second, while parameterized communication over general graphs is notoriously difficult to define — often requiring complex machinery like MSO and bounded tree-width [ 3], or specialized logics [ 11 , 12 ] — tree topologies offer a balance between expressive power and algorithmic tractability. Consider a concurrent web scraper, modeled according to a recursive Rust implementation (github.com/dawksh/mscraper/ ) which scans for URLs embedded in web pages. In this system, when a thread discovers a new URL, it spawns a child thread to scan the corresponding page; this process continues recursively until a predefined depth is reached. Communication follows the tree structure: each process aggregates URL lists from its children and propagates the results back to its parent. Formally, we model each process as a finite-state automaton where transitions represent either synchronizations with a parent (written as ↑), or with a child (written as ↓, see Figure 1). Data exchange involves passing URLs coupled with 

> arXiv:2606.27172v1 [cs.LO] 25 Jun 2026

2 On Parameterized Verification Over Tree Topologies           

> a
> ↓(url ,1)
> ↓(url ,1) ↓List< url >
> for i < d
> ↑(url , i )
> ↓(url , i + 1)
> ↓(url , i + 1)
> ↓List< url >
> ↑List< url >
> ↑(url , d )↑List< url >
> Figure 1 Automata representing the processes of the web scrapper, where dis the maximal depth. The left one represents the main process, and the right one represents the spawned threads.

depth counters; once a process receives a depth value equal to the maximum threshold, it terminates its recursive spawning and stops synchronization with further child processes. In models with peer-to-peer communication, the undecidability of safety checking is immediate under asynchronous communication [ 13 ], regardless of parameterization. We therefore focus on rendez-vous synchronization. Even in this synchronous setting, however, safety checking on trees, or even arrays, remains undecidable [ 4, 19 ]. This motivates the search for restrictions under which safety checking is decidable. We investigate the complexity of two under-approximations of systems with rendez-vous synchronization over trees. The first one consists in bounding the tree depth. This is arguably the most natural restriction that we can impose on our systems. However, we show that in this case the complexity of safety checking is prohibitively high, characterized by the fast growing hierarchy. Specifically, for a fixed depth d, complexity lies between the levels FΩd−1

and FΩd of the hierarchy. This result motivates our second restriction, called phase bound .Within a phase a process may synchronize either exclusively with its parent or exclusively with its children. Our web scraper example above has phase bound 3. It is also interesting to note that many of the standard examples of parametrized protocols over trees are phase bounded, e.g. the Leader election protocol [ 1] (electing a leader among leaves) has also phase bound 3. We show that by fixing the number of phases, safety checking becomes 

Expspace -complete, and if the number of phases is part of the input then it becomes 2Expspace -complete. Somewhat surprisingly, these complexity results hold independently of the tree depth. 

Related work. The verification of parameterized tree-structured systems has been extens-ively studied. Tree Regular Model Checking [ 2, 21 ] provides a symbolic framework relying on tree transducers. It often results in semi-algorithmic procedures and is typically restricted to ranked architectures. To cope with non-termination and implementation complexity, alternative symbolic approaches have been proposed, such as backward reachability [ 1 ] and abstraction-refinement techniques [10]. A parallel line of research focuses on cutoff-based verification, which reduces the para-meterized problem to a finite check on a representative system size. While cutoffs have been established for various logics, including indexed LTL and CTL [ 3], and their existence proved to be polynomially decidable for certain rendez-vous protocols [ 6 ], this approach remains sensitive to the state-space explosion problem. Our work also relates to recent efforts to identify decidable fragments through structural and behavioral constraints. In the context of pi-calculus, bounding the tree depth of process creation leads to a complexity of Fε0 [ 5]. A different phase-based abstraction has been explored for broadcast communication [ 20 ]. In that setting, a phase is defined by the R. Delpy, A. Muscholl, and G. Sutre 3

direction of communication (exclusively broadcasting or receiving); however, safety remains undecidable even for a small number of phases. For convenience, technical terms and notations in the electronic version of this manuscript are hyper-linked to their definitions (cf. https://ctan.org/pkg/knowledge ). Proofs that are missing in the main text can be found in the appendix. 

## 2 Definitions 

In this paper, we consider communicating processes over tree topologies. A rooted tree 

T = ( P, Ch ) is a directed graph without self-loops, where P is a set of processes and 

Ch ⊆ P × P is a set of channels, with one distinct node (the root) such that every node has a unique path from the root. We will denote the root of tree T by root T (or just root if T is clear from the context). The orientation of the edges specifies how processes interact in the tree: (p, q ) ∈ Ch means process p is the parent of process q.We let M denote a finite set of message contents, and we write: 

Dw = {↓ m | m ∈ M } for the set of synchronization actions with some child process (downwards synchronizations). 

Up = {↑ m | m ∈ M } for the set of synchronization actions with the parent process (upwards synchronizations). By Act = Dw ∪ Up ∪ { τ } we denote the set of all actions (with τ an internal action). A communicating automaton is a finite set of processes that exchange messages, each process being given as a finite LTS. In the parametric setting, the number of processes is not known beforehand. All processes have the same state space, as in rendez-vous protocols [ 19 ]. 

▶ Definition 2.1. A Parametrized Communicating Automaton ( PCA for short) is a tuple 

(S, ∆, s init ). Here S is the finite set of states , ∆ ⊆ S × Act × S the (finite) set of transitions ,and sinit ∈ S the initial state. 

We will use synchronous communication as synchronization mechanism. Given a PCA 

A = ( S, ∆, s init ) and a (tree) topology T = ( P, Ch ), we define a configuration as a total function c : P → S mapping each process to its current state. We denote by CA,T the set of all configurations of A over T . We call cinit  

> A,T

the initial configuration of A over T , with 

cinit  

> A,T

(p) = sinit for all p ∈ P (we write cinit when A and T are clear from the context). To define transitions we need to enrich actions by process names. We denote by Act T =

{p⋊⋉q(m) | (p, q ) ∈ Ch , m ∈ M } ∪ { p : τ | p ∈ P } the set of synchronous communication actions. As expected, p⋊⋉q(m) is a synchronization between p and q with content m.The transition relation →A,T ⊆ C A,T × Act T × C A,T over the set CA,T of configurations of A over T is defined as expected. Given two configurations c, c′ ∈ C A,T , we let c a

−→A,T c′

if either is true: 

a = p⋊⋉q(m) for some (p, q ) ∈ Ch and m ∈ M , c(p) ↓m

−−→ c′(p) ∈ ∆, c(q) ↑m

−−→ c′(q) ∈ ∆,and c(t) = c′(t) for all t ∈ P \{ p, q }.

a = p : τ for some p ∈ P , c(p) τ

−→ c′(p) ∈ ∆ and c(q) = c′(q) for all q̸ = p.A run of a PCA A over a topology T is a sequence ρ = c0, a 1, c1, . . . , a n, cn such that 

c0 = cinit  

> A,T

, and ciai+1 

−−−→A,T ci+1 for all 0 ≤ i < n . We call a1 · · · an the trace of the run ρ,and denote it by trace (ρ). A configuration c is reachable if there is a run ending in c.The projection of a trace u ∈ Act T ∗ over a process p, written as u|p ∈ Act ∗, is the subsequence of actions in which p participates: p⋊⋉q(m) projects to ↓m, q⋊⋉p(m) projects to 

↑m, and p : τ projects to τ .4 On Parameterized Verification Over Tree Topologies 

The size of a PCA A = ( S, Act , ∆, s init ) is defined as |S| + |∆|.A common task in parametrized verification is to check safety, i.e., that no error state is reachable, no matter how many processes participate in a run. Formally, the Safety 

problem asks, given a PCA A and a state s ∈ S, whether for all rooted trees T , there is no run ending in some configuration c such that c(root T ) = s. For ease of reading, we will reason on the dual, Coverability problem:                     

> Coverability
> Input: A PCA A= ( S, ∆, s init )and a state s∈S.
> Output: Yes if there exists some rooted tree Tand a run of Aover Tending in some configuration csuch that c(root T) = s.

▶ Remark 2.2. The reader may have noticed that all processes have the same underlying LTS, unlike our example in Figure 1. However, we can simulate systems where the root has a different LTS, say LTS root , by letting a process choose at the beginning of the run between LTS root and the LTS of the non-root processes. For this we create a new LTS as the disjoint union of LTS root (with initial state sinit 

> root

) and the LTS of the non-root processes (with initial state s0), adding a new initial state sinit . State sinit has only two τ -transitions, leading to 

sinit  

> root

and to s0. We also remove all upward actions from Sroot , and set the final state we want to reach sf in Sroot . This way we can ensure that the root has chosen Sroot if sf is reached; if a non-root process chooses to go to Sroot then it cannot receive messages from its parent, so its subtree is disconnected. ⌟

The Coverability problem defined above is undecidable, as to be expected because processes on a branch of the tree are linearly ordered [ 4 ]. A simple argument for undecidability in our setting is to reduce the halting problem for Minsky machines with two counters, using a branch to encode each counter in unary, together with a special symbol at the end of the branch to do zero tests (see also [17] for a similar construction). In order to regain decidability we will study the behavior of PCA under two restrictions. The first one, arguably the most natural, is to bound the depth of trees. The second one is to bound the number of alternations each process can do between synchronizing downwards and upwards in the tree. A run ρ is said to be k-phase-bounded if for every process p ∈ P , trace (ρ)|p ∈ (( Dw + τ )∗ +(Up + τ )∗)≤k. In the example given in Figure 1, the tree depth is bounded by d, and the phase number for each process is bounded by 3: first receive an URL from the parent, then send URLs and receive URL lists from the children, and finally send an URL list back to the parent. A state s ∈ S is (d, k )-coverable in A if there exists a tree T of depth at most d and a k-phase-bounded run ending in some configuration c such that c(root T ) = s. By writing 

d = ∞ we mean that there is no bound on the depth of the topology. Likewise, for k = ∞

we mean that there is no bound on the number of phases. We will consider four variants of the problem, depending on whether d and k are fixed or are part of the input. If d and/or k are inputs of the problem, they are in N and given in unary, otherwise they can be in N ∪ {∞} , with ∞ meaning unbounded.                          

> (d, k )-Coverability
> Input: A PCA Aand a state s∈S.
> Output: Yes if sis (d, k )-coverable in A.
> Coverability( d, k )
> Input: A PCA A, a state s∈Sand two integers d, k ∈N.
> Output: Yes if sis (d, k )-coverable in A.

R. Delpy, A. Muscholl, and G. Sutre 5

(d)-Coverability( k)

Input: A PCA A, a state s ∈ S and an integer k ∈ N.

Output: Yes if s is (d, k )-coverable in A.

(k)-Coverability( d)

Input: A PCA A, a state s ∈ S and an integer d ∈ N.

Output: Yes if s is (d, k )-coverable in A.

The next two tables summarize our results: 

ccc

d k 1 ≥2 ∞

1 Expspace -c 

≥ 2 (Lem 3.1) 

Expspace -c 

Expspace -c (Lem 3.1, Thm 4.7) between FΩd−1

and FΩd (Lem 3.4, Lem 3.5) 

∞ Undec 

Figure 2 Complexity of (d, k )-Coverability for PCA. 

d (d)-Coverability( k)

1 Expspace -c (Lem 3.1) 

≥ 2

∞

2Expspace -c (Thm 4.4, Thm 4.7) 

k (k)-Coverability( d)

≥ 1 Expspace -c (Lem 3.1, Thm 4.7) 

∞ Fε0 -c (Lem 3.4, Lem 3.5) 

Coverability( d, k )

2Expspace -c (Thm 4.4, Thm 4.7) 

Figure 3 Complexity of Coverability for PCA when d and/or k are part of the input. 

Let us comment on some boundary cases for our parameters. If we restrict to 1-phase-bounded runs, the root process can talk to its children; but those processes will not be able anymore to communicate with their children. Thus a state is (∞, 1) -coverable iff it is 

(1 , 1) -coverable. Similarly, if the depth is one then every run is 1-phase-bounded, so a state is (1 , ∞)-coverable iff it is (1 , 1) -coverable. 

## 3 Depth-bounded PCA 

In this section, we will show that coverability of PCA on bounded-depth trees is closely related to coverability in Nested Counter Systems [ 15 , 5]. We start by showing that (1 , k = ∞)-

coverability is equivalent to VASS coverability .

## 3.1 Depth one PCA 

Let m be a positive integer. An m-VASS is a tuple V = ( Q, q init , ∆) where Q is a finite set of states, qinit is the initial state, and ∆ ⊆ Q × Zm × Q a transition relation. A configuration of V is a tuple (q, x) where q ∈ Q is a state and x ∈ Nm is a vector of non-negative integers. The initial configuration is (qinit , 0). The step relation between two configurations is defined by (q, x) v

−→ (q′, x′) if (q, v, q ′) ∈ ∆ and x′ = x + v ≥ 0. It is understood that ≤ denotes the component-wise extension of the usual order ≤ on N. A run of V is a sequence σ = ( q0, x0) v1

−→ (q1, x1) v2

−→ . . . vn

−−→ (qn, xn), where (q0, x0) is initial. Let (q, x) and (q′, x′) be two configurations. We say that (q′, x′) covers (q, x) if q = q′

and x ≤ x′, and we denote this as (q, x) ⪯ (q′, x′).6 On Parameterized Verification Over Tree Topologies 

The size of an m-VASS V is |V| = |Q| + ∑ 

> (q, v,q ′)∈∆

∥v∥1 where ∥v∥1 = ∑mi=1 |v[i]|.We recall the coverability problem for VASS:             

> VASS-Coverability
> Input: An m-VASS Vand a state q∈Q.
> Output: Yes if (q,⃗ 0) is coverable in V.

This problem is known to be Expspace -complete [25, 27]. To show that (1 , k = ∞)-Coverability for PCA is in Expspace , we can use counter abstraction [ 19 ]. We simulate a PCA A = ( S, Act , ∆, s init ) on trees of depth 1 by counting how many children of the root are in a given state. Thus we construct in polynomial time an m-VASS V with m = |S|, in which coverability for a state s ∈ S can be checked in exponential space [27]. For the lower bound, we reduce VASS-Coverability to (1 , k = ∞)-Coverability .Let V be an m-VASS. We construct a PCA A where process root keeps track of the state and simulates the transitions of V, while the children will store the counter values. Any child process will be either in state sinit or in si (1 ≤ i ≤ m), and the number of children in state 

si will represent the value of the i-th counter. To do so, we replace each transition q v

−→ q′ of V by a series of transitions that will move 

v[i] children from state sinit to state si if v[i] ≥ 0, and will move −v[i] children from state 

si to state sinit if v[i] < 0, for each 1 ≤ i ≤ m.From this we get that configuration (q,⃗ 0) is coverable in V iff there exists a tree T of depth 1 and some configuration c ∈ C A,T reachable in A over T where c(root ) = q.

▶ Lemma 3.1. (1 , k = ∞)-Coverability for PCA is Expspace -complete. 

## 3.2 Coverability for Nested Counter Systems 

We have seen that for depth one, PCA are equivalent to VASS. It seems natural to look at nested counters for PCA over trees of fixed depth. We will show in the next subsection a tight correspondence between PCA over trees of depth at most d and d-Nested Counter Systems [ 15 , 5 ]. The latter manipulate higher-order counters, in a similar manner to Nested Petri Nets [26]. We first recall the definition of Nested Counter Systems. Let d ∈ N be an integer. A d-Nested Counter System (d-NCS ) is a tuple N = ( Q, ∆) 

where Q is a finite set of states and ∆ ⊆ ⋃

> 1≤i,j ≤d+1

(Qi × Qj ) is a finite set of rules . The set of configurations CN of N is is defined as the set of all labelled rooted trees of depth at most 

d, with labels from the set Q. Formally, a configuration C ∈ C N is a tuple C = ( V, E, root , h )

where (V, E, root ) is a rooted tree of depth at most d, and h : V → Q is a labeling function. The transition relation −→ ⊆ C N × C N on configurations is defined as follows. Let 

r = (( q0, . . . , q i), (q′

> 0

, . . . , q ′ 

> j

)) ∈ ∆ be a rule, with 0 ≤ i ≤ j ≤ d. We say that a configuration 

C moves to some configuration C′ using rule r (written C r

−→ C′) if there is a path v0, . . . , v i

in C starting at the root labeled by q0, . . . , q i, and C′ is obtained from C by changing the label of each vk to q′ 

> k

for 0 ≤ k ≤ i, and for i < k ≤ j creating a new vertex vk as the child of vk−1, labeled by q′

> k

. Similarly, suppose r = (( q0, . . . , q i), (q′

> 0

, . . . , q ′ 

> j

)) ∈ ∆ is a rule, with 

0 ≤ j < i ≤ d. Then C r

−→ C′ if there is a path v0, . . . , v i in C starting at the root and labeled by q0, . . . , q i, and C′ is obtained from C by changing the label of each vk to q′ 

> k

for 

0 ≤ k ≤ j and removing the subtree rooted at vj+1 .We write C → C′ if there exists a rule r ∈ ∆ such that C r

−→ C′. Moreover, we say that C′ is reachable from C (written C ∗

−→ C′) if there exist C0, . . . , C n, n ≥ 0, such that R. Delpy, A. Muscholl, and G. Sutre 7

C = C0 −→ · · · − → Cn = C′. We say that C′ covers C (written C ⪯ C′) if C is an induced subtree of C′ with the same root. The coverability problem for NCS is:                 

> d-NCS Coverability
> Input: Ad-NCS Nand two configurations Ci, C f∈ C N.
> Output: Yes if there exists Csuch that Ci
> ∗
> −→Cand Cf⪯C.

It is readily seen that 1-NCS are equivalent to VASS, so 1-NCS Coverability is 

Expspace -complete. To state the complexity of d-NCS Coverability with d ≥ 2, we need to introduce the fast-growing hierarchy of complexity classes [28]. We represent ordinal numbers using the Cantor Normal Form (CNF) as α = ωα1 + · · · +

ωαn , where α1 ≥ · · · ≥ αn are ordinals also written in CNF. We denote limit ordinals by λ.These are ordinals such that α + 1 < λ for every α < λ .A fundamental sequence for a limit ordinal λ is a sequence of ordinals (λn)n∈N with supremum λ. We consider the same fundamental sequence as in [ 15 , 7 ], which is defined by 

(α + ωβ+1 )n = α + ωβ + · · · + ωβ

︸ ︷︷ ︸

> n

and (α + ωλ′

)n = α + ωλ′

> n

for ordinals β and limit ordinals λ′. For example, a fundamental sequence for ω2 is given by 

(ω · n)n∈N, a fundamental sequence for ωω by (ωn)n∈N, etc. The fast-growing hierarchy is the ordinal-indexed family of functions Fα : N → N for 

α < ε 0 inductively defined by 

F0(n) = n + 1 Fα+1 (n) = Fα(· · · (Fα

︸ ︷︷ ︸ 

> ntimes

(n)) · · · ) and Fλ(n) = Fλn (n) .

The corresponding hierarchy (Fα)α<ε 0 of fast-growing complexity classes is defined in terms of the fast-growing functions Fα, see [ 28 ] for details. We recall that Fω corresponds to the class of problems solvable in Ackermannian time, and Fωω to the class of problems solvable in hyper-Ackermannian time. To state what is known about the complexity of coverability for NCS, we let Ωn denote the tower of ω’s of height n − 1, i.e., Ω1 = ω and Ωn = ωΩn−1 . The ordinal ε0 is the limit ordinal with fundamental sequence (Ω n)n∈N.

▶ Theorem 3.2 ([ 15 , 7 ]) . The complexity of d-NCS Coverability is between FΩd−1 and 

FΩd when d ≥ 2 is fixed, and it is Fε0 -complete when d is part of the input. 

The complexity of d-NCS Coverability for a fixed d ≥ 2 was shown in [ 15 ] to be between FΩd−1 and FΩ2d . However, as pointed out in [ 7], the proof of the lower bound is flawed. The flawed construction of [ 15 ] is fixed in [ 7], albeit for the extended model of NRCS that adds reset operations to NCS. More precisely, it is shown in [ 7] that d-NRCS Coverability is FΩd -complete (so the upper bound is reduced to FΩd , despite the additional reset operations). The lower bound of [ 7] does not (at least directly) apply to d-NCS Coverability . We show in the next subsection that d-NRCS Coverability can be reduced to (d + 1) -NCS Coverability . This provides a proof of the claimed lower bound of [15]. 

## 3.3 From Nested Reset Counter Systems to Nested Counter Systems 

This subsection presents a simulation of Nested Reset Counter Systems by plain Nested Counter Systems. We first recall the definition of Nested Reset Counter Systems from [7]. 8 On Parameterized Verification Over Tree Topologies 

A d-Nested Reset Counter System (d-NRCS ) is a tuple N = ( Q, ∆u, ∆r ) where Q is a finite set of states , ∆u ⊆ ⋃

> 1≤i,j ≤d+1

(Qi × Qj ) is a finite set of update rules , and ∆r ⊆ ⋃

> 1≤i≤d

(Qi ×

Q × Qi) is a finite set of reset rules . For clarity, an update rule (( q0, . . . , q i), (q′

> 0

, . . . , q ′ 

> j

)) ∈ ∆u

is written (q0, . . . , q i) upd 

−−→ (q′

> 0

, . . . , q ′ 

> j

) and a reset rule (( q0, . . . , q i), p, (q′

> 0

, . . . , q ′

> i

)) ∈ ∆r is written (q0, . . . , q i) rst( p)

−−−→ (q′

> 0

, . . . , q ′

> i

). Notice that a d-NCS (as defined in the previous subsection) is a tuple (Q, ∆) such that (Q, ∆, ∅) is a d-NRCS. The set of configurations CN of a d-NRCS N = ( Q, ∆u, ∆r ) is identical to the set of configurations of the d-NCS (Q, ∆u). Similarly, the transition relation r

−→ of an update rule 

r ∈ ∆u is identical to the transition relation r

−→ of the d-NCS (Q, ∆u). Given a reset rule 

r = ( q0, . . . , q i) rst( p)

−−−→ (q′

> 0

, . . . , q ′

> i

) with 0 ≤ i < d , we say that a configuration C moves to some configuration C′ using rule r (written C r

−→ C′) if there is a path v0, . . . , v i in C starting at the root labeled by q0, . . . , q i, and C′ is obtained from C by changing the label of each 

vk to q′ 

> k

for 0 ≤ k ≤ i, and removing every subtree rooted at a child v of vi such that v is labeled by p. The coverability problem for NRCS is defined in the same way as for NCS. 

▶ Lemma 3.3. For every fixed d ≥ 1, the d-NRCS Coverability problem is reducible in polynomial time to the (d + 1) -NCS Coverability problem. 

The rest of this subsection presents our reduction. We focus on reset rules (q0, . . . , q i) rst( p)

−−−→

(q′

> 0

, . . . , q ′

> i

) that are applied to nodes of depth at least one (the depth of the root is zero), i.e., such that i ≥ 1. Our recipe to simulate such a reset rule on a configuration C may be summarized as follows: 

1. pick a path v0, . . . , v i in C starting at the root labeled by q0, . . . , q i,

2. change the label of each vk to q′ 

> k

for 0 ≤ k ≤ i,

3. copy the subtree rooted at the node vi except for the children v of vi that are labeled with p, and 

4. delete the subtree rooted at vi.It is understood that the root v′ 

> i

of the copy performed in step (3) is a child of vi−1 (i.e., v′ 

> i

is a sibling of vi). As we cannot create a sibling of the root, this approach only works for i ≥ 1.The full version [ 16 ] of the paper [ 15 ] shows, albeit succinctly, how to copy a subtree in lossy manner. The main idea is to copy the source subtree twice, in DFS fashion, using a label i to mark the source node and two labels o1 and o2 to mark the copied nodes. The source subtree is deleted along the way (when the DFS moves up). Our simulation of reset rules (formally presented below) is inspired from the copy gadget of [16]. This simulation only uses update rules and is lossy in the following sense: any configuration obtained after the gadget is ⪯-smaller than the configuration obtained after the reset rule, and there is a run of the gadget that is not lossy, i.e., that provides the configuration obtained after the reset rule. We want to stress that this simulation only works for reset rules (q0, . . . , q i) rst( p)

−−−→

(q′

> 0

, . . . , q ′

> i

) with i ≥ 1. To simulate an arbitrary d-NRCS N = ( Q, ∆u, ∆r ) containing possibly reset rules (q0) rst( p)

−−−→ (q′

> 0

) that apply to the root, one may simply introduce an extra level, i.e., consider the (d + 1) -NRCS N ′ = ( Q ∪ { x}, ∆′

> u

, ∆′ 

> r

) where each update rule (q0, . . . , q i) upd 

−−→ (q′

> 0

, . . . , q ′ 

> j

) is replaced by (x, q 0, . . . , q i) upd 

−−→ (x, q ′

> 0

, . . . , q ′ 

> j

) and each reset rule (q0, . . . , q i) rst( p)

−−−→ (q′

> 0

, . . . , q ′

> i

) is replaced by (x, q 0, . . . , q i) rst( p)

−−−→ (x, q ′

> 0

, . . . , q ′

> i

).The (d + 1) -NRCS N ′ contains no reset rule on the root, so it can be transformed into an equivalent (with respect to coverability) (d + 1) -NCS. R. Delpy, A. Muscholl, and G. Sutre 9

Let us now formally present our simulation of reset rules with update rules. Consider a 

d-NRCS N = ( Q, ∆u, ∆r ) and a reset rule r = ( q0, . . . , q i) rst( p)

−−−→ (q′

> 0

, . . . , q ′

> i

) with d > i ≥ 1.We introduce new states init (r), par (r), cp (r), cpd (r, q ), cpu (r), src (q) and dst (q), where 

q ranges over Q. The rules of the gadget simulating r are given below. In these rules, 

σ stands for the sequence σ = q′

> 1

, . . . , q ′

> i−1

,

m stands for an arbitrary integer in {0, . . . , d − i − 1}, and 

x1, . . . , x m, y, z stand for arbitrary states in Q (note that the sequence x1, . . . , x m is possibly empty). As d is fixed, the gadget simulating r contains polynomially many rules. 

Initialization: case i = 1 (q0, q 1) upd 

−−→ (init (r), src (q′

> 1

)) (init (r)) upd 

−−→ (cp (r), dst (q′

> 1

)) 

Initialization: case i ≥ 2(q0, . . . , q i) upd 

−−→ (init (r), q ′

> 1

, . . . , q ′

> i−2

, par (r), src (q′

> i

)) (init (r), q ′

> 1

, . . . , q ′

> i−2

, par (r)) upd 

−−→ (cp (r), q ′

> 1

, . . . , q ′

> i−2

, q ′

> i−1

, dst (q′

> i

)) 

Move down and copy: 

(cp (r), σ, x 1, . . . , x m, src (y), z ) upd 

−−→ (cpd (r, z ), σ, x 1, . . . , x m, y, src (z)) (cpd (r, z ), σ, x 1, . . . , x m, dst (y)) upd 

−−→ (cp (r), σ, x 1, . . . , x m, y, dst (z)) 

Recall that we want to copy the subtree rooted at the node vi except for the children v

of vi that are labeled with p. To do so, the above two rules are not included when 

m = 0 and z = p.

Move up and delete source: 

(cp (r), σ, x 1, . . . , x m, y, src (z)) upd 

−−→ (cpu (r), σ, x 1, . . . , x m, src (y)) (cpu (r), σ, x 1, . . . , x m, y, dst (z)) upd 

−−→ (cp (r), σ, x 1, . . . , x m, dst (y), z )

End: 

(cp (r), σ, src (y)) upd 

−−→ (end (r), σ )(end (r), σ, dst (y)) upd 

−−→ (q′

> 0

, σ, y )

Note that in the two rules above we could equivalently replace y by q′

> i

.

## 3.4 From PCA to Nested Counter Systems and back 

We show in the remainder of this section that (d, k = ∞)-Coverability for PCA is (polynomially-) equivalent to d-NCS Coverability . We start with the upper bound, by providing a reduction from coverability for PCA over trees of depth at most d to coverability for NCS of nesting depth at most d.Let A = ( SA, Act , ∆A, s init ) be a PCA and d ∈ N a positive integer. We construct a d-NCS 

N = ( QN , ∆N ), with QN = SA ∪{ start } and ∆N defined as follows. For the initialization, the 10 On Parameterized Verification Over Tree Topologies 

root is in state start and it generates the tree with a set of rules spawning new levels in state 

sinit . The initialization is finished with the root going from start to sinit : For all 0 ≤ i < d ,

(( start , s init , . . . , s init 

︸ ︷︷ ︸ 

> itimes

), (start , s init , . . . , s init 

︸ ︷︷ ︸ 

> itimes

, s init )) ∈ ∆N , and (( start ), (sinit )) ∈ ∆N .For each transition, we have one rule per tree level: For all q ↓m

−−→ q′ ∈ ∆A, s ↑m

−−→ s′ ∈ ∆A,

0 ≤ i < d , and q0, . . . , q i−1 ∈ SA, we let (( q0, . . . , q i−1, q, s ), (q0, . . . , q i−1, q ′, s ′)) ∈ ∆N ; for all 

q τ

−→ q′ ∈ ∆A, 0 ≤ i ≤ d, and q0, . . . , q i−1 ∈ SA, we have (( q0, . . . , q i−1, q ), (q0, . . . , q i−1, q ′)) ∈

∆N .This NCS simulates A for every tree of depth at most d. Let T be a tree of depth at most d. For every configuration c of A over T we can construct a configuration C of N over 

T such that h(p) = c(p). We have that a configuration c is reachable in A on trees of depth at most d iff there exists a configuration C of N simulating c that is reachable from the configuration start . So a state s is (d, k = ∞)-coverable in A iff the configuration with the root in state s is coverable from configuration start in N .

▶ Lemma 3.4. The complexity of (d, k = ∞)-Coverability for PCA (with d ∈ N) is in 

FΩd , and the complexity of (k = ∞)-Coverability (d) is in Fε0 .

For the lower bound we show how to simulate a d-NCS with a PCA on trees of depth at most d. Informally, we use each process to store one state of N , with its children representing its subconfiguration. When doing a transition, we nondeterministically synchronize a branch of the tree, making sure every node of the branch has the correct state, and then change each state along the branch. Let d ∈ N be a positive integer, N = ( QN , ∆N ) a d-NCS, and Cinit and Cfinal two configurations of N . For the reduction it is convenient to construct a PCA A with a distinct LTS for the root (see Remark 2.2). The set of states of B is QN , plus sets of intermediary states used in initialization, application of rules and termination. The transitions are obtained as follows. For every rule, we initialize the transition and choose a branch by synchronizing processes from the root, checking at the same time the states on the path. Then we resolve the transition from the process at the deepest level to the root. If the rule removes part of the configuration, we put the corresponding processes in a state sdead , in which they will not be able to synchronize anymore, cutting the subconfiguration from the rest of the tree. For the initialization, we construct the desired configuration of A from the leaves to the root. Each leaf sends the vertex it represents to its parent, and the latter waits to receive every vertex of its children before sending itself to its parent, and we repeat this up to root C .To check if the final configuration is covered, we use a similar construction. A formal definition of the transitions used in this reduction can be seen in Appendix A. The relationship between N and A can be formalised by the following notion of simulation. Let C = ( V, E, root C , h ) be a configuration of N , and c be a configuration of A over some tree T = ( P, Ch ). We say that c simulates C if all conditions below hold: 

C is a subtree of T with the same root. For every v ∈ V , c(v) = h(v).For every v ∈ P \V , c(v) = sinit , c(v) = sdead or there exists a ancestor v′ of v in P \V

such that c(v′) = sdead .For Cinit = ( V, E, root C , h ), and for every tree T that has (V, E ) as subtree, the first configuration c reachable by A over T such that c(root ) ∈ QN simulates Cinit . If T is not big enough, then root will never reach a state in QN . We also have that for every configuration 

C of N reachable from Cinit , there exists a tree T and a reachable configuration c of A over R. Delpy, A. Muscholl, and G. Sutre 11 

T that simulates C. Conversely, for any tree T , if a reachable configuration c of A over T

simulates some configuration C of N , then this configuration is reachable from Cinit . From this we get that Cfinal is coverable from Cinit in N iff sfinal is (d, k = ∞)-coverable in A.

▶ Lemma 3.5. The problem (d, k = ∞)-Coverability for PCA (with d ∈ N) is FΩd−1 -hard, and (k = ∞)-Coverability (d) is Fε0 -hard. 

## 4 Phase-bounded PCA 

In this section we present our results on the complexity of the coverability problem for phase-bounded PCA. 

## 4.1 Lower bound for phase-bounded PCA 

We start with the lower bound for (∞, k )-Coverability , so for k phases and no restriction on the depth. We show a reduction from the coverability problem for a succinct model of Petri nets, called Transducer Defined Petri Nets [ 8]. We recall that a Petri Net is given as a tuple N = ( P, T, F, p 0) with P a finite set of places, T a finite set of transitions, 

F ⊆ (P × T ) ∪ (T × P ) the flow relation, and p0 ∈ P the initial place. A marking of N is a function m ∈ P → N, and we say that there are m(p) tokens on place p. A transition 

t ∈ T is firable in a marking m if m(p) > 0 for all p such that (p, t ) ∈ F . It can then be fired, leading to the marking m′ with m′(p) = m(p) + F (t, p ) − F (p, t ) for all p ∈ P . We also write m t

−→ m′. We say that a marking m is reachable in N if there exists a sequence 

m0

> t1

−→ m1 . . . mn−1

> tn

−→ m where m0(p0) = 1 and m0(p) = 0 for all p̸ = p0. A marking 

m is coverable in N if there exists a marking m′ such that m(p) ≤ m′(p) for all p ∈ P .The question whether a marking is coverable is Expspace -complete [ 25 , 27 ], whereas the reachability of a marking is Ackermann-complete [24, 23, 14]. We fix an alphabet Σ. An n-ary transducer M = ( Q, q 0, Q f , ∆) over alphabet Σ consists of a set of states Q, an initial state q0 ∈ Q, a set of final states Qf ⊆ Q and a transition relation ∆ ⊆ Q × Σn × Q. We write q a1,...,a n

−−−−−→ q′ for a transition between q and q′. The size of M is |M| = |∆|. An n-ary transducer M defines the relation L(M) containing the n-tuples (w1, . . . , w n) of words over Σ for which there exists a sequence of transitions 

q0

> a1,1,...,a n, 1

−−−−−−−→ q1

> a1,2,...,a n, 2

−−−−−−−→ . . . a1,m ,...,a n,m 

−−−−−−−−→ qm of M with qm ∈ Qf and ai, 1 . . . a i,m = wi

for all i ∈ [1 . . . n ].

▶ Definition 4.1. A transducer-defined Petri Net ( TdPN ) N = ( ℓ, Mmove , Mjoin , Mfork , w init )

consists of an integer ℓ ∈ N, a binary transducer Mmove and two ternary transducers 

Mjoin , Mfork , all over input/output alphabet Σ, and an initial word winit ∈ Σℓ. The associ-ated Petri Net PN (N ) = ( P, T, F, p 0) has P = Σ ℓ as set of places and set T of transitions defined as disjoint union of: 

Tm = {(w, w ′) ∈ Σℓ × Σℓ | (w, w ′) ∈ L (Mmove )}

Tj = {(w, w ′, w ′′ ) ∈ Σℓ × Σℓ × Σℓ | (w, w ′, w ′′ ) ∈ L (Mjoin )} and 

Tf = {(w, w ′, w ′′ ) ∈ Σℓ × Σℓ × Σℓ | (w, w ′, w ′′ ) ∈ L (Mfork )}

The initial place of PN (N ) is p0 = winit . The semantics of transitions in terms of flow relation is: if t = ( p, p ′) ∈ Tm then (p, t ), (t, p ′) ∈ F

if t = ( p, p ′, p ′′ ) ∈ Tj then (p, t ), (p′, t ), (t, p ′′ ) ∈ F

if t = ( p, p ′, p ′′ ) ∈ Tf then (p, t ), (t, p ′), (t, p ′′ ) ∈ F12 On Parameterized Verification Over Tree Topologies 

An accepting run of any of the transducers corresponds to a single transition of PN (N ), and is called a transducer move. The size of N is defined as |N | = ℓ+|M move |+|M fork |+|M join |,with ℓ in unary encoding. 

A marking m of a TdPN N is a total function m ∈ Σℓ → N. Coverability of markings (or places) is defined as usual.           

> TdPN-Coverability
> Input: A TdPN Nand a word wfinal ∈Σℓ.
> Output: Yes if the place wfinal is coverable in PN (N).

We will use the following result: 

▶ Theorem 4.2 ([8]) . TdPN-Coverability is 2 Expspace -complete. 

By abuse of language, we call a total function m : Σ l → N a marking of N , and we say that there are m(w) tokens on place w ∈ Σl.To reduce TdPN-Coverability to (d = ∞)-Coverability( k), we construct a PCA on a rooted tree of depth 2. Informally, the root simulates the transducers and each child of the root stores a word, representing the encoding of a place of the TdPN. To do so, a node at depth 1 stores in each of its children a letter of the word, together with its position from 

[1 . . . ℓ ].Each transition of the TdPN is initiated by the root by first synchronizing with one, two or three children (depending on the step), to define their role in the transition. Then root 

will communicate each letter of the words needed for that transition to the participating children, checking at the same time the validity of the transition of the chosen transducer. At the end of the transition, children that have participated as inputs (outputs, resp.) go to state dead (full , resp.). The latter state means that such nodes are ready to be used as inputs later. At a given point, the number of children in state full with children corresponding to some word w ∈ Σℓ represents the number of tokens in the TdPN place named w.In the reduction we use a different LTS for root . This is not a restriction, see Remark 2.2. Let N = ( ℓ, Mmove , Mjoin , Mfork , w init ) be the TdPN and wfinal ∈ Σℓ the final word. We construct a PCA A(N , w final ) with two distinct LTS, Aroot for the root and A for the other processes, over the same set of actions Act . The state space of Aroot will be the disjoint union of {init r , final r , idle }, a set of states for each of the two words winit and wfinal , and one set of states per transducer. The general structure of this LTS is shown in Figure 4. The LTS A will have two tasks. The first one is for processes at level 1, and consists to store a word when asked by the root, and then send it back later. Such a process p is awakened when it synchronizes with root with the message start arg (for some 1 ≤ arg ≤ 3). Process p guesses each letter of a word of length ℓ, communicates it to one of its dormant children together with its position, and waits for the root to acknowledge the letter. After the whole word has been stored, p is in state full .Later, a level 1 process can be called by root to give its word back, when root synchronizes with it with some message start arg . It will then recover one by one the letters of the word from its children and relay the information to the root. After the word is sent, p is in state 

dead . The corresponding LTS is depicted in Figure 5. The second part of A will be for processes at level 2. Such processes will only synchronize to store a letter and a position, and wait to communicate back the corresponding letter and position. This LTS is depicted in Figure 6. Note that the states init and dead are common to both parts of this LTS. R. Delpy, A. Muscholl, and G. Sutre 13 

init r winit 

final r wfinal 

idle 

Mmove 

Mfork 

Mjoin 

↓start 1

↓start 1

Figure 4 Automaton for root. start arg means consuming a word, whereas start arg means creating a word. 

init arg , 1     

> for 1≤arg ≤3

↓(a, 1) ↑(arg , a )

∀a ∈ Σ arg , 2 . . . arg , ℓ ↓(a, ℓ )↑(arg , a )

∀a ∈ Σ

> for  1 ≤ arg  ≤ 3

full 

arg , 1

↓(a, 1) ↑(arg , a )

∀a ∈ Σ

arg , 2. . . arg , ℓ 

↓(a, ℓ )↑(arg , a )

∀a ∈ Σ

dead 

↑start arg 

> ↑start arg

Figure 5 Automaton for processes at level 1. 

init a, i dead 

∀a ∈ Σ,

0 < i ≤ ℓ

↑(a, i ) ↑(a, i )

Figure 6 Automaton for processes at level 2. 14 On Parameterized Verification Over Tree Topologies 

We say that a configuration is stable if the root is in state idle and all processes of depth 1 have their state in {init , full , dead }. These configurations represent markings of N . We say that the stable configuration c simulates marking m, noted c ∼ m, if for every word 

w ∈ Σℓ, there exist exactly m(w) processes at level 1 which have exactly one child in state 

(w[i], i ) for each 0 < i ≤ ℓ, and all their other children are in state init . An example of a stable configuration simulating m = Jab, ab, ba K can be seen in Figure 7. We give a small example of the execution of a move of a token from ab to ba in our system in Figure 8. We can show: 

▶ Lemma 4.3. A marking m is reachable in N iff there exists a stable configuration c

reachable in A(N , w final ) such that c ∼ m.

From Lemma 4.3 we know that A(N , w final ) can reach a stable configuration with some process at level 1 storing wfinal iff there exists a reachable marking with a token in wfinal in 

N . As root needs to consume wfinal as its final operation, we know that it can reach final r

iff wfinal is coverable in N .Moreover, the system A(N , w final ) is phase-bounded. Indeed, root always does only one phase and so do processes at level 2. By construction, processes at level 1 need 1 + 2 · ℓ

phases before reaching full , and then 1 + 2 · ℓ other phases to go to state dead . So they need at most 2 + 4 · ℓ phases. We can construct A(N , w final ) in polynomial time, as for Aroot we only need to copy and unwrap the transitions of each transducers into letter transitions, and then add the production of winit and consumption of wfinal . For A the number of states and transitions is polynomial in the size of Σ and ℓ. We can conclude from [8]: 

▶ Theorem 4.4. 2-Coverability( k) for PCA is 2 Expspace -hard. 

## 4.2 Upper bound for phase-bounded PCA 

In this section we show that (∞, k )-Coverability is in Expspace and that (d = ∞)-Coverability( k) is in 2Expspace . To do so, we will prove that on phase-bounded runs, the language of receives of each process is regular. We will show how to compute automata for these languages on subtrees of depth one first, and then apply the technique recursively. We define an Interface PCA (IPCA for short) as a pair A = ( Aroot , Aℓ), with the LTS 

Aroot for root , and Aℓ for non-root processes. The only difference between IPCA and PCA is that the root process can do actions in Up without a parent to synchronize with. So a run 

ρ of a IPCA A is labelled by trace (ρ) ∈ (Up ∪ Act T )∗ where trace (ρ)|p ∈ (Act T )∗ for every 

p̸ = root . Recall that Act T = {p⋊⋉q(m) | (p, q ) ∈ C, m ∈ M } ∪ { p : τ | p ∈ P } is the set of synchronous communication actions. 

idle 

full 

a, 1 b, 2

full 

a, 1 b, 2

full 

b, 1 a, 2     

> Figure 7 Configuration simulating m=Jab, ab, ba K. All unspecified processes have their state in
> {init ,dead }.

R. Delpy, A. Muscholl, and G. Sutre 15 

idle 

full 

a, 1b, 2

init 

init init 

idle 

1, 1

1, 2

dead 

2, 1

2, 2

full 

dead 

dead 

b, 1

a, 2

start 1

start 2

a, 1

1, a b, 1

2, b 

b, 2

1, b 

a, 2

2, a       

> Figure 8 Execution of a move transition from place ab to place ba . The simulation of Mmove by root is depicted in bold. Dotted edges are tree edges, full edges represent transitions.

The language of an IPCA A is the set of words w ∈ Up ∗ such that there exists some tree 

T and a run ρ of A over T with w = trace (ρ)|Up . We denote by Ld≤1 

> k

(A) the language of IPCA A over trees of depth 1 and k-phase bounded runs. We already know that (1 , ∞)-Coverability is equivalent to VASS coverability. We will now extend this observation by showing that languages of IPCA are also VASS languages. To do so, we will enrich our VASS with action transitions. Let m > 0 be a strictly positive integer and A be a finite alphabet. An m-VASS with actions from A is a tuple V = ( Q, q init , ∆V , A τ , ∆A) where Q is a finite set of states, qinit 

is the initial state, ∆V ⊆ Q × Zm × Q is a counter transition relation, Aτ = A ∪ { τ } is the alphabet and ∆A ⊆ Q × Aτ × Q is an action transition relation. The size of an m-VASS with actions V is |V| = |Q| + ∑ 

> (q, v,q ′)∈∆V

∥v∥1 + |∆A|. Configura-tions are defined as for VASS. The step relation of a m-VASS with actions is (q, x) a

−→ (q′, x′)

if either (q, a, q ′) ∈ ∆V , a = v ∈ Zm, x′ = x + v ≥ 0, or (q, a, q ′) ∈ ∆A, a ∈ Aτ , x = x′. In other words, actions (i.e., transitions from ∆A) do not affect the counters. A run of V is a sequence σ = ( q0, x0) a1

−→ (q1, x1) a2

−→ . . . an

−−→ (qn, xn) where (q0, x0)

is initial. The trace of a run σ is the sequence of actions labelling it, and is denoted as 

trace (σ) ∈ Aτ ∗. The language of a VASS with actions V is defined as L(V) = {w ∈ A∗ | w =

trace (σ)|A for some run σ}.We say that a run σ of V is k-phase bounded if there exist σ1, . . . , σ k such that σ =

σ1 · σ2 . . . σ k, and every σi has all its transitions either in ∆V or in ∆A. We define Lk(V) as the language of V restricted to k-phase bounded runs. We will now show how to construct a VASS with actions V from an IPCA A over trees of depth 1, which will simulate the interface language of A.

▶ Lemma 4.5. Let A = ( Aroot , Aℓ) be an IPCA over trees of depth one. We can construct an m-VASS with actions V = ( Q, q init , ∆V , A τ , ∆A), with Q = Sroot , m = |Sℓ|, and Aτ =

Up ∪ { τ } such that, for any k ∈ N∞, Lk(V) = Ld≤1 

> k

(A).16 On Parameterized Verification Over Tree Topologies 

Next we show how to construct a finite automaton for the language of phase-bounded runs of a VASS with actions. Let m and k be two integers. Let V be an m-VASS with actions, and σ be a k-phase bounded run of V. There exist (q1, x1), . . . , (qk, xk) such that 

σ = ( q0, x0), σ 1, (q1, x1), σ 2, (q2, x2) . . . (qk, xk), where every σi is either only on ∆V , or only on ∆A. The important observation is that every subrun σi over ∆A can be replaced by any other subrun σ′ 

> i

over ∆A starting and ending in the same states as σi, as these phases have no effect on the counters. 

▶ Lemma 4.6. Let m and k be two integers. Let V = ( Q, q init , ∆V , A, ∆A) be an m-VASS with actions. There exists an automaton B with O(k ×(2 ×| Q|)k+1 ) states such that L(B) = Lk(V),that can be constructed in 2O(m) · log( k × |V| )-space. 

Proof. Let t = ( q0, d 1, q 1, d 2 . . . , d k, q k) be a sequence of states and transition types, with 

qi ∈ Q and di ∈ { V, A } for each 0 < i ≤ k. We say that t is valid if there exists a run 

σ = ( q0, x0), σ 1, (q1, x1), σ 2, (q2, x2), . . . , σ k, (qk, xk) where for each 0 < i ≤ k, subrun σi is only over ∆di . In that case we say that σ complies with t.For every valid sequence t, we will construct a finite automaton Bt which language is the set of words w ∈ A∗ such that there exists a run σ = ( q0, x0), σ 1, (q1, x1), σ 2, (q2, x2) . . . (qk, xk)

complying with t, and with w = trace (σ)|A.Let t = ( q0, d 1, q 1, d 2 . . . , d k, q k) be a valid sequence. We construct Bt = ( QBt , q init  

> Bt

, ∆Bt , q final  

> Bt

),with QBt the set of states, qinit  

> Bt

∈ QBt the initial state, ∆Bt ⊆ QBt × Aτ × QBt the transition relation, and qfinal  

> Bt

∈ QBt the final state, as follows: 

QBt = Q × k, with (q, i ) ∈ QBt meaning that the automaton is in state q and is simulating the i-th phase of a run. 

qinit  

> Bt

= ( q0, 1) .For each 0 < i ≤ k, if di = V , then (qi−1, i ) τ

−→ (qi, i ) ∈ ∆Bt , otherwise if di = A, then for all q a

−→ q′ ∈ ∆A, we have (q, i ) a

−→ (q′, i ) ∈ ∆Bt . Moreover, we have (qi−1, i − 1) τ

−→

(qi−1, i ) ∈ ∆Bt .

qfinal  

> Bt

= ( qk, k ).A run of Bt is a sequence ρ = q0

> a1

−→ q1 . . . q n−1

> an

−−→ qn where q1 = qinit  

> Bt

, qn = qfinal  

> Bt

, and for each 0 < i ≤ n, qi−1

> ai

−→ qi ∈ ∆Bt . We denote by trace (ρ) ∈ Aτ the trace of a run 

ρ. The language of Bt is the set L(Bt) = {w | w = trace (ρ)|A for some run ρ of Bt}. Now we show that L(Bt) is exactly the set of all projections on A of runs of V complying with 

t. Let σ = ( q0, x0), σ 1, (q1, x1), σ 2, (q2, x2) . . . (qk, xk) be a run complying with t, and let 

w = trace (σ)|A. There exist w1, . . . , w k such that wi = trace (σi)|A. We have that if di = V

then wi = ε. Then when di = A, one can read wi between (qi−1, i ) and (qi, i ) in Bt by construction, and when di = V , one can go from state (qi−1, i ) to state (qi, i ) in Bt reading 

τ (as when di = V then wi = ε). Moreover, one can change phases through transitions 

(qi−1, i − 1) τ

−→ (qi−1, i ). So w is accepted by Bt.Now let w ∈ L(Bt). There exists some run ρ of Bt such that trace (ρ)|A = w. By construction, there exist ρ1, . . . , ρ k such that ρ = ρ1 · ρ2 . . . ρ k, where for each 0 < i ≤ k if 

di = V , then ρi = ( qi−1, i − 1) τ

−→ (qi−1, i ) τ

−→ (qi, i ), and if di = A then ρi = ( qi−1, i − 1) τ

−→

(qi−1, i ) ui

−→ (qi, i ) for some sequence of actions ui ∈ Aτ ∗. By construction, ui can be read in 

V between any two configurations (qi−1, x) and (qi, x). And as t is valid, one can construct a k-bounded run σ = ( q0, x0), σ 1, (q1, x1), σ 2, (q2, x2) . . . (qk, xk) where for each 0 < i ≤ k,if di = V , then σi makes only counter updates, and if di = A then trace (σi) = ui. So σ is complying with t and trace (σ)|A = w.Finally we construct B by taking the disjoint union of every Bt, over all valid sequences t.This automaton has at most (k × | Q|) × (2 × | Q|)k states. R. Delpy, A. Muscholl, and G. Sutre 17 

Finally we can check if a sequence t = ( q1, d 1, q 2, . . . , d k−2, q k) is valid through a reduction to a coverability question on an m-VASS with |Q|× k states. The details are in Appendix B.1. As coverability in an n-VASS U can be checked in 2O(n) × log (|U| )-space (see [ 22 ]), we can check if t = ( q1, d 1, q 2, . . . , q k) is valid in 2O(m) × log( k × |V| )-space. ◀

Using the above reasoning we can check if a state of a PCA is (d = ∞, k )-coverable, so for arbitrary depth. First we demonstrate how to use the technique for depth 2. Let k > 0 be a phase bound, A = ( S, Act , ∆, s init ) a PCA, and s ∈ S a state. By Lemma 4.5 we can simulate the language of any process at depth 1 on k-phase-bounded runs using an m-VASS with actions V = ( S, s init , ∆V , A τ , ∆A), where m = |S|, Aτ = Up ∪ { τ }, that can be constructed in polynomial time. Moreover, using Lemma 4.6, we can replace V by a finite automaton B of size 

O(k × (2 |S|)k+1 ) that can be built in 2O(|S|) × log (O(k × | S| × | ∆|)) -space. So we can reduce 

(2 , k )-coverability to (1 , k )-coverability . Using the same technique as for the upper bound of Lemma 3.1, we can check if s is coverable in 2O(k×(2 |S|)k+1 ) × log( O(|S|)) -space. If we want to apply the same technique for depth 3, we first replace all processes of depth 

2 by automata of size O(k × (2 |S|)k+1 ), and then we transform all processes of depth 1 into 

m-VASS with actions where m = O(k × (2 |S|)k+1 ), and which statespace is still S. Using Lemma 4.6 again we can construct a finite automaton of size O(k × (2 |S|)k+1 ) which can be built in 2O(k×(2 |S|)k+1 ) × log (O(k × | S| × | ∆|)) -space. Then one can check coverability of s

in 2O(k×(2 |S|)k+1 ) × log( O(|S|)) -space again. One can see that repeating the process of compression does not require more space after depth 3, as the automaton produced will always be of size O(k × (2 |S|)k+1 ). Moreover, as the size of B is bounded, we know that if s is coverable with k-bounded runs, it is coverable with k-bounded runs over a tree of depth at most O(k × (2 |S|)k+1 ).We can conclude: 

▶ Theorem 4.7. The problem (d = ∞, k )-Coverability for PCA (with d ∈ N) is Expspace -complete. If k is encoded in unary, then the problem (d = ∞)-Coverability( k) is 

2Expspace -complete. 

## 5 Conclusion 

We investigated two kinds of bounds under which we determine the complexity of safety checking of systems with rendez-vous synchronization over tree topologies. When bounding the depth we obtained that the complexity is related to the fast growing hierarchy. The second bound on the phases led to complexity Expspace -complete if the number of phases is fixed, resp. 2 Expspace -complete if the number of phases is part of the input. An interesting question left open is whether given d, k and a PCA A, if all executions of A over d-bounded trees are k phase-bounded. A further natural question is to close the complexity gap when the depth is fixed. 

References    

> 1Parosh Aziz Abdulla, Noomene Ben Henda, Giorgio Delzanno, Frédéric Haziza, and Ahmed Rezine. Parameterized tree systems. In Kenji Suzuki, Teruo Higashino, Keiichi Yasumoto, and Khaled El-Fakih, editors, Formal Techniques for Networked and Distributed Systems - FORTE 2008, 28th IFIP WG 6.1 International Conference, Tokyo, Japan, June 10-13, 2008, Proceedings , Lecture Notes in Computer Science, pages 69–83. Springer, 2008. doi: 10.1007/978-3-540-68855-6\_5 .

18 On Parameterized Verification Over Tree Topologies 

2 Parosh Aziz Abdulla, Bengt Jonsson, Pritha Mahata, and Julien d’Orso. Regular tree model checking. In Ed Brinksma and Kim Guldstrand Larsen, editors, Computer Aided Verification, 14th International Conference, CAV 2002,Copenhagen, Denmark, July 27-31, 2002, Proceedings , Lecture Notes in Computer Science, pages 555–568. Springer, 2002. doi: 10.1007/3-540-45657-0\_47 .

3 Benjamin Aminof, Tomer Kotek, Sasha Rubin, Francesco Spegni, and Helmut Veith. Paramet-erized model checking of rendezvous systems. Distributed Comput. , 31(3):187–222, 2018. URL: 

https://doi.org/10.1007/s00446-017-0302-6 , doi:10.1007/S00446-017-0302-6 .

4 Krzysztof R. Apt and Dexter Kozen. Limits for automatic verification of finite-state concurrent systems. Inf. Process. Lett. , 22(6):307–309, 1986. doi:10.1016/0020-0190(86)90071-2 .

5 A. R. Balasubramanian. Complexity of coverability in depth-bounded processes. In Bartek Klin, Slawomir Lasota, and Anca Muscholl, editors, 33rd International Conference on Concurrency Theory, CONCUR 2022, Warsaw, Poland, September 12-16, 2022 , LIPIcs, pages 17:1–17:19. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2022. URL: https://doi.org/10.4230/ LIPIcs.CONCUR.2022.17 , doi:10.4230/LIPICS.CONCUR.2022.17 .

6 A. R. Balasubramanian, Javier Esparza, and Mikhail A. Raskin. Finding cut-offs in leaderless rendez-vous protocols is easy. Log. Methods Comput. Sci. , 19(4), 2023. URL: https://doi. org/10.46298/lmcs-19(4:2)2023 , doi:10.46298/LMCS-19(4:2)2023 .

7 A.R. Balasubramanian and Franzisco Schmidt. The complexity of nested reset counter systems. In 41st Annual ACM/IEEE Symposium on Logic in Computer Science, LICS 2026, Lisbon, 20-23 July, 2026 , LIPIcs. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2026. To appear. 

8 Pascal Baumann, Rupak Majumdar, Ramanathan S. Thinniyam, and Georg Zetzsche. The complexity of bounded context switching with dynamic thread creation. In Artur Czumaj, Anuj Dawar, and Emanuela Merelli, editors, 47th International Colloquium on Automata, Languages, and Programming, ICALP 2020, Saarbrücken, Germany (Virtual Conference), July 8-11, 2020 , volume 168 of LIPIcs , pages 111:1–111:16. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2020. URL: https://doi.org/10.4230/LIPIcs.ICALP.2020.111 ,

doi:10.4230/LIPICS.ICALP.2020.111 .

9 Roderick Bloem, Swen Jacobs, Ayrat Khalimov, Igor Konnov, Sasha Rubin, Helmut Veith, and Josef Widder. Decidability of Parameterized Verification . Synthesis Lectures on Distributed Computing Theory. Morgan & Claypool Publishers, 2015. doi:10.2200/ S00658ED1V01Y201508DCT013 .

10 Ahmed Bouajjani, Peter Habermehl, Adam Rogalewicz, and Tomás Vojnar. Abstract regular (tree) model checking. Int. J. Softw. Tools Technol. Transf. , 14(2):167–191, 2012. URL: 

https://doi.org/10.1007/s10009-011-0205-y , doi:10.1007/S10009-011-0205-Y .

11 Marius Bozga, Javier Esparza, Radu Iosif, Joseph Sifakis, and Christoph Welzel. Structural invariants for the verification of systems with parameterized architectures. In Armin Biere and David Parker, editors, Tools and Algorithms for the Construction and Analysis of Systems - 26th International Conference, TACAS 2020, Held as Part of the European Joint Conferences on Theory and Practice of Software, ETAPS 2020, Dublin, Ireland, April 25-30, 2020, Proceedings, Part I , Lecture Notes in Computer Science, pages 228–246. Springer, 2020. doi:10.1007/ 978-3-030-45190-5\_13 .

12 Marius Bozga and Radu Iosif. Specification and safety verification of parametric hierarchical distributed systems. In Gwen Salaün and Anton Wijs, editors, Formal Aspects of Component Software - 17th International Conference, FACS 2021, Virtual Event, October 28-29, 2021, Proceedings , Lecture Notes in Computer Science, pages 95–114. Springer, 2021. doi:10.1007/ 978-3-030-90636-8\_6 .

13 Daniel Brand and Pitro Zafiropulo. On communicating finite-state machines. J. ACM ,30(2):323–342, 1983. doi:10.1145/322374.322380 .

14 Wojciech Czerwinski and Lukasz Orlikowski. Reachability in vector addition systems is ackermann-complete. In 62nd IEEE Annual Symposium on Foundations of Computer Science, R. Delpy, A. Muscholl, and G. Sutre 19 

FOCS 2021, Denver, CO, USA, February 7-10, 2022 , pages 1229–1240. IEEE, 2021. doi: 10.1109/FOCS52979.2021.00120 .

15 Normann Decker and Daniel Thoma. On freeze LTL with ordered attributes. In International Conference on Foundations of Software Science and Computation Structures , pages 269–284. Springer, 2016. 

16 Normann Decker and Daniel Thoma. On freeze LTL with ordered attributes, 2016. URL: 

https://arxiv.org/abs/1504.06355 , arXiv:1504.06355 .

17 Allen E. Emerson and Kedar S. Namjoshi. On reasoning about rings. International Journal of Foundations of Computer Science , 14(04):527–549, 2003. 

18 Javier Esparza. Keeping a crowd safe: On the complexity of parameterized verification (invited talk). In Ernst W. Mayr and Natacha Portier, editors, 31st International Symposium on Theoretical Aspects of Computer Science, STACS 2014, Lyon, France, March 5-8, 2014 ,LIPIcs, pages 1–10. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2014. URL: https: //doi.org/10.4230/LIPIcs.STACS.2014.1 , doi:10.4230/LIPICS.STACS.2014.1 .

19 Steven M. German and Prasad A. Sistla. Reasoning about systems with many processes. 

J. ACM , 39(3):675–735, 1992. 

20 Lucie Guillou, Arnaud Sangnier, and Nathalie Sznajder. Phase-bounded broadcast net-works over topologies of communication. In Rupak Majumdar and Alexandra Silva, ed-itors, 35th International Conference on Concurrency Theory, CONCUR 2024, Calgary, Canada, September 9-13, 2024 , LIPIcs, pages 26:1–26:16. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2024. URL: https://doi.org/10.4230/LIPIcs.CONCUR.2024.26 ,

doi:10.4230/LIPICS.CONCUR.2024.26 .

21 Yonit Kesten, Oded Maler, Monica Marcus, Amir Pnueli, and Elad Shahar. Symbolic model checking with rich assertional languages. Theor. Comput. Sci. , 256(1-2):93–112, 2001. 

doi:10.1016/S0304-3975(00)00103-1 .

22 Marvin Künnemann, Filip Mazowiecki, Lia Schütze, Henry Sinclair-Banks, and Karol Wegrzycki. Coverability in VASS revisited: Improving Rackoff’s bounds to obtain condi-tional optimality. J. ACM , 72(5):33:1–33:27, 2025. doi:10.1145/3762178 .

23 Jérôme Leroux. The reachability problem for Petri nets is not primitive recursive. In 62nd IEEE Annual Symposium on Foundations of Computer Science, FOCS 2021, Denver, CO, USA, February 7-10, 2022 , pages 1241–1252. IEEE, 2021. doi:10.1109/FOCS52979.2021.00121 .

24 Jérôme Leroux and Sylvain Schmitz. Reachability in vector addition systems is primitive-recursive in fixed dimension. In 34th Annual ACM/IEEE Symposium on Logic in Computer Science, LICS 2019, Vancouver, BC, Canada, June 24-27, 2019 , pages 1–13. IEEE, 2019. 

doi:10.1109/LICS.2019.8785796 .

25 Richard J. Lipton. The reachability problem requires exponential space. Technical Report 62, Yale University, 1976. URL: http://cpsc.yale.edu/sites/default/files/files/tr63.pdf .

26 Irina A. Lomazova and Philippe Schnoebelen. Some decidability results for nested Petri nets. In International Andrei Ershov Memorial Conference on Perspectives of System Informatics ,pages 208–220. Springer, 1999. 

27 Charles Rackoff. The covering and boundedness problems for vector addition systems. Theor-etical Computer Science , 6(2):223–231, 1978. 

28 Sylvain Schmitz. Complexity hierarchies beyond elementary. ACM Transactions on Computa-tion Theory (TOCT) , 8(1):1–36, 2016. 20 On Parameterized Verification Over Tree Topologies 

## A Formal construction for the reduction of Coverability for NCS to Coverability for PCA 

For every r = (( q0, . . . , q i), (q′

> 0

, . . . , q ′ 

> j

)) ∈ ∆N , we initialize the transition and choose a branch by synchronizing processes from the root: If i = j = 0 , then q0

> τ

−→ q′ 

> 0

∈ ∆root , otherwise q0

> ↓(r, 1)

−−−→ (r, 0) ∈ ∆root .For every 0 < k < i , qk

> ↑(r,k )

−−−−→ ↓(r,k +1) 

−−−−−→ (r, k ) ∈ ∆A.If i ≥ j, then qi

> ↑(r,i )

−−−→ (r, i ) ∈ ∆A.If i < j , then qi

> ↑(r,i )

−−−→ ↓(r,i +1) 

−−−−−→ (r, i ) ∈ ∆A; for every i < k < j , sinit ↑(r,k )

−−−−→ ↓(r,k +1) 

−−−−−→

(r, k ) ∈ ∆A. Finally, sinit ↓(r,j )

−−−→ (r, j ) ∈ ∆A.Then we resolve the transition from the process at the deepest level to the root: If i > j , then (r, i ) ↑(ok )

−−−→ sdead ∈ ∆A; for every j < k < i (r, k ) ↓(ok )

−−−→ ↑(ok )

−−−→ sdead ∈ ∆A.Finally, if j̸ = 0 , then (r, j ) ↓(ok )

−−−→ ↑(ok )

−−−→ q′ 

> j

∈ ∆A.If i ≤ j, then (r, j ) ↑(ok )

−−−→ q′ 

> j

∈ ∆A.In both cases, for every 0 < k < j , we have (r, k ) ↓(ok )

−−−→ ↑(ok )

−−−→ q′ 

> k

∈ ∆A, and (r, 0) ↓(ok )

−−−→

q′ 

> 0

∈ ∆root .When a process reaches state sdead , it will not be able to synchronize anymore, cutting its subconfiguration from the rest of the tree. For the initialization, let Cinit = ( V, E, root C , h ) be the initial configuration of the d-NCS. We first construct a corresponding configuration in A from the leaves to the root. The leaves will send the vertex they represent in V to their parent, that will wait to receive every vertex of its children before sending itself to its parent, and we repeat this up to root C :For every leaf v ∈ V , sinit ↑(init ,v )

−−−−−→ h(v) ∈ ∆A.For every other v̸ = root C , let v1, . . . , v n be the children of v, we have sinit ↓(init ,v 1)

−−−−−−→

. . . ↓(init ,v n)

−−−−−−→ ↑(init ,v )

−−−−−→ h(v) ∈ ∆A.For root C let v1, . . . , v n be the children of root C , we have sinit ↓(init ,v 1)

−−−−−−→ . . . ↓(init ,v n)

−−−−−−→

h(root C ) ∈ ∆root .Once a process is chosen to be root C , it will only be allowed to do synchronizations with its children. This way we ensure that for any tree T , if any other process than root is chosen as 

root C , its subconfiguration will be isolated from the rest of the tree (see Remark 2.2). To check if the configuration Cfinal = ( V, E, root C , h ) is covered, we use a similar con-struction as initialization: For every leaf v ∈ V , h(v) ↑(init ,v )

−−−−−→ sdead ∈ ∆A.For every other v̸ = root C , let v1, . . . , v n be the children of v, we have h(v) ↓(init ,v 1)

−−−−−−→

. . . ↓(init ,v n)

−−−−−−→ ↑(init ,v )

−−−−−→ sdead ∈ ∆A.For root C let v1, . . . , v n be the children of root C , we have h(root C ) ↓(init ,v 1)

−−−−−−→ . . . ↓(init ,v n)

−−−−−−→

sfinal ∈ ∆root .

## B Proofs of Subsection 4.2 

▶ Lemma 4.5. Let A = ( Aroot , Aℓ) be an IPCA over trees of depth one. We can construct an m-VASS with actions V = ( Q, q init , ∆V , A τ , ∆A), with Q = Sroot , m = |Sℓ|, and Aτ =

Up ∪ { τ } such that, for any k ∈ N∞, Lk(V) = Ld≤1 

> k

(A).R. Delpy, A. Muscholl, and G. Sutre 21 

Proof. Let A = ( Aroot , Aℓ) be an IPCA. We will construct an m-VASS, for m = |Sℓ|, by simulating up synchronizations of root in A as ∆A transitions. As usual, we use the counters to store how many children of the root are in each state, simulating down synchronizations from 

root as vector operations. To avoid adding an extra phase at initialisation, we do not track the number of children processes in the initial state. We define V = ( Q, q init , ∆V , A τ , ∆A)

of dimension m over alphabet Aτ = Up ∪ { τ } as follows. We set Q = Sroot . For s ∈ Σℓ we denote vs ∈ ZSℓ the vector with vs[s] = 1 and vs[s′] = 0 for s′̸ = s.The transition relation is defined as follows: For all (s, ↑m, s ′) ∈ ∆root we let (s, ↑m, s ′) ∈ ∆A.For all (sroot , ↓m, s ′

> root

) ∈ ∆root and for all pairs of states (sinit  

> ℓ

, s ′

> ℓ

) of Aℓ such that 

sinit  

> ℓτ∗

−→ ↑m

−−→ τ ∗

−→ s′ 

> ℓ

is a path in Aℓ, we let (sroot , v, s ′

> root

) ∈ ∆V , with v = vs′ 

> ℓ

.For all (sroot , ↓m, s ′

> root

) ∈ ∆root and for all pair of states (sinit  

> ℓ

, s ′

> ℓ

) of Aℓ such that sℓ̸ = sinit 

> ℓ

and sℓτ ∗

−→ ↑m

−−→ τ ∗

−→ s′ 

> ℓ

is a path in Aℓ, we let (sroot , v, s ′

> root

) ∈ ∆V , with v = vs′ 

> ℓ

− vsℓ .For all (s, τ, s ′) ∈ ∆root , we let (s, τ, s ′) ∈ ∆A and (s, 0, s ′) ∈ ∆V .Let T be a tree of depth one and c a configuration of A over T . We say that (q, x) simulates 

c, denoted as (q, x) ∼ c, if c(root ) = q and for every state s ∈ S ℓ\{ sinit  

> ℓ

}, |c−1(s)| = x(s).We will now show that Lk(V) = Ld≤1 

> k

(A). First, let w ∈ Ld≤1 

> k

(A). Let T be a tree of depth 1 such that there exists a k-bounded run ρ = ρ1 · ρ2 . . . ρ k of A over T with 

w = trace (ρ)|Up . We suppose wlog. that for every process p̸ = root and for every i ≤ k, if 

trace (ρi)|Up ̸ = ε, then p does not act in ρi. Moreover, if p does not synchronize with root ,then it stays in state sinit  

> ℓ

. If this is not the case, ρ can be modified and still produce the same word w without changing the number of phases. We will construct a k-bounded run σ

of V such that trace (σ)|Up = w. Initially, every child process of the root is in state sinit  

> ℓ

, so 

(sinit 

> root

, 0) ∼ cinit  

> A,T

. We will show that for each ρi, we can construct a subrun σi of V such that if ρi starts in configuration c and ends in configuration c′, then σi starts in configuration 

(q, x) ∼ c and ends in configuration (q′, x′) ∼ c′.First, suppose that trace (ρi)|Up ̸ = ε, and ρi starts in c and ends in c′. Let (s, x) be a configuration of V that simulates c. We know that for every action a = ↑m of ρi, the only moving process is root . Let s be the state of root before a, and s′ the state of root after a.We know there exists a transition (s, ↑m, s ′) ∈ ∆A, so we can replace every action of ρi by an action of V, and construct a subrun σi starting in (s, x) and ending in (s′, x) ∼ c′.Now suppose that trace (ρi)|Up = ε, and ρi starts in c and ends in c′. For each p̸ = root , we can suppose wlog. that for some n ∈ N we have ρi = ρ′ 

> 1

· ρ′ 

> 2

. . . ρ ′

> n

, where for all j ≤ n there is some p̸ = root with trace (ρ′ 

> j

)|p ∈ τ ∗ · ↑ m · τ ∗ for some m ∈ M , and for every p′̸ = p, root ,

trace (ρ′ 

> j

)|p′ = ε. If this is not the case, ρi can be reordered and still reach c′. Now, let c′ 

> j

and 

c′ 

> j+1

be the configurations before and after ρ′ 

> j

. We know that in ρ′ 

> j

only root and one process 

p moves. Moreover, p will only send one message in this subrun. So we can replace ρ′ 

> j

by the transition (sroot , v, s ′

> root

) ∈ ∆V with c′ 

> j

(root ) = s, c′ 

> j+1

= s′, c′ 

> j

(p) = sℓ, c′

> j+1

(p) = s′

> ℓ

, and 

v = vs′ 

> ℓ

− vsℓ .We can then construct a run σi by replacing each ρ′ 

> j

by one action, and if σi starts in a configuration simulating c, it will end in a configuration simulating c′.Finally, each σi we constructed is exclusively over ∆A or over ∆V , so the run σ =

σ1 · σ2 . . . σ k is k-phase bounded. ◀22 On Parameterized Verification Over Tree Topologies 

## B.1 Checking validity of a sequence in proof of Lemma 4.6 

Let t = ( q1, d 1, q 2, . . . , d k−2, q k) be a sequence of states and transition types. As explained below, checking if t is valid is reducible to a coverability question on an m-VASS with |Q| × k

states. Let Ut = ( QUt , q init  

> Ut

, ∆Ut ) be the following m-VASS: 

QUt = Q × k.

qinit  

> Ut

= ( qinit , 1) 

For each 0 < i ≤ k, if di = V , then for all q v

−→ q′ ∈ ∆V , we have (q, i ) v

−→ (q′, i ) ∈ ∆Ut .Otherwise, if di = A, then for all q a

−→ q′ ∈ ∆A, we have (q, i ) 0

−→ (q′, i ) ∈ ∆Ut . Moreover, we have (qi−1, i − 1) 0

−→ (qi−1, i ) ∈ ∆Ut .We show that the sequence t is valid iff the configuration (( qk, k ), 0) is coverable in 

Ut. Suppose that t is valid. Then there exists a run σV of V that complies with t. Let 

σV = ( q0, x0), σ V 

> 1

, (q1, x1), σ V 

> 2

, (q2, x2) . . . (qk, xk) with every σV 

> i

only on ∆di . We construct a run σU on Ut by doing the same counter updates done in σV 

> i

when di = V , and by replacing every action transitions by 0 transitions when di = A. Moreover, at the end of each σV 

> i

, we fire the transition (qi−1, i − 1) 0

−→ (qi−1, i ). This run will reach a configuration (( qk, k ), x),which covers (( qk, k ), 0).Now suppose that we have a run σU that covers (( qk, k ), 0) in Ut. There exist σU 

> 1

, . . . , σ U

> k

such that σU = ( q0, 0, x0) σU

> 1

−−→ (q1, 0, x1) 0

−→ (q1, 1, x1) σU

> 1

−−→ . . . σU

> k

−−→ (qk, k −1, xk) 0

−→ (qk, k, xk).By construction, for each 0 < i ≤ k, if di = V , one can do the same counter updates between states (q, i ) and (q′, i ) on Ut as between states q and q′ of V. So if configuration (( qi, i ), xi) is reachable from (( qi−1, i ), xi−1) in Ut, then configuration (qi, xi) is reachable from (qi−1, xi−1)

in V doing the same counter updates. Now if di = A, then xi−1 = xi, and by construction if (( qi, i ), xi−1) is reachable from (( qi−1, i ), xi−1) in Ut, then (qi, xi−1) is reachable from 

(qi−1, xi−1) doing only action transitions. So we can construct a run of V of the form 

σV = ( q0, x0), σ V 

> 1

, (q1, x1), σ V 

> 2

, (q2, x2) . . . (qk, xk) where every σi is only on ∆di . Then σV

complies with t.