---
title: "HighTide: An Agent-Curated Open-Source VLSI Benchmark Suite"
title_zh: HighTide：一个由智能体策划的开源VLSI基准测试套件
authors: "Benjamin Goldblatt, Paolo Pedroso, Farhad Modaresi, Ethan Sifferman, Matthew R. Guthaus"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.04126v1"
tags: ["query:llm-hw-fv"]
score: 8.0
evidence: AI辅助的VLSI基准套件，包含RTL编译验证
tldr: 开源VLSI基准测试套件缺乏多样性和AI辅助，HighTide通过整合多语言多节点设计、采用Bazel增量编译与远程缓存，并引入12种智能体技能进行设计策展，实现了具有RTL编译验证的稳定发布。其决策日志作为长期记忆支持持续调优，套件公开且可扩展，助力开源硬件生态发展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04126v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 949, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04126v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04126v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 888, \"height\": 483, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04126v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 875, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04126v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1834, \"height\": 899, \"label\": \"Table\"}]"
motivation: 现有开源VLSI基准测试缺乏AI辅助和多样性，难以适应快速演进的硬件生态。
method: 构建涵盖多语言多节点的套件，使用Bazel增量编译与远程缓存，并引入12种AI智能体技能覆盖设计生命周期。
result: 实现了具有RTL编译验证的稳定发布，智能体策展的决策日志形成长期记忆，支持持续调优。
conclusion: HighTide作为开放套件，随开源硬件生态演进，推动VLSI设计自动化和协作。
---

## 摘要
我们推出了HighTide，一个不断演进的AI辅助基准测试套件。具体贡献包括：（i）涵盖多种设计语言和技术节点的多样化开源套件，（ii）基于Bazel的增量RTL到GDS编译，并带有远程缓存，（iii）通过十二种智能体技能进行AI辅助的设计策划，这些技能覆盖设计生命周期、流程优化、工具参考和元维护，并由每个设计的决策日志支持，这些日志作为跨套件调优理由的长期记忆，以及（iv）具有RTL编译验证的基础设施，用于稳定发布。该套件公开可用，并旨在与开源硬件生态系统共同成长。

## Abstract
We introduce HighTide, an evolving AI-assisted benchmark suite. Specifically, the contributions are: (i) a diverse open-source suite spanning multiple design languages and technology nodes, (ii) Bazel-based incremental RTL-to-GDS compilation with remote caching, (iii) AI-assisted design curation through twelve agent skills covering the design lifecycle, flow optimization, tool reference, and meta-maintenance, backed by per-design decision logs that serve as long-term memory of tuning rationale across the suite, and (iv) an infrastructure with RTL compilation verification for stable releases. The suite is publicly available and designed to grow with the open-source hardware ecosystem.

---

## 论文详细总结（自动生成）

好的，这是根据您提供的论文内容，生成的结构化、深入、客观的中文总结。

### 论文核心问题与整体含义（研究动机和背景）

*   **核心问题**：现有开源VLSI基准测试套件（如OpenROAD Flow Scripts (ORFS)、CircuitOps、EDALearn等）存在**严重的多样性和时效性不足**的问题。它们过度集中于RISC-V CPU和过时的设计，缺乏对现代异构SoC（如ML加速器、NoC、GP-GPU）中多样化IP核的覆盖。同时，这些套件通常是静态快照，无法跟踪上游硬件和EDA工具的更新，导致在其上训练的机器学习（ML）模型可能过时，难以泛化到现代设计。
*   **整体含义**：为了推动ML for EDA（电子设计自动化）领域的发展和EDA工具本身的评估，需要一个**现代、多样、动态更新且支持多种设计语言**的基准测试套件。HighTide应运而生，旨在通过AI辅助策展，创建一个能够随着开源硬件生态系统共同演进的评估平台。

### 论文提出的方法论

*   **核心思想：AI辅助的动态策展**
    HighTide不是一个静态的设计集合，而是一个**由AI智能体（Agent）持续维护和扩展**的活体套件。其核心在于利用AI减轻维护负担，并通过智能体的决策日志实现知识的跨设计复用。

*   **关键技术细节**：
    1.  **多样化的设计库**：包含从<20k到>1.5M cell范围的16个设计，覆盖RISC-V CPU、ML加速器、NoC、GP-GPU、外设控制器等多种架构，并支持SystemVerilog、Chisel、Python-based Generator等多种硬件描述语言。
    2.  **Bazel-based增量编译与远程缓存**：
        *   使用 `bazel-orfs` 集成，将RTL-to-GDS流程划分为独立的构建目标。
        *   仅重新编译输入发生变化的阶段，大幅减少大型设计的运行时间。
        *   提供公共的远程缓存，允许用户获取预编译的工具和基线结果，实现零本地计算。
    3.  **AI智能体技能（Agent Skills）**：定义了12种结构化提示（prompt），指导AI编码代理（Claude Code）执行复杂硬件设计工作。分为四类：
        *   **设计生命周期（Design Lifecycle）**：`find-designs`（发现新设计）、`new-design`（集成设计）、`port-design`（跨平台移植）、`update-design`（跟踪上游更新）。
        *   **流程优化（Flow Optimization）**：`debug-design`（诊断构建失败）、`optimize-ppa`（优化功耗、性能、面积）、`generate-sram`（生成内存宏）。
        *   **工具参考（Tool Reference）**：`openroad-tcl`、`sdc-sta`（提供EDA工具命令缓存）。
        *   **元维护（Meta-Maintenance）**：`track-bug`（记录上游工具bug）、`update-results`（更新公开结果）、`improve-skills`（“做梦”，从历史会话中学习并改进自身技能）。
    4.  **跨设计决策记忆**：
        *   每个设计都配有 `DECISIONS.md` 日志，记录非直观的配置选择及其原因。
        *   这种“长期记忆”使得针对一个设计的调试或优化经验，能在遇到结构相似的其它设计时被自动复用。

### 实验设计

*   **使用的“数据集”**：HighTide本身就是一个基准测试套件，其“数据集”包括：
    *   **16个核心设计**：涵盖不同功能和复杂度，如Snitch Cluster、Gemmini、NVDLA等。
    *   **3个技术节点**：ASAP7（7nm）、NanGate45（45nm）、SkyWater 130nm（130nm），以验证跨平台可移植性。
*   **Benchmark**：本文不进行传统意义上的对比实验，而是将HighTide自身的特性与现有套件（ORFS、EDALearn等）进行定性对比。
*   **对比方法**：主要通过与现有套件的**静态、缺乏多样性、更新停滞**等特征进行对比，突出HighTide的**动态、多样、AI辅助策展**的优势。文中没有进行“方法A vs 方法B”在特定任务上的定量性能对比。

### 资源与算力

*   论文中**未明确说明**训练或运行整个套件所需的GPU型号、数量或时长。
*   文中提到的基础设施是**Bazel远程缓存和Kubernetes分布式执行**。远程缓存供用户读取，写入需凭证；Kubernetes用于并行构建多个设计。这暗示了算力支持，但未量化。AI智能体本身（Claude Code）是由Anthropic提供服务的，其算力消耗也未被讨论。

### 实验数量与充分性

*   **实验数量**：**缺乏传统的对比实验**。论文主要展示的是**HighTide套件本身的构建和设计多样性分析**。例如：
    *   展示了3个设计（CNN, Eyeriss v2, Gemmini）的版图放置视图（Figure 2）。
    *   分析了多个设计的面积构成（宏、时序、组合、缓冲/反相器），展示了结构多样性（Figure 3）。
    *   提及了智能体技能在套件增长中的作用（如将设计移植到新PDK）。
*   **充分性与客观性**：
    *   **不充分**：作为一篇提出新基准套件的论文，其**对套件的验证不够充分**。例如，没有展示在HighTide上运行任务（如ML模型训练或EDA工具评估）的实验结果，以证明其多样性和时效性确实能带来好处（如更好的模型泛化性或工具鲁棒性）。
    *   **客观性**：对自身套件的分析和优点描述是客观的，但对现有套件的批评也基本属实。然而，缺乏定量的性能基准测试，使得其声称的“更好”缺乏有力证据。

### 论文的主要结论与发现

*   HighTide成功构建了一个**现代、多样、动态更新**的开源VLSI基准测试套件，填补了现有套件的空白。
*   **AI智能体技能**（尤其是决策日志和元维护技能）被证明能有效降低套件维护的人力成本，加速新设计的集成和跨平台移植。
*   **Bazel构建系统**和**远程缓存**的组合，为大型VLSI设计提供了高效、可复现且可扩展的编译和运行环境。

### 优点

1.  **理念先进**：提出了“活体”基准套件的概念，通过AI策展实现持续演进，这比静态快照更适合现代硬件开发。
2.  **系统设计优秀**：Bazel、远程缓存和Kubernetes的组合设计精良，解决了大型VLSI项目构建和复现的痛点。
3.  **AI应用创新**：将AI智能体应用于基准套件的维护（而不是直接用于电路设计），是一个有创意且实用的切入点。特别是`track-bug`和`improve-skills`这类元维护技能，展现了AI在软件工程管理中的潜力。
4.  **设计覆盖广泛**：打破了先前套件对RISC-V和低复杂度设计的过度依赖，其结构多样性分析（Figure 3）很有说服力。

### 不足与局限

1.  **缺乏定量验证**：这是最核心的局限。论文没有提供任何在HighTide上运行ML模型或EDA工具并与现有套件对比的定量结果来证明其“更好”。它是一个**基础设施建设报告**，而非验证该设施优越性的实验报告。
2.  **验证不完整**：文中坦言`post-flow verification & silicon validation`缺失，仅依赖DRC、LVS和STA。这使得套件中的设计是否能达到流片标准存疑。
3.  **内存宏模型误差**：使用抽象的黑盒SRAM模型会牺牲功耗、性能和面积的精度，对ML模型训练和EDA工具评估可能引入偏差。
4.  **流程依赖**：内建流程依赖基于OpenROAD的ORFS，其工具自身的限制会传导给所有设计。虽然理论上可用其他流程，但实际集成和测试工作未展示。
5.  **成本与可及性**：AI智能体依赖于商业化的Claude Code，且远程缓存写入需要权限。这对于完全开源、社区驱动的项目来说，可能存在单一供应商依赖和准入门槛。

（完）
