---
title: "From Rocq to Metal: A Pipeline for Formally Verified Microcontroller Firmware"
title_zh: 从Rocq到Metal：一个形式化验证微控制器固件的流水线
authors: "Valentin Bergeron, Karolina Gorna"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.02651v1"
tags: ["query:llm-hw-fv"]
score: 8.0
evidence: LLM辅助策略合成用于形式验证的微控制器固件
tldr: "随着AI生成代码普及，安全关键系统的形式化验证需求迫切，但现有运行时对微控制器太大。作者构建Encore!裸机CPS虚拟机，运行Rocq提取的Scheme，并将固件结构化为纯状态转换函数，核心完全可验证，未验证主机层恒定。该流水线使AI生成固件可通过LLM辅助战术合成自我证明。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02651v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1976, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02651v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02651v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 816, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02651v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1009, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02651v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1651, \"height\": 475, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02651v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1545, \"height\": 571, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02651v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1066, \"height\": 396, \"label\": \"Table\"}]"
motivation: 微控制器固件需形式化验证以确保安全，但高级语言运行时资源开销过大。
method: "构建Encore!基于CPS的裸机虚拟机，运行Rocq提取的Scheme，固件设计为纯状态转换函数。"
result: 成功在微控制器上运行形式化验证的固件，核心完全可证明，未验证层不随复杂度增加。
conclusion: LLM辅助战术合成可让AI生成固件自动证明自身正确性。
---

## 摘要
在安全关键系统中强制执行不变量随着AI生成代码的普及而日益紧迫。不幸的是，支持高级规范语言所需的运行时对于大多数嵌入式目标来说过于庞大。在本文中，我们展示了如何实现在今天可用的形式化验证固件。我们构建了Encore!，一个裸机延续传递风格（CPS）虚拟机（VM），可在微控制器上运行Rocq提取的Scheme。我们还展示了如何将固件结构化为纯状态转换函数，使其核心在Rocq中完全可证明，同时无论固件复杂度如何，都能保持未验证的主机层不变。大型语言模型（LLM）辅助的策略合成自然融入此工作流：形式定理陈述取代手动代码审查，使AI生成的固件能够自我证明。

## Abstract
Enforcing invariants in safety-critical systems is increasingly urgent as AI-generated code becomes widespread. Unfortunately, the runtimes required to support high-level specification languages are too large for most embedded targets. In this article, we show how formally verified firmware is achievable today. We built Encore!, a bare-metal Continuation Passing Style (CPS) virtual machine (VM) that runs Rocq-extracted Scheme on microcontrollers. We also show how to structure firmware as a pure state-transition function, making its core fully provable in Rocq while keeping the unverified host layer constant regardless of firmware complexity. Large Language Model (LLM)-assisted tactic synthesis fits naturally into this workflow: formal theorem statements replace manual code review, allowing AI-generated firmware to prove itself.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：随着AI生成代码的普及，安全关键系统（如微控制器固件）亟需形式化验证来保证正确性，但现有高级规范语言的运行时（runtime）对资源受限的微控制器（如STM32系列）来说过于庞大，无法直接部署。
- **背景**：传统做法要么手动将形式规范翻译成宿主语言，要么依赖宿主语言扩展，这些方法牺牲了形式化证明的严谨性和可组合性。而大型语言模型（LLM）辅助策略合成为自动生成证明提供了可能，但需要一条从定理证明器到嵌入式硬件的可行路径。
- **目标**：构建一条端到端的流水线，使得在Rocq（Coq的后继）中编写和验证的固件逻辑能够直接提取并运行在Cortex-M微控制器上，并保证核心业务逻辑完全可证明，未验证部分保持恒定。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- 将固件架构设计为**纯状态转换函数** `step: State × Event → State × (list Effect)`，把整个嵌入式设备视为状态机。通过将状态复制而非原地修改、用效果描述（Effect）而非直接执行副作用，最大化可证明逻辑部分。
- 构建**Encore!**：一个裸机、基于延续传递风格（CPS）的字节码虚拟机，运行Rocq提取出的Scheme代码。该VM用`no_std` Rust编写，无标准库依赖，内存分配使用静态bump分配器，垃圾回收为mark-compact方式，所有值用32位自描述字表示。
- 编译流水线：Rocq源码 → 提取为Scheme → 经Encore!编译器（解析、去柯里化、CPS变换、优化、寄存器分配）生成字节码 → 部署到MCU。其中虚拟机作为库嵌入宿主Rust应用中，宿主通过窄接口（函数调用、外部注册）与VM交互。

### 关键技术细节
- **状态转换模型**：`step`函数接收当前状态和输入事件，返回新状态和效果列表。效果由宿主解释执行，事件轮询也由宿主完成。这样，宿主层（TCB）大小不随应用复杂度增长。
- **Encore! VM**：256个寄存器、无调用栈、所有控制流通过单条`ENCORE`指令实现尾调用；使用mark-compact GC，扫描所有寄存器作为根；支持`extern`钩子调用宿主函数。
- **编译器**：CPS优化包括shrinking和expanding阶段（参考Appel和Kennedy的工作），最终生成位置无关的字节码（ENCR格式）。
- **构建时绑定生成**：通过`build.rs`自动生成宿主侧的`funcs`和`ctors`常量模块，确保标识符与字节码一致。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
### 实验场景与数据集
- **基准测试**：7个标准测试用例：`sum`、`fibonacci`、`gcd`、`fsm`（简单有限状态机）、`rbtree`（红黑树，规模为10、50、100个节点）。
- **实际应用**：一个Ledger Flex交易签名应用（400行Gallina核心 + 575行Rust宿主），在真实硬件上验证端到端。
- **硬件目标**：ST33K1M5（64KB RAM, 1.5MB Flash）、ST33J2M0（50KB RAM, 2MB Flash），最紧约束为50KB RAM。仿真使用QEMU（Cortex-M）。

### 对比方法
- **其他提取路径**：Lean 4、OCaml/OMicroB、CertiRocq、Chibi-Scheme、Ribbit。对比指标：是否需要GC、是否支持`no_std`、Cortex-M可行性、证明基础设施保留情况。
- **对比方案**：Encore! vs CertiRocq（针对7个基准测试的Flash占用、最小堆内存、构建时间）。
- **其他Scheme运行时**：Chibi（解释器，二进制超256KB）、Ribbit（不支持quasiquote，无法运行Rocq提取的Scheme）、Encore!（满足所有要求）。

## 4. 资源与算力
- 论文未明确提及使用的GPU型号、数量或训练时长。所有实验是在QEMU模拟的Cortex-M上运行，实际部署在物理Ledger Flex硬件上。构建工具链为Rocq 9.1.1、Rust 1.88等，无需大规模算力。
- 注意：实验不涉及AI/LLM训练，仅使用LLM辅助策略合成作为工作流的一部分，但未给出具体算力需求。

## 5. 实验数量与充分性
- **实验组数**：
  - 基准测试：7组（sum, fibonacci, gcd, fsm, rbtree-10/50/100），对比Encore!和CertiRocq在Flash、堆、构建时间三个维度。
  - 实际应用：1个交易签名应用端到端验证，在真实硬件上运行。
  - 方案可行性：对各提取路径的兼容性评估（表3、4），包含多种运行时（Lean4, OCaml, CertiRocq, Scheme等）。
- **充分性评价**：
  - 实验覆盖了典型计算模式（算术、状态机、数据结构），并考虑了最紧硬件约束。
  - 对比公平：CertiRocq使用了修改的静态bump分配器（禁用GC），Encore!使用mark-compact GC，对比条件基本一致。
  - 缺失：未进行更多嵌入式场景（如实时响应、中断）测试；未测量运行性能（如每秒状态转换数）；未对比多种GC策略。但论文主要验证可行性而非性能极限，实验基本充分。

## 6. 论文的主要结论与发现
- **主要结论**：形式化验证的固件可以在资源受限的Cortex-M微控制器上端到端运行，且核心逻辑完全可证明，未验证层不随复杂度增长。
- **关键发现**：
  - Encore!是唯一满足所有约束的提取路径（与CertiRocq并列，但CertiRocq仅在有限分配场景可行）。
  - 在红黑树基准中，Encore!由于运行时计算而非编译时展开，Flash占用恒定（18KB），而CertiRocq随树规模线性增长（N=100时146KB），超过硬件限制。
  - Encore!的堆需求远小于CertiRocq（如rbtree-100仅3.7KB vs 63KB），且构建时间快数个数量级（N=100时597ms vs 397s）。
  - LLM辅助策略合成可自然融入此工作流：形式定理陈述代替代码审查，AI生成固件可自我证明。

## 7. 优点：方法或实验设计上的亮点
- **架构创新**：将固件结构化为纯状态转换函数，使验证焦点集中，TCB大小固定。这是对嵌入式形式验证领域的重要贡献。
- **VM设计巧妙**：利用CPS消除调用栈，寄存器映射自然，GC根连续，使得垃圾回收简单高效。
- **端到端自动化**：从Rocq提取到部署，通过`build.rs`自动生成绑定，无需手动协调宿主和VM标识符。
- **实际硬件验证**：不仅在仿真中测试，还在真实Ledger Flex设备上运行，证明了可行性。
- **对比实验全面**：系统评估了所有主要提取路径（Lean4、OCaml、CertiRocq、多种Scheme运行时），并给出量化指标。

## 8. 不足与局限
- **实验覆盖不足**：
  - 缺乏对更复杂应用（如网络协议、加密算法）的测试。
  - 未评估真实中断、低功耗模式下的行为。
  - 未比较不同垃圾回收策略对实时性的影响。
- **CertiRocq对比有偏向**：CertiRocq本身需要GC，但实验中使用静态bump分配器（禁用GC），这使得对于有动态分配的应用不利。论文承认可以改进GC但未实现。
- **应用限制**：
  - Encore!目前只支持Rocq提取的特定Scheme子集（宏由`macros_extr.csm`定义），不通用。
  - 需要手工将外部函数声明为公理，可能会引入未验证的信任点。
  - 未讨论多任务或并发场景。
- **未涉及LLM辅助策略合成的具体实现**：论文仅提及这是一个自然工作流，未给出具体工具或实验验证。
- **性能数据有限**：仅测量了Flash、堆、构建时间，未比较运行时速度或能量消耗。对于时间敏感的固件，解释器/VM的动态开销可能难以接受。

（完）
