---
title: Shift-Left High-Level Synthesis Verification via Knowledge-Augmented LLM Agent
title_zh: 基于知识增强的LLM智能体实现左移高层次综合验证
authors: "Zhihan Xiao, Zhe Zhao, Luke Ztz Hu, Songping Mai"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17128v1"
tags: ["query:llm-hw-fv"]
score: 9.0
evidence: 使用大语言模型代理进行高级综合功能一致性验证
tldr: "高综合(HLS)流程中，C规格与HLS-C实现的功能一致性验证耗时易错。现有LLM测试生成存在覆盖率低、环境不一致等问题。提出知识增强的agent驱动左移验证框架，通过双层级一致性检查(静态结构对齐与动态行为等价)、HLS验证知识图谱提供拓扑先验、自主agent协调迭代与诊断。在107个基准对上平均覆盖率达98.26%，动态一致性95.33%，优于多种基线方法。框架开源可复现，显著提升验证效率与可靠性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17128v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17128v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1889, \"height\": 1454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17128v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1877, \"height\": 1113, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17128v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 486, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17128v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1882, \"height\": 684, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17128v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1881, \"height\": 336, \"label\": \"Table\"}]"
motivation: HLS一致性验证依赖人工，LLM方法随机性导致覆盖率不足与结果不可靠，亟需自动化、完备的验证方案。
method: 提出双层级一致性检查(静态结构+动态行为)+HLS验证知识图谱+自主agent迭代优化与诊断的框架。
result: "在107个基准对上平均覆盖率98.26%，动态一致性95.33%，优于AST、检索增强及迭代agent基线。"
conclusion: 知识增强的agent框架有效提升HLS验证自动化水平与效果，开源代码促进实用化。
---

## 摘要
高层次综合通过将C/C++程序转换为硬件实现，实现了快速硬件开发。在HLS设计流程中，验证黄金C规范与面向HLS的C实现之间的功能一致性是一项关键但劳动密集型的任务。尽管大语言模型在自动测试台生成方面展现出前景，但其随机性往往导致覆盖率不足、验证环境不一致以及等价性检查结果不可靠。为解决这些限制，我们提出了一种知识增强的、智能体驱动的左移验证框架，用于在综合前自动检查黄金C与HLS-C实现之间的功能一致性。该框架引入了双层一致性检查机制，共同强制配对测试台之间的静态结构对齐和动态行为等价，同时结合符号执行和覆盖率驱动的细化以提高验证完备性。此外，我们构建了一个异构的HLS验证知识图谱，为测试台生成提供拓扑感知的推理先验，并设计了一个自主验证智能体，以协调异构工具链上的迭代细化和故障诊断。在107个HLS基准对上的实验结果表明，所提框架实现了98.26%的平均覆盖率和95.33%的动态一致性，优于代表性的基于AST、检索增强和迭代智能体的基线方法。代码地址：https://github.com/cz-5f/HLS-LeVeri.git

## Abstract
High-Level Synthesis (HLS) enables rapid hardware development by translating C/C++ programs into hardware implementations. Functional consistency verification between golden C specifications and HLS-oriented C implementations is a critical yet labor-intensive task in HLS design flows. While Large Language Models (LLMs) have recently shown promise in automated testbench generation, their stochastic nature often leads to insufficient coverage, inconsistent verification environments, and unreliable equivalence checking results. To address these limitations, we propose a knowledge-augmented, agent-driven shift-left verification framework for automated functional consistency checking between golden C and HLS-C implementations before synthesis. The framework introduces a Dual-Tier Consistency Checking mechanism that jointly enforces static structural alignment and dynamic behavioral equivalence between paired testbenches, while integrating symbolic execution and coverage-driven refinement to improve verification completeness. Furthermore, we construct a heterogeneous HLS Verification Knowledge Graph to provide topology-aware reasoning priors for testbench generation, and design an autonomous verification agent to orchestrate iterative refinement and failure diagnosis across heterogeneous toolchains. Experimental results on 107 HLS benchmark pairs demonstrate that the proposed framework achieves 98.26\% average coverage and 95.33\% dynamic consistency, outperforming representative AST-based, retrieval-augmented, and iterative agent-based baselines. https://github.com/cz-5f/HLS-LeVeri.git

---

## 论文详细总结（自动生成）

# 基于知识增强的LLM智能体实现左移高层次综合验证 —— 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：高层次综合（HLS）通过将 C/C++ 程序自动转化为 RTL 实现，加速硬件设计。但在实际流程中，算法级规范的“黄金 C”（Golden C）与经过硬件约束适配后的“HLS-C”之间存在大量结构性和语义差异（如动态内存 vs 静态内存、浮点运算 vs 任意精度类型、标准库 vs 硬件接口等）。传统的 C/RTL 协同仿真只能在综合后检查，若功能不一致延迟到 RTL 阶段才发现，调试成本极高。
- **核心问题**：在综合前验证 Golden C 与 HLS-C 的功能一致性（即“左移验证”）是当前 HLS 流程的瓶颈。现有 LLM 驱动的自动测试台生成方法虽有潜力，但存在随机性导致覆盖率低、验证环境不一致、等价性检查不可靠等问题，缺乏统一的机制来同时保证高覆盖率和严格的环境一致性。
- **整体含义**：提出一种知识增强的、智能体（Agent）驱动的左移验证框架，将符号执行、覆盖率反馈、知识图谱先验与 LLM 相结合，在综合前自动生成高覆盖、严格一致的测试台对，并对功能差异进行确定性诊断，从而显著提升 HLS 验证的自动化和可靠性。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 整体架构
目标：给定一对程序（Golden C 记为 P_c，HLS-C 记为 P_h），自动构建高覆盖率且语义一致的测试台对（TB_c, TB_h），并验证 P_c 与 P_h 的功能等价性（或暴露反例）。框架核心组件：
- **双层一致性检查机制**（Dual-Tier Consistency Checking）
- **覆盖率驱动的测试台构建**（Coverage-Driven Testbench Construction）
- **自主验证智能体**（Agent）
- **HLS 验证知识图谱**（HLS Verification Knowledge Graph）

### 2.2 双层一致性检查
将测试台一致性检查分解为两个阶段：
1. **静态结构前提检查**：在仿真前，通过 AST 分析测试台对输入刺激、控制流图（CFG）和数据依赖图（DDG）的结构相似性。使用加权公式（公式 5）计算静态一致性分数 S_static = ω1·S_IO + ω2·S_CFG + ω3·S_DDG，若低于阈值 τ_static 则拒绝，避免无效仿真。
   - 输入刺激一致性 S_IO：基于归一化 Levenshtein 距离。
   - CFG 一致性 S_CFG：控制节点分布相似性和嵌套深度差异。
   - DDG 一致性 S_DDG：Def-Use 链的 Jaccard 系数。
2. **动态行为等价检查**：通过周期级协同仿真，逐周期比较输入序列和输出序列。若输入不同则判定为测试台缺陷（TB_BUG），若输入一致但输出不同则判定为设计缺陷（DESIGN_BUG）。

### 2.3 覆盖率驱动的测试台构建
- **符号执行引导的刺激增强**：集成 KLEE 符号执行引擎，对输入参数进行符号化处理，自动求解路径约束，生成覆盖深度分支和边界情况的刺激向量。
- **闭环覆盖率协调**：使用 gcov 进行动态覆盖率分析（语句/分支/调用覆盖），构造综合覆盖率指标 C_total。若低于阈值 τ_cov，Agent 将未覆盖信息反馈给 LLM 和 KLEE，迭代补充测试用例，直到达到阈值或超时。

### 2.4 自主验证智能体（Agent）
- 定义 Agent 为元组 A = ⟨Σ, T, P⟩，其中 Σ 为状态记忆（覆盖率、一致性分数、执行轨迹、错误日志），T 为工具链接口（LLM、KLEE、gcov、g++、Vitis HLS），P 为基于 LLM 的推理和规划引擎。
- **验证调度**：每轮迭代依次执行覆盖率评估、静态一致性检查、动态协同仿真。若失败则触发对应类型的修复循环。
- **故障诊断策略**：将失败分为四种类型（覆盖率不足、静态不一致、动态输入不匹配、设计缺陷），每种对应特定的修复动作（如补充路径约束、对齐测试台结构、同步输入、报告反例）。

### 2.5 知识图谱增强的验证
- **数据集构建**：基于 Chrysalis-HLS 数据集，通过数据增强和自动化验证流程（KLEE + gcov + 双层一致性过滤）生成 1,597 组高质量 4 元组 (P_c, P_h, TB_c, TB_h)。
- **知识图谱结构**：构建异构有向图 G = ⟨V,E,R⟩，包括覆盖知识图谱（Gcov）和语义知识图谱（Gsem）。Gcov 存储程序结构模式与刺激策略的关系；Gsem 存储配对测试台之间的结构对齐规则。
- **推理流程**：对新输入程序对，构造查询子图 G_query，通过加权评分（嵌入余弦相似度、拓扑匹配率、语义邻居奖励）从图谱中检索最相似示例，将抽象推理规则（而非原始代码）注入 LLM 提示，引导初始测试台生成，减少搜索空间。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **数据集**：基于 HLSTrans 基准（包含来自 HLSFactory、ForgeBench、C2HLS、MachSuite、PP4FPGA 等数据集的配对 C 和 HLS-C 程序），经严格去重后保留 107 个功能独立的程序对作为测试目标，涵盖数值计算、信号处理、控制密集型设计等。
- **数据集质量**：作者构建的验证数据集（1,597 个 4 元组）在语法正确性 100%、平均覆盖率 98.08%、静态一致性 0.7998、动态一致性 100%。
- **对比方法**：
  - **AST-based Knowledge Graph**：Chinthareddy 的基于 AST 的图检索增强方法。
  - **RAG-based**：SynthAI，检索相关代码片段引导 LLM 生成。
  - **Iteration-based Agent**：ConfiBench，通过执行反馈迭代优化测试台。
- **评估指标**：语法正确率（Syntax Rate）、综合覆盖率（C_total）、静态一致性分数（S_static）、动态一致性率（Dynamic Rate）。

## 4. 资源与算力

- 论文中 **未明确说明** 使用的 GPU 型号、数量、训练时长或推理集群配置。仅提及实验基于 GPT-4.5-mini、Gemini-3-pro、GLM-5 等模型，但未给出具体部署硬件的细节。

## 5. 实验数量与充分性

- **实验组数**：
  - 消融实验：4 种配置（Zero-Shot、KG-Only、Agent-Only、KG+Agent）在 GPT-4.5-mini 上全面对比；另在 GLM-5、GPT-4.5-mini、Gemini-3-pro 三个模型上进行了模型泛化消融。
  - 与基线对比：3 种基线方法（AST-based KG、RAG-based、Iteration-based Agent）与所提框架在 107 个基准对上全面对比。
  - 数据集质量统计：展示了整体数据集指标。
  - 所有实验均报告了 Syntax Rate、Coverage（语句/分支/调用/平均）、静态一致性（输入/CFG/DDG/平均）、Dynamic Rate 共 4~5 个维度。
- **充分性与公平性**：
  - 消融设计合理，能独立评估 KG 和 Agent 的贡献。
  - 基线方法均按原论文核心思想复现，且控制迭代轮数（最大 3 轮）与计算预算对齐（非迭代基线采用 pass@3）。
  - 阈值（τ_cov=0.95, τ_static=0.75）统一设置，避免倾向。
  - 对比表格完整，未遗漏重要基线。
  - **不足**：未在多模态、更大规模基准上验证，仅使用 HLSTrans 中 107 对，样本量偏小；未与形式化验证工具（如 HLS 形式化方法）对比；未报告运行时间或计算成本。

## 6. 论文的主要结论与发现

- 所提框架（KG + Agent）在 107 个基准对上实现了 **98.26% 平均覆盖率** 和 **95.33% 动态一致性率**，显著优于所有基线（最佳基线 Iterative Agent 的动态率为 85.05%）。
- 知识图谱提供结构先验，能有效提升初始测试台质量（KG-Only 相比 Zero-Shot 动态率从 67.29% 提升至 75.70%）。
- Agent 的闭环细化对修复执行错误和保证高覆盖率至关重要（Agent-Only 动态率达到 86.92%）。
- 完整框架组合后，较弱模型（如 GLM-5）性能也大幅提升，且模型间差距缩小，表明框架将开放生成问题转化为更可控的推理任务。
- 静态一致性检查能有效避免无效仿真，动态一致性检查可准确区分测试台缺陷与设计缺陷。

## 7. 优点

1. **创新性**：提出双层一致性检查机制，将验证环境对齐与设计验证解耦，是首个系统性解决 HLS 验证中测试台不一致问题的方案。
2. **系统性**：将 LLM、符号执行、覆盖率反馈、知识图谱、自主智能体有机结合成闭环，工具链整合完整。
3. **可复现**：开源代码，提供详细实验设置，数据集构建流程透明。
4. **鲁棒性**：在多个模型上验证了泛化能力；框架对较弱模型提升显著，降低了基座模型能力依赖。
5. **有效性**：全部评估指标均优于现有方法，尤其动态一致性大幅领先，验证了实际工程价值。
6. **可扩展性**：知识图谱可增量更新，故障诊断策略可扩展至更多失败类型。

## 8. 不足与局限

1. **实验规模有限**：仅使用 107 个基准对，且主要来自 HLSTrans 数据集，未在更大型、更多样的 HLS 设计（如整个芯片子系统）上验证。
2. **缺乏运行效率分析**：未报告每轮迭代的平均耗时、总验证时间、推理成本等工程实用性指标。
3. **未与形式化验证方法对比**：未与 Karfa 等的形式化验证或 HLSTester（KLEE 直接对比）等工具在相同基准上比较。
4. **阈值设定依赖经验**：τ_static=0.75、τ_cov=0.95 的取值依据不够清晰，可能存在偏差风险。
5. **知识图谱构建依赖人工数据增强**：虽然基于 Chrysalis-HLS 并自动化过滤，但仍需原始高质量数据集，对新设计模式可能覆盖不足。
6. **未考虑时序收敛与综合后验证**：框架仅针对综合前 C 级功能等价，未扩展到 RTL 级等价性或时序约束验证。
7. **Agent 的故障诊断策略**：目前仅定义了 4 种失败类型，实际应用中可能遇到更多异常（如资源冲突、时序违规），需要进一步扩展。

（完）
