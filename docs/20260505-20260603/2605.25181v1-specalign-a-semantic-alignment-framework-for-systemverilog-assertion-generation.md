---
title: "SpecAlign: A Semantic Alignment Framework for SystemVerilog Assertion Generation"
title_zh: SpecAlign：一种面向SystemVerilog断言生成的语义对齐框架
authors: "Jaime Rafael Imperial, Hao Zheng"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.25181v1"
tags: ["query:llm-hw-fv"]
score: 9.0
evidence: LLM生成SystemVerilog断言并进行语义对齐
tldr: 现有LLM生成SystemVerilog断言主要关注语法正确性，但语义对齐难以量化，导致幻觉断言降低信任度。本文提出SpecAlign框架，通过基于蕴含的分类和自一致性投票的迭代对齐循环，评估并优化自然语言属性与断言间的语义一致性。实验表明，无需golden RTL即可有效检测语义不一致并提升对齐质量。该工作为SVA生成提供了可扩展的语义评估与改进手段，补充了传统形式验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25181v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2605.25181v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1774, \"height\": 824}, {\"url\": \"assets/figures/arxiv/2605.25181v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 817, \"height\": 607}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25181v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 816, \"height\": 159}, {\"url\": \"assets/tables/arxiv/2605.25181v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 156}, {\"url\": \"assets/tables/arxiv/2605.25181v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 400}, {\"url\": \"assets/tables/arxiv/2605.25181v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1792, \"height\": 528}]"
motivation: 现有LLM生成SVA缺乏语义对齐度量，导致幻觉断言降低信任度和增加调试成本。
method: SpecAlign通过基于蕴含的分类和自一致性投票的迭代对齐循环，评估并优化断言语义一致性。
result: 无需golden RTL即可有效检测语义不一致，并通过迭代反馈提升断言对齐。
conclusion: SpecAlign为SVA生成提供了语义评估与改进的可扩展方案，补充传统形式验证。
---

## 摘要
现有的大语言模型（LLM）在生成SystemVerilog断言（SVA）时，主要关注语法有效性和形式验证结果，而生成的断言与自然语言规范之间的语义对齐程度仍难以量化。因此，在缺乏黄金RTL的情况下，产生幻觉或未对齐的SVA会降低可信度并增加调试工作量。本文提出SpecAlign——一个用于LLM生成的SVA的语义评估与优化框架。SpecAlign引入两个迭代对齐循环，利用基于蕴含的分类方法，针对设计规范评估自然语言属性与SVA。我们通过链式思维提示生成多条推理路径，并利用自一致性投票机制进行聚合，从而改进对齐决策。对未对齐的断言进行分析，生成可操作的反馈以进行优化。我们进一步定义了一个量化对齐分数，用于衡量各迭代间的语义一致性。实验结果表明，SpecAlign能有效检测语义不一致性，并在不依赖黄金RTL的情况下提升断言对齐度，为传统形式验证评估指标提供了可扩展的补充。

## Abstract
Existing Large Language Model (LLM) approaches to SystemVerilog Assertion (SVA) generation primarily focus on syntactic validity and formal verification outcomes, while semantic alignment between generated assertions and natural language specifications remains difficult to quantify. As a result, hallucinated or misaligned SVAs can reduce confidence and increase debugging efforts in the absence of golden RTL. This paper presents SpecAlign, a framework for semantic evaluation and refinement of LLM-generated SVAs. SpecAlign introduces two iterative alignment loops that assess both natural language properties and SVAs against the design specification using entailment-based classification. We improve alignment decisions by generating multiple reasoning paths using chain-of-thought prompting and aggregating them via a self-consistency voting mechanism. Misaligned assertions are analyzed to generate actionable feedback for refinement. We further define a quantitative alignment score to measure semantic consistency across iterations. Experimental results demonstrate that SpecAlign effectively detects semantic inconsistencies and improves assertion alignment without relying on golden RTL, providing a scalable complement to traditional formal verification evaluation metrics.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

现有的大语言模型（LLM）在生成 SystemVerilog 断言（SVA）时，主要聚焦于语法的有效性（syntactic validity）以及是否能在形式验证工具（如 JasperGold）中得到通过或反例。然而，**生成的断言是否真正与自然语言规范所表达的语义一致（semantic alignment）**仍然难以量化。缺乏 golden RTL 时，幻觉（hallucinated）或语义未对齐的断言会导致信任度下降和调试负担增加。形式验证结果（如 proven）无法保证断言反映了规范意图（例如断言在时钟沿采样自身，虽然可被证明，但语义空洞）。因此，本文提出 **SpecAlign** 框架，旨在**不依赖 golden RTL** 的情况下，通过迭代对齐循环来评估并优化 LLM 生成断言的语义一致性，作为形式验证的补充。

## 2. 论文提出的方法论

### 核心思想
- **两阶段对齐循环**：① Property Alignment Loop：检查从规范中提取的自然语言属性（P）与设计规范（S）的对齐。② SVA Alignment Loop：将生成的 SVA 转换为自然语言摘要（N），再与规范或已对齐的属性库进行对齐检查。
- **基于蕴含的分类**：将每个描述（X）分为三类：
  - **Entails**（1）：所有元素被规范支持。
  - **Contradicts**（0）：至少一个元素与规范矛盾。
  - **Unknown**（2）：至少一个元素既不被支持也不被矛盾。
- **自一致性投票**（Self-Consistency）：对同一条描述，利用 Chain-of-Thought（COT）提示生成 k 条推理路径，每条路径给出子判决 \(v_{i,j}\)，再通过多数投票得到最终判决 \(V_i\)。平局时优先 Contradicts > Unknown > Entails。
- **反馈与优化**：Contradicts 的描述进入 Inconsistency Analysis，生成结构化反馈（指出矛盾元素、正确行为），并驱动 LLM 重新生成属性或 SVA。
- **证据提取**：Entails 和 Unknown 的描述进入 Evidence Extraction，提取规范中的支持证据或识别假设，供人工审查。

### 关键技术细节
- **自然语言摘要提取**：将 SVA 分解成 trigger、antecedent、implication op、consequent 等组件，生成单句自然语言描述。
- **参考文件选择**：Property Loop 用规范；SVA Loop 优先使用已对齐的属性库（若存在），否则用规范。
- **对齐分数**：\(a_t = \frac{E}{n}\)，其中 \(E\) 为 Entails 数，\(n\) 为总断言数，目标是在固定迭代次数内提高 \(a_t\)。
- 使用 GPT-5 作为后端，设置 k=3 条推理路径，最大 3 次迭代，推理 effort 设为 high。

## 3. 实验设计

### 数据集
- **APB**：APB to I2C 接口规范（12 页，2,746 token）
- **UART**：UART to Bus 接口规范（10 页，3,160 token）
- 来自 OpenCores 和 AssertLLM 相关工作。

### Benchmark 与对比方法
- **Baseline**：AssertionForge（基于知识图谱从规范和 RTL 联合生成 SVA）。
- 对比指标：总 SVA 数（#SVA）、对齐分数（AS）、以及 Entails (E)、Contradicts (C)、Unknown (U) 的分布。
- 额外对比：JasperGold 的形式验证结果与 SpecAlign 语义标签的交叉表（Table I），以展示形式验证无法捕捉语义矛盾的案例。

## 4. 资源与算力

论文**未明确说明**所使用的 GPU 型号、数量、训练时长等算力信息。仅提及后端为 GPT-5（OpenAI 的模型），推测依赖 API 调用而非本地训练。设置了 3 次迭代、3 条推理路径以及高推理 effort，但未报告总成本或耗时。

## 5. 实验数量与充分性

- **实验数量**：在 2 个设计（APB、UART）上运行，每个设计对比了 baseline（AssertionForge）和 SpecAlign 的结果。还给出了 SpecAlign 迭代过程的 aligned score 变化（图3），以及 Table I 的交叉分析。
- **消融实验**：未进行严格的消融（如移除自一致性、移除反馈、改变迭代次数等）。
- **充分性评估**：实验覆盖了两种不同复杂度的通信协议，但**规模较小**，缺乏更大型、更多样的设计（如 ARM 总线、内存控制器等）。对比方法单一（仅 AssertionForge），未与 ASSERTLLM、SANGAM 等其他方法比较。实验设计客观，但充分性有限。

## 6. 论文的主要结论与发现

- **形式验证不能保证语义对齐**：Table I 显示，46 个被 JasperGold 证明（Proven）的断言，SpecAlign 标记为 Contradicts，说明这些断言虽可通过验证，但语义与规范矛盾。
- **SpecAlign 显著降低矛盾断言**：在 APB 上，矛盾断言从 148 降至 0；UART 上从 142 降至 6。对齐分数从 0.02/0.06 提升至 0.15/0.28。
- **Unknown 类别增加**：反映了 SpecAlign 的保守分类策略，将实现依赖细节归于未知，更好地引导人工审查或规范补充。
- **迭代有效**：在 3 次迭代内，每轮对齐分数逐步上升（图3）。

## 7. 优点

- **新颖的语义评估框架**：首次明确将 LLM 生成 SVA 的语义对齐问题从形式验证中分离，提出无需 golden RTL 的评估与优化方法。
- **自一致性机制**：利用多条推理路径和投票提高判决可靠性，减少 LLM 单次输出偏差。
- **可解释性**：通过证据提取和结构化反馈，提供规范引用和矛盾说明，便于人工审计或自动修正。
- **易集成**：设计为两个对齐循环，可插入现有 LLM 生成流程中。

## 8. 不足与局限

- **实验规模小、基准少**：仅在两个设计上验证，对比方法只有 AssertionForge，缺乏与 ASSERTLLM、SANGAM、Self-Refine 等多种方法的公平比较。
- **未知类的处理尚不完善**：Unknown 断言可能包含有用验证意图，目前仅提取假设，未提供自动优化策略。
- **依赖 LLM 本身**：对齐分类和反馈生成仍依赖 LLM 的推理能力，存在二次幻觉的风险。
- **未考虑 RTL 错误**：论文指出若 RTL 含错误，则 Entails 的断言产生反例可能表示 RTL 问题，但本文未评估该场景。
- **计算开销未报告**：未提供时间或 token 消耗，实际使用中 API 调用成本可能较高。
- **规范完整性影响**：如果规范本身不完整或模糊，SpecAlign 的分类准确性会下降。

（完）
