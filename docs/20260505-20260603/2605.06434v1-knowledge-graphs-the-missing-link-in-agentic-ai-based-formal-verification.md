---
title: "Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification"
title_zh: 知识图谱：基于智能代理的AI形式验证中缺失的一环
authors: "Vaisakh Naduvodi Viswambharan, Keerthan Kopparam Radhakrishna, Deepak Narayan Gadde, Aman Kumar"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06434v1"
tags: ["query:llm-hw-fv"]
score: 9.0
evidence: 使用大语言模型和知识图谱进行硬件形式验证
tldr: "大型语言模型（LLM）在从自然语言规范生成SystemVerilog断言（SVA）时，常因规范模糊、缺乏寄存器传输级（RTL）细节导致语义不匹配和语法错误。本文提出构建验证中心的知识图谱（KG），从规范、RTL和形式工具反馈中提取结构化中间表示，并通过多智能体工作流驱动语法修复、反例修正和覆盖导向增强三个优化循环。在七个基准设计上，该方法提升了规范到RTL的接地，生成可编译SVA且语法修复开销低，形式覆盖率达78.5%-99.4%，但复杂时序与算术推理仍面临挑战。该工作展示了知识图谱在形式验证中桥接规范与设计的潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06434v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1881, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2605.06434v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 542}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.06434v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 956, \"height\": 355}, {\"url\": \"assets/tables/arxiv/2605.06434v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 2012, \"height\": 693}, {\"url\": \"assets/tables/arxiv/2605.06434v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1906, \"height\": 383}]"
motivation: 现有LLM生成SVA方法因规范模糊和缺乏RTL细节导致语义错配与语法失败。
method: 构建多源知识图谱，通过多智能体工作流查询更新，驱动语法修复、反例修正和覆盖增强循环。
result: "七个基准上实现78.5%-99.4%形式覆盖率，语法修复开销低，但复杂推理仍受限。"
conclusion: 知识图谱有效提升规范到RTL的接地，为LLM驱动的形式验证提供可靠上下文。
---

## 摘要
大规模语言模型（LLM）的最新进展使得从自然语言规范生成SystemVerilog断言（SVA）的工作流程成为可能，这有望加速形式验证（FV）。然而，高质量的断言合成仍然具有挑战性，因为规范通常模糊或不完整，且关键的微架构细节位于寄存器传输级（RTL）中。许多现有方法将规范和RTL视为松散结构的文本，这削弱了规范到RTL的关联，导致语义不匹配以及形式解析和细化过程中频繁的语法错误。本文通过构建以验证为中心的知识图谱（KG）来解决这些局限，该图谱从规范、RTL和形式工具反馈（包括语法诊断、反例（CEX）和覆盖率报告）中提取的结构化中间表示（IR）构建而成。KG链接需求、设计层次结构、信号、假设和属性，为生成提供可追溯的、基于设计的上下文。一个多智能体工作流程查询并更新该KG以生成SVA，并驱动三个优化循环：工具诊断指导的语法修复、利用跟踪链接的CEX指导修正以及覆盖率导向的属性增强。在七个基准设计上的评估表明，基于KG的上下文检索改善了规范到RTL的关联，并持续生成低语法修复开销的可编译SVA。该方法实现了78.5%到99.4%的形式覆盖率，尽管收敛性表现出对设计的依赖性，且复杂的时序和算术推理对当前LLM的能力仍具有挑战性。

## Abstract
Recent advances in Large Language Models (LLMs) have enabled workflows that generate SystemVerilog Assertions (SVAs) from natural-language specifications, with the potential to accelerate Formal Verification (FV). However, high-quality assertion synthesis remains challenging because specifications are often ambiguous or incomplete and critical micro-architectural details reside in the Register Transfer Level (RTL). Many existing approaches treat the specification and RTL as loosely structured text, which weakens specification-to-RTL grounding and leads to semantic mismatches and frequent syntax failures during formal parsing and elaboration. This work addresses these limitations with a verification-centric Knowledge Graph (KG) constructed from structured Intermediate Representations (IRs) extracted from the specification, RTL, and formal-tool feedback, including syntax diagnostics, Counterexamples (CEXs), and coverage reports. The KG links requirements, design hierarchy, signals, assumptions, and properties to provide traceable, design-grounded context for generation. A multi-agent workflow queries and updates this KG to generate SVAs and to drive three refinement loops: syntax repair guided by tool diagnostics, CEX-guided correction using trace links, and coverage-directed property augmentation. Evaluation across seven benchmark designs indicates that KG-based context retrieval improves specification-to-RTL grounding and consistently produces compilable SVAs with low syntax-repair overhead. The approach achieves formal coverage ranging from 78.5% to 99.4%, though convergence exhibits design dependence with complex temporal and arithmetic reasoning remaining challenging for current LLM capabilities.

---

## 论文详细总结（自动生成）

# 基于知识图谱的智能代理AI形式验证方法：详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）可从自然语言规范自动生成SystemVerilog断言（SVA）以加速形式验证（FV）。然而，现有方法面临两大挑战：
  - 规范常存在模糊、不完整，且关键微架构细节隐含在寄存器传输级（RTL）代码中，规范与RTL之间缺乏显式关联。
  - 生成的SVA常出现语法错误和语义不匹配，需要大量人工迭代修正。
- **核心问题**：如何有效连接规范、RTL设计与形式工具反馈，实现稳定、可追溯、高覆盖的断言自动生成与自动修正。
- **整体含义**：提出以**验证中心的知识图谱（Knowledge Graph, KG）**为核心，将规范意图、RTL结构、生成属性、工具诊断和覆盖率结果整合为结构化、可查询的上下文，驱动多智能体协作工作流，实现从规范到形式验证的自动化闭环。

## 2. 方法论

- **核心思想**：构建一个**验证中心的知识图谱**，作为所有验证制品的统一注册表和上下文枢纽，使多智能体系统能够基于显式、可追溯的链接生成和优化SVA。
- **关键技术细节**：
  1. **中间表示（IR）**：将规范、RTL和形式工具输出解析为类型化的JSON IR制品，包括：
     - `spec_chunks.json`（规范分块）、`requirements.json`（需求）、`testplan.json`（验证计划）、`design_model.json`（RTL元数据，含模块层次、信号、FSM等）、`properties.json`（生成的SVA属性）、`tracelinks.json`（追溯链接）、`formal_results.json`（形式验证结果）、`cex_cases.json`（反例）、`coverage_metrics.json`（覆盖率指标）、`run_context.json`（运行上下文）。
  2. **知识图谱构建**：从IR制品导出节点和边（nodes.csv, edges.csv），使用NetworkX构建运行时KG。节点类型包括SpecChunk、Requirement、Property、FormalResult、CoverageMetrics等；关系包括derives_from、validates、proves、fails、covers等。
  3. **多智能体工作流**（分为四个子系统）：
     - **属性生成代理**：sva_lead协调，spec_analyst分解需求，sva_author生成SVA代码，sva_reviewer审核，sva_patcher修正，code_extractor组装SVA文件。
     - **语法修正代理**：syntax_analyzer解析编译错误，syntax_fixer进行确定性修复（如未声明标识符、宏缺失等），syntax_validator隔离测试，最多3次尝试，失败则注释并记录。
     - **反例（CEX）修正代理**：vcd_parser提取时序行为，spec_assertion_analyzer分类根因（RTL bug、过约束、缺少假设等），rtl_analyzer分析RTL逻辑，cex_fixer调整属性或假设。
     - **覆盖率改进代理**：cov_analyzer分析覆盖率缺口，cov_processor链接缺口到需求，cov_improver生成覆盖属性和断言。
  4. **上下文检索**：KG根据任务锚点（如需求或属性）提取局部邻域作为上下文，包含相关需求、规范片段、RTL信号、相关属性及工具反馈。
- **算法流程**（文字描述）：
  - 输入：自然语言规范、RTL设计、配置文件。
  - 步骤1：处理规范，提取需求；解析RTL，提取模块层次、信号、参数等元数据。
  - 步骤2：为每个需求构建KG上下文邻域，多智能体生成SVA。
  - 步骤3：将SVA注入形式工具运行，收集结果、反例、覆盖率。
  - 步骤4：根据结果触发语法修正循环、CEX修正循环、覆盖率改进循环（各最多3次迭代）。
  - 步骤5：更新KG和IR制品，保留追溯链接。

## 3. 实验设计

- **数据集/场景**：7个开源基准设计：
  - FIFO（先进先出队列）、Lemming（游戏状态机）、ALU（算术逻辑单元）、CIC Decimator（级联积分梳状抽取器）、BST（二叉搜索树）、TTC Counter（完成时间计数器）、CSR APB Interface（控制/状态寄存器APB总线接口）。
  - 数据来源：OpenCores和CVDP基准套件（Pinckney et al., 2025）。
- **Benchmark**：无公开标准benchmark，但设计覆盖了不同复杂度（组合逻辑、状态机、数据结构、时序逻辑、总线协议等）。
- **对比方法**：论文未设置独立的基线对比实验（如与AssertLLM、ChIRAAG、AssertionForge等直接比较），而是通过自身多步流程的前后对比（如语法修正前后、CEX修正前后、覆盖率改进前后）评估效果。但文中引用了相关工作的局限性作为间接对比。
- **关键评估指标**：生成的属性总数/通过数/失败数、形式覆盖率（%）、语法修正尝试次数、空属性数量、KG规模（节点/边数）。

## 4. 资源与算力

- 论文**未明确说明**所使用GPU型号、数量、训练时长等计算资源信息。
- 仅提及使用**GPT-5.2模型**（推测为LLM）和**Cadence JasperGold**作为形式验证工具。推断实验在云或本地服务器上完成，但具体算力无细节。

## 5. 实验数量与充分性

- **实验数量**：表中报告了7个设计上的完整结果，每个设计包含多个指标（属性数量、语法修正次数、CEX修正结果、覆盖率等）。在属性生成阶段、语法修正、CEX修正、覆盖率改进各阶段均给出了数值。
- **充分性与客观性**：
  - 实验覆盖了多种设计类型（FIFO、状态机、运算单元、树结构、计数器、总线接口），具有一定代表性。
  - 指标维度较全（语法、覆盖率、空属性、修正率等），但缺乏与现有方法的直接定量对比。
  - 未进行消融实验（如去掉KG上下文、去掉某个代理循环等），因此难以严格归因于KG的作用。
  - 仅使用GPT-5.2单一LLM，未测试其他模型（如GPT-4、开源模型），存在模型偏差。
- **公平性**：未说明超参数、温度、种子等设置，难以保证可复现性。

## 6. 主要结论与发现

- KG驱动的上下文检索能够**显著提升规范到RTL的关联**，使生成的SVA语法编译错误率大幅降低（低语法修正开销）。
- 结合语法、CEX、覆盖率三个修正循环，**形式覆盖率可达78.5%~99.4%**（终值，各设计不同）。
- **语法修正**效果一致良好，多数设计仅需少量尝试（0~3次）。
- **CEX修正**在部分设计（FIFO、CSR）上有效提升通过率，但在BST等需要全局数据结构不变式的设计上收敛困难。
- **空属性**问题在CIC Decimator和BST中较突出，表明需要更好的环境建模。
- **复杂时序推理（如重置行为、多周期运算）和全局归纳推理**仍是当前LLM能力瓶颈，自动化修正往往无法解决。

## 7. 优点

- **创新性**：首次将知识图谱作为“验证中心枢纽”集成规范、RTL、形式工具输出，实现全链路可追溯，而非仅用KG做一次性上下文检索。
- **系统化**：多智能体工作流分工明确，每个代理职责清晰，且支持闭环迭代（语法→CEX→覆盖率），体现了agentic AI的完整思路。
- **可追溯性**：所有制品以IR储存，KG保持关系链接，支持增量更新（仅需重新验证受影响的部分）。
- **实用性**：在7个实际设计上验证，覆盖率结果较好（多数超过80%），表明该方法有潜力减少人工SVA编写工作。

## 8. 不足与局限

- **缺乏基线对比**：未与现有关键方法（如AssertLLM、ChIRAAG、AssertionForge等）进行定量比较，无法明确KG的边际收益。
- **消融实验缺失**：无法证明每个组件（KG、各修正循环）的独立贡献，尤其是KG相对于普通RAG或结构化提示的优越性。
- **LLM单一性**：仅用GPT-5.2，未测试其他LLM或不同参数量下效果，泛化性存疑。
- **实验规模有限**：仅7个设计，且部分设计（如BST、TTC Counter）和覆盖率较低，未涉及更大规模或工业级设计。
- **计算资源未报告**：缺乏运行时间和成本分析，实际应用可行性不明。
- **依赖外部工具**：形式验证工具为Cadence JasperGold（商业工具），实验中未提供免费替代方案，可能限制复现。
- **深度局限**：在复杂时序、算术和数据结构不变式上表现不佳，表明当前方案在真正需要深入推理的场景仍有瓶颈。
- **零风险管理**：未讨论LLM幻觉导致的错误断言风险；如果系统自动接受错误属性，可能误导验证结论。

（完）
